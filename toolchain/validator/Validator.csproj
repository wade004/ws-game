<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <AssemblyName>Validator</AssemblyName>
    <RootNamespace>Toolchain.Validator</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- 阶段 3 整理"事项四"：改引用 Core.Carriers.csproj（经其传递引用 Core.Rules.csproj），
         调用 Core.Carriers.Assembly.CarriersSchemaCatalog.RegisterAll 而不是只到 L2 的
         Core.Rules.Assembly.RulesSchemaCatalog.RegisterAll，覆盖 L3 item/creature/gobj 的
         TableSchema/IValidationRule，见 Program.cs 判断记录。 -->
    <ProjectReference Include="../../core/carriers/Core.Carriers.csproj" />
    <ProjectReference Include="../../adapters/stub/Adapters.Stub.csproj" />
  </ItemGroup>

</Project>
