# 阶段 0：无第三方依赖。
# validate_data.py 只使用 Python 标准库（argparse、json、pathlib、re、sys）。
# 后续阶段如需第三方包，请在此追加并注明用途（例如：某包 —— 用于某检查项/某构建步骤）。

# 阶段 5：资产导入工具（import_assets.py / toolchain/asset_import/）新增依赖。
# 工程收尾 K（CI）新增判断记录：本文件只列"必需"依赖——CI 与本地一键门禁（check.ps1）都要用到
# Pillow（占位资产生成器一致性检查 gen_placeholder_assets.py --check 依赖它）；rembg 是一个较重的
# 可选依赖（体积大、且需要本机预先准备好 ~/.u2net/*.onnx 权重才真正可用，见下方 requirements-
# optional.txt 头注释），CI 环境不需要装它，拆到 requirements-optional.txt 单独安装，避免每次 CI
# 运行都白白花时间下载一个大多数校验路径用不到的包。
Pillow>=10.0  # 必需 —— 精灵/图标/特效帧的抠图、裁边、缩放、图集打包（sprite/icon/vfx 子命令）。
