# 工程收尾 K 新增：仅约束 .githooks/ 下的文件强制用 LF 换行、正常纳入文本 diff。
#
# 判断记录：本机 `core.autocrlf=true`（Windows 常见配置），默认会在检出到工作目录时把仓库里的
# LF 转成 CRLF。`.githooks/pre-commit` 是一个 POSIX sh 脚本，Windows Git Bash 调用 `sh.exe`
# 解释执行时，脚本首行 `#!/bin/sh` 如果带 CRLF（`#!/bin/sh\r`），在多数 sh 实现下会被当成整行
# shebang 的一部分从而解析失败或产生难以排查的问题；任务硬性规则明确要求该钩子"LF 换行"。用
# `.gitattributes` 显式声明 `.githooks/*` 为 `text eol=lf`，不管本机/协作者的 `core.autocrlf`
# 如何配置，检出到工作目录时这一路径下的文件始终保持 LF，覆盖全局 autocrlf 设置（Git 的
# `.gitattributes` 逐路径规则优先级高于全局 `core.autocrlf`）。范围只限定 `.githooks/`，不影响
# 仓库其它任何文件的换行策略。
.githooks/* text eol=lf

# ---------------------------------------------------------------------------
# 通用文本文件类型换行规范（2026-09-07 新增）
# ---------------------------------------------------------------------------
#
# 仓库既有约定：源码/文档/数据一律 LF 存储与检出（`git ls-files --eol` 显示绝大多数文件已经是
# `i/lf w/lf`）。这里把常见文本类型显式声明为 `text eol=lf`，二进制资源类型显式声明 `binary`
# （`-diff -merge -text`），避免依赖 Git 内容嗅探（`*.otf`/`*.ttf` 目前虽已被 Git 自动识别为
# 二进制，显式声明更可靠、不随内容变化）。下面"Unity 批处理自行改写的文件"一节对其中三类路径
# （`ProjectSettings/*.asset`、`Packages/manifest.json`、`Packages/packages-lock.json`）声明了
# `eol=crlf` 例外——Git 的 `.gitattributes` 按"同一属性最后一条匹配规则生效"，那几条例外必须
# 写在本节之后才能覆盖这里的 `*.asset`/`*.json` 通用规则，不能颠倒顺序。
*.cs text eol=lf
*.py text eol=lf
*.ps1 text eol=lf
*.md text eol=lf
*.json text eol=lf
*.txt text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.csproj text eol=lf
*.sln text eol=lf
*.props text eol=lf
*.asmdef text eol=lf
*.meta text eol=lf
*.unity text eol=lf
*.mat text eol=lf
*.asset text eol=lf
*.shader text eol=lf
*.shadergraph text eol=lf
*.cginc text eol=lf
*.hlsl text eol=lf
*.gitattributes text eol=lf
*.gitignore text eol=lf

*.dll binary
*.exe binary
*.png binary
*.wav binary
*.otf binary
*.ttf binary

# ---------------------------------------------------------------------------
# 根治门禁噪音：Unity 批处理自行改写的文件固定行尾（2026-09-07 新增；同日勘误见下）
# ---------------------------------------------------------------------------
#
# 勘误（2026-09-07 二次实测）：上一版判断记录断言"三者 Unity 写出的字节都是 CRLF"并据此把这三类
# 路径声明为 `eol=crlf`，结论有误——把"本机 `core.autocrlf=true` 检出时的 CRLF"误当成了
# "Unity 自己写出的 CRLF"，两者被 `eol=crlf` 声明混在一起后无法用 `git status`/`git diff`
# 区分（检出即 CRLF，Unity 若也写 CRLF 自然比对干净，误导了上一版结论）。
#
# 实测方法与结果：先 `git checkout --` 把 `ProjectSettings.asset` 复原成 `eol=crlf` 检出态
# （`git ls-files --eol` 确认三者均为 `i/lf w/crlf attr/text eol=crlf`），确认无残留 Unity
# 进程后，用 PowerShell 按字节扫描文件判断行尾（数 `\n` 前一字节是否为 `\r`），排除"看 git
# status/diff"这种会被 `eol=` 归一化污染判断的方法：
#   - 单独跑 `check.ps1`"Unity 编译检查"那一步的命令（`-batchmode -nographics -quit`，不联测试
#     不构建）：连跑两次，`ProjectSettings.asset` 字节内容与哈希均不变（Unity 只刷新 mtime，
#     不改内容），字节仍是纯 CRLF——这一步不触发实际内容变化，不能拿来复现问题。
#   - 跑 `Unity EditMode 测试`（`-runTests -testPlatform EditMode`）：同样字节/哈希不变，纯
#     CRLF，不复现。
#   - 跑一次真正的构建（`-buildWindows64Player`，对应"独立版构建 + -gf-smoke 冒烟"那一步）：
#     `ProjectSettings.asset` 内容真正发生变化（哈希不同、体积从 21320 字节变为 20614 字节，
#     `git status` 报 `M`，`git ls-files --eol` 变成 `i/lf w/lf`），按字节扫描确认整份文件
#     706 处换行全部是纯 `\n`（0 处 `\r\n`）——Unity 在真正需要回写内容变化时，写出的是 LF，
#     不是 CRLF。也就是说触发条件是"内容确有变化"，不是"任意一次编辑器启动/关闭"；一次不改
#     内容的编译检查或测试跑不出这个现象。
#   - `Packages/manifest.json`、`Packages/packages-lock.json`：在以上编译检查×2 / EditMode
#     测试 / 独立版构建这四次实测里全程字节与哈希未变（一直是 CRLF，即检出态本身，不是 Unity
#     写的）。`build.ps1 -Release` 里另有实测证据（见其"写回遗漏根治"判断记录）：UPM 在
#     `packages-lock.json` 里的依赖版本号确有不一致时会自己回写该字段——即同样"内容确有变化
#     才回写"的模式；未见任何证据支持这两个文件被 Unity/UPM 写成 CRLF，維持 `eol=crlf` 例外
#     缺乏实测依据。
#
# 结论：三类路径统一改为 `eol=lf`，与仓库通用的 `*.asset`/`*.json text eol=lf` 规则一致（这三
# 条例外因此实质上等价于删掉例外、回退到通用规则；保留成独立三条，是为了显式标注"这是 Unity
# 批处理会自行改写内容的路径，行尾已核实钉死为 LF"，便于后续再有人怀疑行尾问题时直接查到这段
# 判断记录，不必重新摸索）。`git status` 在 Unity 批处理跑完后保持干净，原理不变——仍是让检出
# 行尾与 Unity 实际写出的行尾一致，不依赖任何人本机的 `core.autocrlf` 配置；只是"实际写出的
# 行尾"这一实测结论从 CRLF 改成了 LF。这三条必须写在上面通用规则之后（虽然效果与通用规则相同，
# 但同一属性以最后一条匹配规则生效，写在后面才不受未来通用规则调整的影响）。
adapters/unity/ProjectSettings/*.asset eol=lf
adapters/unity/Packages/manifest.json eol=lf
adapters/unity/Packages/packages-lock.json eol=lf
