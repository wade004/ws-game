# 第十四方深度审核核实跟进（codex 第十二轮，基线 `ac3b622`）

基线：`ac3b622`（main，v1.10.0）。审计报告本体见本目录 `AUDIT_REPORT.md`/`core/`/`docs-project/`/
`presentation/`（codex 原文，`git add` 归档，未改写）。

修复由三个并行 Sonnet 5 子 agent 按领域分工执行（核心侧 WA、引擎/表现侧 WB、工具链与归档 WC），
判断记录留存于 `C:\Users\1\AppData\Local\Temp\claude\D--workespace-ws-game\
f1d8941d-33da-4643-9759-6533f7c7ff79\scratchpad\audit12\{WA,WB,WC}.md`（未随仓库提交，属会话临时
材料）。主会话（本 agent）独立核实全部条目（重读改动后源码、重跑测试、交叉核对报告文字与实际文件
清单）、统一分领域提交、执行全量门禁、撰写本文档。提交：

| 提交 | hash | 范围 |
|---|---|---|
| A（核心侧） | `61bd11c` | `core/**`（CORE-110-01～03 + 方向移动导航阻挡 + WorldMap 四字段 schema + 过时注释）、`architecture/03`/`05`/`10` |
| B（引擎/表现侧） | `b8aa93f` | `adapters/unity/**`（NAV-110-01/02）、`adapters/conformance/**`、`presentation/vfx_sfx/**`（PRES-110-01）、`architecture/09` |
| C（归档证据补全） | `f23d6f0` | `.gitignore`、`toolchain/**`、既往 `audit-3224ca1`/`audit-8160178`/`audit-c86bfa9`/`audit-e070e3f` 四个归档目录补漏 |
| D（归档） | 本提交（含本文档） | `architecture/落地计划/audit-ac3b622-20260909/` 整目录（codex 报告原文与证据） |

A/B/C 三次提交各自的 pre-commit 快速门禁（`-SkipUnity -Quick`，20 步 PASS/SKIP，0 FAIL）均一次
通过，`dotnet test`（六项目）在 A/B/C 三次提交时均为 Foundation 685/685、Numbers 107/107、
Carriers 354/354、Rules 404/404、PresentationCommon 498/498、Gameplay 499/499，全绿（提交 A 时
`adapters/**`/`presentation/**` 虽尚未 `git add`，但工作树已是 WA+WB+WC 全部改动叠加后的状态，
门禁针对真实磁盘文件构建/测试，故三次提交的六项目计数从一开始就已保持一致，非"逐步累加"）。三次
提交落地后 `git status --short` 只剩 D 待提交的归档目录本身。

## 核实方法说明

本文档"核实"列的判断基于：(1) 独立重读改动后的源码（`git show <commit> -- <path>`，非只读
WA/WB/WC 报告文本），逐一确认修复手法与报告描述一致；(2) 独立核对接口/方法签名变更是否构成源码
级破坏性变更；(3) 独立核对判断记录的推理链条（如清理顺序、事件抑制作用域边界）是否站得住；
(4) 对报告文字与实际文件清单做交叉核对；(5) 第 4 步全量门禁前台实跑复核最终测试计数与既有回归
测试全绿。

## 核实表：CORE-110-01～03、NAV-110-01/02、PRES-110-01、方向移动契约、地图四字段、P3 归档

| 编号 | 判断 | 复现 | 修复位置 | 验收 | 判断记录 |
|---|---|---|---|---|---|
| CORE-110-01（失败读档回滚后派生状态不恢复） | **成立（P2）** | `CORE_110_01_RaceSubCase_RollbackRestoresFieldAndDerivedStatAndAura`、`CORE_110_01_EquipmentSubCase_RollbackRestoresPowerMaxAndHealth`（真实 `GameplayAssembly`/`RulesAssembly`/`SaveSystem`/`EventBus`，A/B 真实存档 + 强制回滚） | `core/foundation/save_system/core/SaveSystem.cs`（`RollbackLoadedSections` 对每个成功回滚的段重放一次 `IDerivedStateRebuilder.OnSectionLoaded`；失败分支的回滚集合额外总是尝试纳入 `player.vitals`） | 独立重读 `SaveSystem.cs` диff 确认回调点位置（回滚循环内、`Load(快照)` 成功之后）；同复现测试通过；另跑既有 `CORE_170_03_*`/`CORE_180_*` 事件零泄漏回归确认无破坏 | 回滚只恢复"字段"不等于恢复"派生状态"（评级/光环/资源池上限-当前值是字段的函数，不是字段本身）——独立核对该推理成立，`player.vitals` 是资源池当前值的唯一权威段（不是任何重算能推出的派生结果），失败读档若在它被读到之前就已因其它段的 `OnSectionLoaded` 副作用改写过当前值，只有让它也参与回滚才能纠正 |
| CORE-110-02（同图跨职业读档只覆盖共同基础键，旧职业独有键与资源类型未清理） | **成立（P2）** | `CORE_110_02_SameMapClassSwitch_ClearsLegacyBaseKeyAndPowerType`（真实 A/B 职业 fixture，同图 `RestoreFromSlot`） | `core/numbers/stat_block/core/StatHost.cs`（新增 `ResetBase`）+ `core/rules/assembly/RulesAssembly.cs`（`ReloadArchetypeAndRace` 补齐旧职业独有基础键清理 + 资源类型集合对账） | 独立重读 `RulesAssembly.cs` diff 确认清理顺序（旧种族修正→旧职业独有基础键→新职业完整基础键→新种族修正/光环→资源类型对账）与判断记录一致；同复现测试通过 | 独立核对顺序合理性：资源类型对账必须放最后，因为 `PowerHost.RegisterUnit` 按注册时刻的属性聚合值初始化上限，若先重新注册会用到"还没换完"的中间态属性值；`StatHost.ResetBase` 用"移除显式值退回 default_base"而非"SetBase 成猜测值"，独立核对与 `RemoveModifiersBySource`"按来源整体撤销"既有惯例一致 |
| CORE-110-03（同一 tick 多条 move 意图逐条重复推进位移） | **成立（P2）** | `CORE_110_03_TargetMove_MultipleSameTickIntents_AdvancesOnlyOnce_RegardlessOfCount`、`CORE_110_03_DirectionMove_MultipleSameTickIntents_AdvancesOnlyOnce`、`CORE_110_03_TargetMove_StopThenTwoMovesInSameTick_AdvancesOnlyOnce_LikeOneMove`、`CORE_110_03_TargetMove_ReplacingPriorTickPath_FiresOnMoveStopped_Replaced_AtMostOnce`（真实 `WorldSim`/`MovementHost`/`MovementTickHandler`） | `core/carriers/unit/core/MovementTickHandler.cs`（`Execute` 第一遍 B 改为两趟遍历：先扫描每单位本 tick 最后一条存活 move 意图下标，第二趟只在命中该下标时调用一次 `ApplyIntent`） | 独立重读 diff 确认未使用 `Dictionary` 迭代顺序（保证单位间处理顺序不变，只是同单位更早的已确定被替换的意图不再各自触发）；同复现测试通过：1/2/3 条意图位移相同，Stop+2 条位移等于 1 条 | 语义拍板（CORE-110-03 语义）：每单位每 tick 只积分一次固定 dt，同 tick 多条 move 意图最后一条生效；`Replaced` 判定读的是"进入本 tick 处理之前"的 `CurrentPath`，独立核对天然至多触发一次，不需要额外簿记；已写入 `architecture/05` 变更记录（2026-09-09 一行） |
| NAV-110-01（端点所在采样格中心被阻挡） | **成立（P2）** | `NAV110_01_EndpointCellCenterBlocked_ExactEndpointAndDirectRaycastClear_ReturnsNonNullPath`（改写自审计 `AuditAc3b622NavigationProbes.CandidateA`） | `UnityNavigation2D.cs`：新增 `ResolveEntryCell`，在 `AStar` 之前对起止格分别解析——格子本身可行走原样返回；受阻时在 8 邻居里找一个可行走且与精确端点直连不受阻（同 `Raycast` 判定）的最近格子 | 独立重读 diff 确认新方法与 `BuildWorldPath` 既有邻格接合兜底不重复（原兜底在 `AStar` 已拒绝时完全没机会执行）；同复现测试通过：首尾精确等于请求端点，逐段 `Raycast` 均为 `null` | 根因确认：`AStar` 入口检查 `grid.Walkable[start]/[goal]`，格中心恰好落在阻挡矩形内部时直接返回 `null`，与端点/直线本身是否可行走无关 |
| NAV-110-02（薄墙窄于采样间距被 A* 视而不见） | **成立（P2）** | `NAV110_02_ThinWallNarrowerThanGrid_DetourExists_ReturnsNonNullPath`（改写自 `CandidateB`） | `UnityNavigation2D.cs`：新增 `BuildGridWithCellSize`/`IsCellInteriorBlockedByRects`/`FindPathWithFineGrid`——主网格路径未通过收尾防线（逐段 `SegmentBlocked` 复核）时，用半格尺寸（`ThinObstacleFallbackCellSize` = `DefaultCellSize`/2）+ 精确占用判定（格子内部与矩形内部是否有非零面积相交，而非"格子中心是否落在矩形内部"）的独立细网格重新完整寻路一次，仍未通过才是真正无路可走 | 独立重读 diff 确认只在"主网格路径未通过收尾防线"这一具体失败分支触发兜底（不改主网格本身，不影响既有场景/性能）；同复现测试通过：绕路 3 段各段 `Raycast` 均为 `null` | 根因确认：默认网格只在格子中心采样阻挡，窄于格子尺寸的薄阻挡矩形可能使两侧相邻格中心都落在矩形外，两格都判定可行走，`AStar` 建一条实际穿墙的邻接边 |
| PRES-110-01（读档后武器风格缓存未刷新） | **成立（P2）** | `PRES110_01_SaveLoadedEvent_ClearsCache_NextQueryReResolvesRealState`、`PRES110_01_RepeatedSaveLoadedEvents_NeverReuseStaleValueAcrossReloads` | `presentation/vfx_sfx/core/EquipmentWeaponStyleSource.cs`：构造函数新增订阅 `SaveEventKeys.SaveLoaded`，触发时整表清空 `_cache` | 独立重读 diff 确认订阅写法与 `ViewBinder.OnSaveLoaded` 同一惯例；同复现测试通过：连续两次读档（A→B→A）各自按当时真实状态重新解析 | 根因确认：`SaveSystem.Load` 把逐段 `Load` + 失败回滚整段包在 `IEventBus.SuppressDispatch` 抑制作用域内，`EquipmentPersistable.Load` 内部重放的 `ItemEquipped`/`ItemUnequipped` 被丢弃，本类型仅有的两个失效订阅永远收不到；`save.loaded` 在该抑制作用域外正常派发，不受影响。核对同类缓存来源 `presentation/render/core/EquipmentVisualSource.cs`：理论上受同一抑制作用域问题影响，**但不是同一种缺陷形状**——后者是无状态查询缓存（清空即自愈），前者是已经"应用"到具体 View 实例的状态日志，真正修复需要让已绑定 View 重新走一次 `ReplayEquippedForUnit + view.OnEvent`（视图重放/Mesh 冷加载范畴）；本轮任务书边界条款明确"该结论只覆盖风格缓存……不外推为 Mesh 冷加载、HUD 刷新或 Unity 屏幕外观缺陷"，独立核对该边界拍板合理（两种缓存的失效自愈机制不同，不能用同一次修复覆盖），不在本轮范围内一并根治，留给后续以该模块自己的复现证据立项 |
| 方向移动导航阻挡契约（NAV-DOC-02） | **已拍板并落地** | `MovementTickHandlerTests.cs`：`DirectionMove_BlockedByNavigation_TruncatesToJustBeforeIncidencePoint`、`DirectionMove_TargetNotWalkable_DoesNotMove`、`DirectionMove_Unblocked_StillAdvancesNormally` | `core/carriers/unit/core/MovementTickHandler.cs`（`ApplyDirectionalMove` 新增 `Raycast`/`IsWalkable` 检查）；`architecture/05` §6.1 勘误 | 独立重读 diff 确认受阻按 `MovementOptions.ArrivalEpsilon` 截断到入射点前（不直接落在入射点——`IsWalkable` 的点包含判定通常已把入射点本身算作阻挡内）；未装配 `INavigation2D` 时两项检查全部跳过，行为与改动前完全一致；同复现测试通过 | 根因确认：`05` 第 6.1 节"移动前调用 isWalkable"是对全部移动的一般表述，但此前方向类移动只检查同帧其它单位阻挡，从未调用 `IsWalkable`/`Raycast`，与目标类移动（经 `FindPath`）不对称；拍板方向移动与目标移动共用同一套统一可通行规则（02 第 1.8 节） |
| WorldMap 四字段 schema | **已补齐** | `python toolchain/validate_data.py`（合并根 + `data/_framework`） | `core/foundation/scene_router/core/WorldMapSchema.cs`（`regions: IdList`、`teleport_points: Array`、`music_ref: String`、`allowed_difficulties: IdList`，均可选，仅类型校验、无引用完整性） | 独立重读 diff 确认按 `05` 第 4.1 节原文类型/必填性登记；`validate_data.py`（合并根）0 错误 0 警告、`--data-root data/_framework` 0 错误 1 既有警告，与审计基线一致 | `regions`/`allowed_difficulties` 均不声明 `ReferenceTable`/`ReferenceDomain`——当前均无独立登记表可供引用完整性校验（08 难度档位只在运行期以 `Id` 标识），登记完整性校验属于后续独立收口项，本次只补齐类型校验，与既有 `spawn_points`/`scene_ref`/`nav_ref` 同样"只做类型校验、不做引用完整性"的口径一致 |
| P3 归档（e070e3f 等四个归档目录共 30 处证据链接因 `.gitignore` 误伤缺失） | **成立（证据完整性 P3）** | `git check-ignore -v` 命中未加路径前缀的 `**/[Ll]ogs/` 规则；取消 `test_markdown_relative_links.py` 对 `audit-*` 目录的整段排除后测试报出 30 处真实失效链接 | `.gitignore`（`**/[Ll]ogs/` 收窄为 `adapters/unity/**/[Ll]ogs/`）；找回 e070e3f（7 个文件）、3224ca1（11 个文件+1 项标注未归档）、8160178（2 个文件）、c86bfa9（15 个文件）共 30+ 处证据；2 处 presentation 动画回归证据原件已随会话清理丢失，正文标注"原件未归档"并登记 `toolchain/tests/.linkcheck-ignore` 白名单 | 独立核对找回文件均 <300KB、逐个按仓库禁用词扫描口径核对不含具体游戏代号；`git check-ignore -v` 对本轮新增归档证据文件逐个核对均不再被忽略；`python -m pytest toolchain/tests -q` 全量通过（见下"验收"节实跑数值） | 根因确认：1-7 号（e070e3f）文件其实一直在本机磁盘、非 codex 产出目录丢失，是被 `**/[Ll]ogs/` 规则挡在 git 之外；8-9 号才是真的原件已随 codex 会话清理丢失，`.codex` 全量树搜索不到，按任务口径标注 + 登记白名单；`source_snapshot.zip`（约 33MB 全量源码快照，3224ca1）体积超出文档归档合理范围，正文标注"原件未归档"+SHA256，登记白名单，不入库 |

## 1.9 修复复核

`AUDIT_REPORT.md` 第 3 节"1.9 修复复核"已记录 codex 侧复核结论："1.9 的 CORE-180-01/02/03 在 1.10
当前真实 fixture 中复核通过：成功 Load 的 rating=10、power max/health=200；正向回滚顺序恢复等级与
装备；A/B 独立 race 存档同图恢复后为 stat=91、AuraA=false、AuraB=true"。本次三份修复（A/B/C）独立
复核未破坏该结论：

- **核心侧**（CORE-180-01～03，1.9.0，`IDerivedStateRebuilder` 契约）：本轮 CORE-110-01 复用同一
  契约（回滚路径新增调用点），未新增平行机制、未改变 `OnSectionLoaded` 签名；`GameplayAssembly.
  DerivedStateRebuilder` 内部实现改为"滚动更新"`_previousArchetypeId`/`_previousRaceId`，同一
  实现复用于正向与回滚两条路径，独立核对未破坏 1.9.0 成功路径的既有调用时机（`player.equipment`
  段完成时触发重算、早于 `player.vitals` 段读取）。CORE-110-02 的 `RulesAssembly.
  ReloadArchetypeAndRace` 是 1.9.0 CORE-180-03 新增方法的增强（补齐此前忽略的 `previousClassId`
  参数），独立核对种族部分逻辑（`RemoveModifiersBySource`/`Release`/`ReapplyRacePassiveAuras`）
  未改动，只新增职业部分的清理与资源类型对账。独立重跑 `CORE_180_*`/`CORE_170_03_*` 事件零泄漏
  回归测试随 Tests.Rules 404/404、Tests.Gameplay 499/499、Tests.Foundation 685/685 一并全绿（数值
  见下"验收"节）。
- **表现侧**（PRES-180，1.9.0，`ViewBinder` 订阅 `save.loaded`）：本轮 PRES-110-01 在不同类型
  （`EquipmentWeaponStyleSource`）里对照同一惯例新增订阅，独立核对与 `ViewBinder.OnSaveLoaded`
  无交叉依赖、不共享内部状态，未改动 `ISimSnapshot`/`ViewBinder` 任何一行。
- **归档/文档**：本轮 C 提交只新增/找回既往归档目录下的证据文件与 `.gitignore`/链接检查改动，
  未触碰 1.9.0 涉及的 `README.md`/`architecture/06`/`11`/ADR-0017 正文。

结论：1.9.0（CORE-180 系列 + PRES-180）三类修复未被本轮任何改动破坏。

## 验收

前台实跑 `powershell -ExecutionPolicy Bypass -File check.ps1 -LogFile <scratchpad>\check_full_z15.log`
（不加 `-SkipUnity`/`-Quick`），执行前 `tasklist | findstr /i "Unity.exe"` 已确认 Unity 编辑器本体
未占用。一次实跑即全部通过，无需二次修复重跑：

| 步骤 | 结果 | 用时(s) | 详情 |
|---|---|---|---|
| 门禁自检：`Test-NativeExitCode` | PASS | 0.3 | — |
| `dotnet build Core.sln -c Release` | PASS | 5.1 | 0 警告 0 错误 |
| `dotnet test`（六项目） | PASS | 4.9 | Foundation 685/685、Numbers 107/107、Carriers 354/354、Rules 404/404、PresentationCommon 498/498、Gameplay 499/499（合计 2547 条全绿），与 A/B/C 三次提交时的计数一致 |
| `validate_data.py`（合并根） | PASS | 4.7 | 60 tables/285 records/0 errors/0 warnings/1 override |
| `validate_data.py --data-root data/_framework` | PASS | 1.5 | 5 tables/124 records/0 errors/1 warning（缺 l10n 表，既有已知情况） |
| `gen_event_constants.py --check` | PASS | 0.1 | 90 个常量一致 |
| `gen_placeholder_assets.py --check` | PASS | 0.1 | — |
| `import_assets.py check --dataset _sample` | PASS | 0.1 | 0 问题 |
| `pytest toolchain/tests -q` | PASS | 11.4 | 82 passed（与 C 提交独立核实数值一致） |
| 禁用词扫描：全仓库不出现具体游戏代号 | PASS | 23.9 | 0 命中 |
| 禁用词扫描：architecture 正文技术名（immunity 例外） | PASS | 0.1 | 0 真实命中 |
| 版本一致性 | PASS | 0.1 | VERSION=1.10.0，两个 `package.json`、`packages-lock.json`、`CHANGELOG.md` 一致 |
| `build.ps1 -SkipTests`（同步 DLL） | PASS | 5.3 | 六个 DLL 哈希核对通过 |
| 包清单一致性 | PASS | 8.6 | 三个 npm 包 version=1.10.0 一致，`npm pack --dry-run` 清单不含排除项 |
| Unity 编译检查 | PASS | 23.0 | 退出码 0 |
| Unity EditMode 测试 | PASS | 8.9 | total=62 passed=62 failed=0 |
| Unity PlayMode 测试 | PASS | 75.9 | total=265 passed=265 failed=0 |
| 独立版构建 + `-gf-smoke` 冒烟（连续模式） | PASS | 29.8 | — |
| 独立版 `-gf-smoke-discrete` 冒烟（离散模式） | PASS | 3.3 | — |
| IL2CPP 独立版构建/冒烟 ×3 | SKIP | 0 | 未传 `-Il2cpp`（任务未要求） |
| 消费方演练（`toolchain/consumer_smoke.ps1`） | PASS | 139.1 | — |

**汇总：23 步（20 PASS + 3 SKIP），0 FAIL，总用时 346.2s。**

## 自检复核

- 全仓库禁用具体游戏代号扫描（`check.ps1` 内置扫描步骤，同一禁用词；本次全量门禁一并覆盖）：
  **0 命中**（含 A/B/C 三次提交与本文档全部新增内容——首次撰写时曾在核实表判断记录一处误写该
  禁用词本身作为示例命令的一部分，被 B/C 提交之间的一次快速门禁拦下，已改写为不含该词的表述后
  复核通过）。
- `architecture/00～14` 与 `architecture/adr/` 正文技术名 grep（`unity|c#|csharp|dotnet|\.net|
  nunit|python|newtonsoft|il2cpp|powershell|github|verdaccio|npm`）：仅 `immunity` 既有豁免误报，
  **0 真实命中**（含本轮 03/05/09/10 号文档新增段落、变更记录新增行；独立重跑覆盖 `architecture/`
  下全部 15 个编号文档 + `adr/` 目录，非仅门禁脚本自身的排除范围）。
- `git ls-files '*.ps1'` 含非 ASCII 字符的 11 个文件逐一核对：均以 UTF-8 BOM（`EF BB BF`）开头
  （本轮未新增/改动任何 `.ps1` 文件）。
- A/B/C 三次提交、CHANGELOG/落地计划提交（`eafb1d8`）后 `git status --short` 只剩本目录
  （`audit-ac3b622-20260909/`）待提交，落地 D 后应为空；全量门禁实跑（不含 `-SkipUnity`/`-Quick`）
  未产生任何新的未提交改动（`build.ps1 -SkipTests` 同步 DLL 哈希均"内容未变化"，非首次同步）。
