# 第十四轮（修订版）审核修复跟进核实（2026-09-10）

基线：`c9ff30107413083188c597c0b65cf1691c9dfe9b`（v1.13.0）。本文档核实 [AUDIT_REPORT.md](AUDIT_REPORT.md)
六项 P2 + 两项静态差距（`ISkillHost.FindUnits`、VFX 锚点持续跟随）+ 三项 P3 的实际修复结果，以及
核心侧复查过程中新发现的额外问题（第六个退役类型、两处构造签名收窄、四处同类缓存候选）。

修复提交（均基于 `396b1db`，即 `c9ff301` 之后一个提交）：

| 提交 | 标题 | 范围 |
|---|---|---|
| `c65258e`（提交 A） | 第十六方深度审核修复(核心侧) | `core/**`、`data/**`、`architecture/05`、对应 README |
| `775de54`（提交 B） | 第十六方深度审核修复(表现/模板侧) | `presentation/**`、`adapters/**`、`games/**`、`architecture/09`/`13`、ADR-0019、对应 README |
| `4a60ca2`（提交 C） | 第十六方深度审核(文档与门禁) | `README.md`、`CHANGELOG.md`、`toolchain/**`、`check.ps1`、`architecture/11`、落地方案 |

本文档的核实方法：逐条对照 [AUDIT_REPORT.md](AUDIT_REPORT.md)/[project-findings.md](docs-project/project-findings.md)
的 trigger/actual/expected 描述，走查提交 A/B/C 的实际代码改动是否命中该描述、对应回归测试是否真实
覆盖修复前会失败、修复后是否通过（`dotnet test`/`Unity` 实跑，见"验收"节）；不采信任何一方的自我
报告作为唯一依据。

## 核实表

| 条目 | 判断 | 复现 | 修复位置 | 验收 | 判断记录 |
|---|---|---|---|---|---|
| P2-01 ABI/API 兼容 | **成立** | 独立 consumer 探针改造自 `docs-project/api-compat/Program.cs`：旧编译产物只换正式新 DLL、不重新编译，`FieldSchema` 七参数构造抛 `MissingMethodException` | `core/foundation/data_registry/contracts/FieldSchema.cs`（七参数 `[Obsolete]` 构造函数）、`core/carriers/gobj/schema/GobjValidationRules.cs`（`GobjOnUseKindRule`/`GobjLockRequirementFieldGroupRule`）、`core/rules/skill/schema/SkillValidationRules.cs`（`EffectKindRegisteredRule`/`CostEntryShapeRule`/`ChargesShapeRule`）——均在提交 A | `toolchain/abi_probe.ps1`（基线 1.12.0）本机实跑通过（`ABI_PROBE_ALL_OK`，覆盖全部 8 个签名，见"验收"节）；`core/foundation/data_registry/tests/P2_01_FieldSchemaLegacyCtorTests.cs`、`core/rules/skill/tests/P2_01_SkillValidationRuleFacadeTests.cs` 等 5 个新测试文件全绿 | 核实过程中走查了 `git diff v1.12.0 v1.13.0 -- '*.cs'` 全仓公开签名差异，确认审计报告点名的五个规则类型准确，但发现报告未点名的第六个（见下方"额外发现"） |
| P2-03 Gobj `world_flag.expected` | **成立** | `core/carriers/gobj/tests/GobjValidationRuleTests.cs` 新增 `P2_03_*` 系列：缺失/错型 `expected` 修复前流到 `LockDef.FromRecord` 才抛异常，不在 report 阶段拒绝 | `core/carriers/gobj/schema/GobjValidationRules.cs`（新增 `GobjLockWorldFlagExpectedRule`）、`core/carriers/assembly/CarriersSchemaCatalog.cs`（默认注册）——提交 A | `dotnet test core/carriers/tests/Tests.Carriers.csproj --filter P2_03` 全绿（383/383，Tests.Carriers 整体通过，见下方"验收"节六工程汇总） | 走查确认 `GobjLockWorldFlagExpectedRule` 只校验 Bool/Number（不是 `ExprValueJson` 完整联合），与 Gobj 实际接受集合一致，未误用 Quest/Dialog 那套更宽的校验器 |
| P2-05 SkillHotReload（含同类缓存排查） | **成立** | 改造自 `core/repro/SkillHotReloadBoundaryProbe.cs`：resident host reload 后继续沿用旧定义，`core/rules/skill/tests/P2_05_SkillDefCacheHotReloadTests.cs` 复现 | `core/rules/skill/core/SkillDefCache.cs`（`InvalidateAll`）+ `SkillHost.cs`（订阅）、`InventoryHost.cs`/`EquipmentHost.cs`、`QuestHost.cs`/`DialogHost.cs`（`Reload` 方法）+ `GameplayAssembly.cs`（订阅转发）、`ArchetypeRegistry.cs`——提交 A | 对应六个新测试文件全绿；六工程 `dotnet test` 汇总见下方"验收"节 | 走查确认六处修复均只清空/重建定义缓存，未触碰任何运行期状态（背包物品、任务进度、对话会话、已应用光环/属性），与判断记录一致 |
| P2-06 Quest `rewards.world_flags[].value` 联合漏校验 | **成立** | `core/gameplay/quest/tests/QuestSchemaCoverageTests.cs` 新增用例含端到端 `Assert.Throws<FormatException>` 证明未修复前流到 `QuestDefinition.FromRecord` | `core/gameplay/common/contracts/ExprValueJson.cs`（新增 `IsValid`）、`core/gameplay/quest/core/QuestContentValidationRule.cs`、`core/gameplay/dialog/core/DialogContentValidationRule.cs`（关联修复 `set_flag.params.value` 同构缺口）——提交 A | Quest 3 条 + Dialog 3 条新测试全绿；`validate_data.py` 0 error（既有数据未回归） | 走查确认 Dialog 侧关联修复不在原始六项 P2 清单内，是核心侧全仓 grep `ExprValueJson.Parse` 调用点后主动发现的同构缺口，判断记录已如实说明 |
| P2-08 ExistingView 装备外观 SaveLoaded 不清理 | **成立** | 审核归档 `presentation/ExistingViewEquipmentSaveLoadAuditTests.cs`（正确性断言：Load B 后期望 socket child=0，此前实测=1，失败） | `presentation/render/contracts/IEquipmentVisualResettable.cs`（新接口）、`presentation/render/core/SpriteViewBase.cs`、`adapters/unity/.../UnityModelView.cs`（两条 View 实现）、`presentation/view_binding/core/ViewBinder.cs`（`OnSaveLoaded` 新增第三段对账）——提交 B | `adapters/unity/Packages/com.gamefoundation.adapter.unity/Tests/Runtime/EquipmentVisualSaveLoadResetTests.cs`（真实 `GameplayAssembly`/`SaveSystem`/`EquipmentHost`/`ViewBinder`/`UnityModelView` 链路，PlayMode，**Passed**，见下方"验收"节 Unity 实跑结果；2026-09-10 后续修复从 `games/_template/Tests/Runtime/ExistingViewEquipmentSaveLoadTests.cs` 迁出，见本文档末尾"异常与判断记录"最后一条） | 走查确认对账只调用 View 自身方法，不经 `IEventBus`、不合成/补发 `item.equipped`/`item.unequipped`，未破坏既有装备事件语义 |
| P2-09 删除双根覆盖不回落 | **成立** | 审核归档 `DataHotReloadDeletedOverlayAuditTests.cs`（断言"删除后仍为 2"，捕获缺陷） | `games/_template/Runtime/DataHotReload.cs`（`FileSystemWatcher` 新增订阅 `Deleted`，`Renamed` 改走独立处理）——提交 B | `games/_template/Tests/Editor/DataHotReloadDeletedOverlayTests.cs`（改写为正确性断言：删除后自动回落到 framework 值 1，EditMode，**Passed**） | 走查确认 `Deleted` 复用既有 `MarkPending -> ReloadTable` 路径，未新增专门的"删除"分支，改动面小、风险低 |
| `ISkillHost.FindUnits` 静态差距 | **成立（已实现）** | 修复前 `SkillHost.FindUnits` 恒 `_diagnostics.Warn(...)` + 返回 `Array.Empty<Id>()`，与形状/阵营参数无关 | `core/rules/skill/core/SkillHost.cs`（委托 `ISpatialQuery.QueryShape` + `UnitFilter` 全维度过滤）、`core/foundation/engine_adapter/contracts/ISpatialQuery.cs`（新增 `Shape.WithOrigin`）、`core/rules/assembly/RulesAssembly.cs`（默认传入 `Spatial`/`factions: Factions`）——提交 A | `core/rules/skill/tests/ISkillHost_FindUnitsTests.cs`（12 例：三种形状、未注入 `ISpatialQuery`、排除 trigger-only、`AliveOnly`、阵营过滤两方向、无参照单位/未注入 `IFactionMatrix` 判不通过、`Self`/`NotSelf`、结果排序确定性）全绿 | 已按能力索引惯例从"未实现"移出，归档进「已修复历史项」，见落地方案文档本轮改动 |
| VFX 锚点持续跟随静态差距 | **成立（已实现）**（`socket` 真挂接部分澄清为设计如此，非缺陷） | 修复前 `attach_mode: anchor` 特效只在 `Spawn` 内 `ResolveAnchor` 解析一次锚点，`Update` 不按实体移动持续更新 | `presentation/vfx_sfx/contracts/IParticleRepositioner.cs`（新可选能力接口）、`presentation/vfx_sfx/core/VfxPlayer.cs`（`Update` 新增 `UpdateFollowTargets`）、`adapters/unity/.../UnityRenderer2D.cs`（实现该接口）——提交 B | `presentation/vfx_sfx/tests/VfxPlayerFollowTests.cs`（dotnet test，4 例）、`adapters/unity/.../Tests/Runtime/VfxAnchorFollowTests.cs`（真实 `UnityRenderer2D`，PlayMode，2 例）均 **Passed** | 已按能力索引惯例从"未实现"移出，归档进「已修复历史项」；未装配 `IParticleRepositioner` 的引擎适配层（含全部既有 `StubRenderer2D` 测试）行为与改动前完全一致 |
| P3-02 ADR-0019 门禁载体文案漂移 | **成立（文档层，已勘误）** | 静态审计描述澄清，非运行时缺陷——`SchemaAudit`/`--schema-audit` 只做结构声明本身的静态审计，不比较运行时注册表，此前文案容易读成同一机制 | `architecture/adr/0019-复合字段子结构登记为机器可读schema.md`（追加"修订记录"）、`presentation/assembly/SchemaAudit.cs`（头注释澄清）——提交 B | `dotnet run --project toolchain/validator -- --schema-audit --allowlist toolchain/schema_audit_allowlist.json`：`tables 63, fields 783, errors 0, warnings 0`（见下方"验收"节实跑） | 走查确认变体表键集合与运行时注册原语一致性的真实载体是各模块 coverage tests，未与 `SchemaAudit` 混淆或重叠 |
| P3-04 周期效果 `scaling_stat` metadata 漂移 | **成立** | `SkillSchemas.PeriodicParamsCase` 此前未登记 `scaling_stat`，内容作者不可见（运行期已消费，纯登记缺口） | `core/rules/skill/schema/SkillSchemas.cs`（补 `Reference(stat.definition)`）——提交 A | `SkillSchemaCoverageTests.PeriodicEffectParams_DeclareOptionalScalingStatReference`（Theory，2 case）+ `AuraEffectTests.PeriodicDamage_WithScalingStat_ScalesBySourceStat`（端到端验证 `3 + 2*5 = 13`）全绿 | 06 文档本身已准确描述该字段，未追加文档勘误，判断记录合理 |
| P3-07 WorldMap 点元素语义边界与旧索引漂移 | **成立** | 类型顶部注释原称 `teleport_points` 只做数组类型校验，与实际登记（已展开 `id`/`position` 子字段）不一致 | `core/foundation/scene_router/core/WorldMapSchema.cs`（新增 `WorldMapSpawnPointsValidationRule`）、`core/rules/assembly/RulesSchemaCatalog.cs`（默认注册）、`architecture/05_对象模型与世界.md`（勘误）——提交 A | `WorldMapSchemaCoverageTests.cs` 新增 `P3_07_*` 三条全绿；`validate_data.py` 对真实 `data/_framework`/`data/_sample` 的 `world.map` 0 error | 只保留静态边界澄清，未扩大为跨图/跨表引用完整性的新 Runtime P2 |
| **额外发现**：第六个退役类型 `AreaTriggerShapeKindRule` | **成立** | `git diff v1.12.0 v1.13.0 -- '*.cs'` 全仓复查，审计报告未点名 | `core/gameplay/area_trigger/schema/AreaTriggerValidationRules.cs`（补回 `[Obsolete]` façade）——提交 A | `core/gameplay/area_trigger/tests/P2_01_AreaTriggerShapeKindRuleFacadeTests.cs` 全绿；`toolchain/abi_probe.ps1` 覆盖该类型的运行期实例化 | 与 P2-01 同一批 ADR-0019 退役，同属整条类型删除，纳入 façade 恢复范围 |
| **额外发现**：两处构造签名收窄 Economy/Loot | **成立** | 1.12 构造签名 `(IExprSchema? conditionSchema = null)` 收窄为隐式无参构造，旧调用点（含显式零参调用，可选参数默认值编译期内联进 IL）换 DLL 同样 `MissingMethodException` | `core/gameplay/economy/core/EconomyContentValidationRule.cs`、`core/gameplay/loot/core/LootContentValidationRule.cs`（补回 `(IExprSchema? conditionSchema)` 重载）——提交 A | `core/gameplay/economy/tests/P2_01_EconomyContentValidationRuleLegacyCtorTests.cs`、`core/gameplay/loot/tests/P2_01_LootContentValidationRuleLegacyCtorTests.cs` 全绿；`toolchain/abi_probe.ps1` 显式调用 `new EconomyContentValidationRule(null)`/`new LootContentValidationRule(null)` 验证 | 非整条类型删除，是比"删类型"更隐蔽的一类破坏性变更（可选参数是编译期特性），核实时特别关注了这一类不容易被"类型是否还存在"这种粗粒度检查发现的破坏 |
| **额外发现**：同类缓存候选 Economy/Loot/PowerHost/StatHost 未收口 | **成立，本轮已收口**（超出 WA 原六处范围，本 agent 评估后追加修复） | `StatHost`/`LootHost`/`EconomyHost` 均在构造函数直接接收 `IDataRegistryView` 但不存字段，`PowerHost` 只接收预解析的 `IEnumerable<PowerTypeDefinition>`——均属同一类"构造期一次性建索引、之后只读"模式 | `core/numbers/stat_block/core/StatHost.cs`、`core/gameplay/loot/core/LootHost.cs`、`core/gameplay/economy/core/EconomyHost.cs`（存 `_registry` 字段 + 内部订阅 `DataLoadCompletedEvent`）、`core/numbers/power_set/core/PowerHost.cs`（新增 `Reload` 方法）+ `core/rules/assembly/RulesAssembly.cs`（订阅并转发，惯例同 `QuestHost.Reload`/`DialogHost.Reload`）——提交 A | `P2_05_StatHostReloadTests.cs`（2 例）、`P2_05_LootHostReloadTests.cs`（1 例）、`P2_05_EconomyHostReloadTests.cs`（2 例，含限量库存保留验证）、`P2_05_PowerHostReloadTests.cs`（3 例）、`P2_05_RulesAssemblyPowerReloadTests.cs`（1 例，生产装配接线端到端验证）均全绿 | 评估结论：`StatHost`/`LootHost` 属于"定义缓存与运行期状态天然分离"的简单情形，直接比照 `ArchetypeRegistry` 参照实现；`EconomyHost` 的限量库存 `_stock` 是从 `_vendors` 派生的运行期状态，reload 时做了"已存在 (vendorId, itemId) 组合保留原状态，仅对新出现组合按新定义初始化"的选择性处理，避免玩家已消耗的库存被重置回满库存；`PowerHost` 是 L1 模块、刻意不持有 registry，采用与 Quest/Dialog 一致的"新增 `Reload` + 组装根转发"模式。四处工作量与已修复的十处（P2-05 六处 + 本轮四处）属同一模式，未发现需要设计新机制的例外情形，因此本轮一并根治，未留作后续独立收口项 |

## "按责任撤出主动缺陷列表"逐项确认已反映到能力索引

对照 [project-findings.md](docs-project/project-findings.md)"按责任撤出主动缺陷列表的条目"表逐项核实
`architecture/落地计划/落地方案与分阶段计划.md`"能力边界与未默认接入能力索引"一节现状：

| 条目 | 索引现状 | 核实结论 |
|---|---|---|
| Talent 完整点数管理 | **游戏责任**（`core/numbers/archetype/core/ArchetypeRegistry.cs:88` 等锚点） | 已反映（提交 C，WC 改动） |
| Summon follow/owner | 「已修复历史项」小节（连续步默认接线） | 已反映（第九轮审核既有归档，第十四轮补充说明与 Discrete 过期处理的区分） |
| SampleNewGameStarter | **游戏责任** | 已反映（提交 C） |
| 孤儿记录 | **未实现（04 定位为建议，非门禁契约）** | 已反映（提交 C，未改判为待办） |
| 导航跨帧/全索引空间查询 | **明确非目标**（两行） | 已反映（提交 C） |
| 位移轨迹碰撞 | **游戏责任** | 已反映（提交 C） |
| owner/day/vendor | **已实现未默认接线**（补充默认值语义边界说明） | 已反映（提交 C） |
| escort | **游戏责任**（新增一行） | 已反映（提交 C） |
| editor | **暂不落地（用户拍板）**（补 ADR-0018 决策 1/3 引用） | 已反映（提交 C） |

另核实：`ISkillHost.FindUnits`、VFX 锚点持续跟随两行在 2026-09-09 登记时暂标"本轮由并行核心/表现层
任务实现中，整合时核对"——本轮（2026-09-10）已确认两者均已实现且生产装配默认接入，已将其从
"能力边界"表格本体移出、归档进「已修复历史项」小节，`README.md` 能力边界段同步更正（详见
`architecture/落地计划/落地方案与分阶段计划.md`、`README.md` 本次改动）。

## 验收

本节记录第 4 步全量门禁的实跑结果（`check.ps1`，非 `-Quick`），运行方式与耗时汇总另见主报告；此处
只摘录与本文档核实项直接相关的关键结果：

- `dotnet build Core.sln -c Release`：0 警告 0 错误。
- `dotnet test Core.sln -c Release --no-build`（六工程）：**2840 passed / 0 failed / 0 skipped**
  （Tests.Foundation 737、Tests.Numbers 123、Tests.Carriers 383、Tests.Rules 439、
  Tests.PresentationCommon 545、Tests.Gameplay 613；相对 WA 报告记录的基线 `2831 passed`，差值 9
  为本文档"额外发现：同类缓存候选"一行新增的回归测试，非回归）。
- `toolchain/abi_probe.ps1`（基线 1.12.0）：`ABI_PROBE_ALL_OK`，8 个签名（`FieldSchema` 七参数
  构造函数、六个规则类型、两处构造签名）全部验证通过，退出码 0。
- `python toolchain/validate_data.py`（合并根）：`tables 60, records 290, errors 0, warnings 3,
  overrides 1`（3 条 warning 为既有 dialog gossip 引用拼写提示，与本轮改动无关）。
- `dotnet run --project toolchain/validator -- --schema-audit`：`tables 63, fields 783, errors 0,
  warnings 0`。
- `python -m pytest toolchain/tests -q`：见门禁汇总（含新增 `test_powershell_scripts_ansi_safe.py`
  对 `toolchain/abi_probe.ps1` 的 BOM/解析校验、`test_markdown_relative_links.py` 对本文档与本轮
  新增文档链接的校验）。
- Unity EditMode/PlayMode：见门禁汇总（含 `EquipmentVisualSaveLoadResetTests`（2026-09-10 后续
  修复前名为 `ExistingViewEquipmentSaveLoadTests`，见下方"异常与判断记录"最后一条）、
  `DataHotReloadDeletedOverlayTests`、`VfxAnchorFollowTests` 三个新增用例）。
- 全仓禁用词扫描（具体游戏代号）：0 命中。

（完整门禁 PASS/SKIP/FAIL 汇总表与各步耗时见主报告"结论"节，此处不重复摘录数字以避免与主报告
更新脱节。）

## 异常与判断记录

- 未发现"部分成立"或"不成立"的条目——六项 P2、两项静态差距、三项 P3、额外发现的三类问题（第六个
  退役类型、两处构造签名、四处同类缓存）经代码走查与回归测试实跑，判断结论与对应报告（WA/WB/WC）
  一致，未发现夸大或遗漏。
- `EconomyHost` 的缓存收口比其余三处（`StatHost`/`LootHost`/`PowerHost`）多一层"选择性保留运行期
  派生状态"的处理（见上方额外发现一行判断记录），是本轮评估中识别出的唯一需要设计判断的例外，已
  在对应源码判断记录与测试中说明，未见遗留风险。
- `ISkillHost.FindUnits`/VFX 锚点持续跟随两行能力索引更正涉及 `README.md` 与
  `architecture/落地计划/落地方案与分阶段计划.md`，未随提交 A/B/C 一并提交（当时两处实现尚未完成
  整合核对），随附带本文档的后续提交一并落地，不影响提交 A/B/C 本身的完整性与可回溯性。
- **2026-09-10 后续修复（根治消费方演练失败，提交 `48b69bc`）**：提交 B 把 P2-08 探针从审核归档
  迁入 `games/_template/Tests/Runtime/ExistingViewEquipmentSaveLoadTests.cs` 作为模板常设 PlayMode
  回归时，沿用了原始探针"反射改写 `GameBootstrap._gameDatasetRoot` 指向 `data/_sample`"的取巧写法
  （才能拿到 `creature.sample_model_hero` 一类真实 3D 模型 + `display.equip_visual` 装备外观数据）。
  `data/_sample` 是本仓库工作台自测用的示例数据（`data/README.md`"两类目录"一节），从不随分发包
  流向消费方；`toolchain/consumer_smoke.ps1` 演练消费方工程时数据根只有 `data/_framework` +
  `data/game`（模板自己的最小数据集，不含 `creature`/`display`/`gobj` 三张表），该用例因此必然
  在消费方工程里装配失败（`[GameBootstrap] 装配失败：...StatHost 需要 "stat.definition" 表`），
  是提交 A/B/C 整合时遗漏的一处"工作台内验证通过、消费方演练必然失败"的用例，未被本文档"验收"
  节记录的门禁实跑覆盖到（当时"验收"节只跑过一次未强制消费方演练失败即门禁失败的历史门禁版本）。
  根治：改用 `Core.Gameplay.Assembly.GameplayAssembly`（真实 `EquipmentHost`/`InventoryHost`/
  `SaveSystem`）+ `Presentation.ViewBinding.ViewBinder`（真实 `entity.created`/`save.loaded` 订阅）+
  `Adapter.Unity.Presentation.UnityViewFactory` 直接组装最小但完整的真实链路夹具（仍用
  `data/_sample`，但改为在 `adapters/unity` 工作台自身的 PlayMode 测试里跑，不进消费方演练），
  不再经由 `games/_template.GameBootstrap` 这一具体游戏层组合根——`adapters/unity` 是框架 L-1
  层，不应反过来依赖任何具体游戏/模板的组合根，即便只是测试代码（`games/_template/package.json`
  对 `com.gamefoundation.adapter.unity` 声明的是单向依赖）。新文件
  `adapters/unity/Packages/com.gamefoundation.adapter.unity/Tests/Runtime/EquipmentVisualSaveLoadResetTests.cs`
  验证的行为（`ViewBinder.OnSaveLoaded` 对 `IEquipmentVisualResettable` 的对账）与原探针完全一致，
  只是不再声称覆盖"模板自身数据集"这条对模板设计不成立的路径；`games/_template/Tests/Runtime`
  下原文件已删除，模板侧改回只由 `GameTemplateSmokeTests.cs` 覆盖默认 `GameOptions` 零阻断加载 +
  主菜单验收。根治过程中另外复现并修复一处真实的 PlayMode 套件间隔离缺口：新夹具最初向
  `UnityEngineHost.SpatialQuery`（跨整个 `-runTests` 批处理进程存活的单例）注册了测试专属单位 id
  却未在用例结束时退订，导致同批次晚跑的 `GreyBoxTests`（`Attack_ReducesBeastHealth_...`/
  `Sfx_PlaysAtLeastOnce_...`）在各自独立的 `WorldSim` 上做空间索引查询时捞到这个陈旧 id 并抛
  `InvalidOperationException`；已在 `finally` 块补上 `SpatialQuery.Unregister`。根治后本机实跑：
  `toolchain/consumer_smoke.ps1` 10/10 步通过（模板 PlayMode `total=4 passed=4 failed=0`）；全量
  `check.ps1`（非 `-Quick`）25 步全过，`Unity EditMode 测试 total=70 passed=70 failed=0`、
  `Unity PlayMode 测试 total=272 passed=272 failed=0`，全仓库具体游戏代号禁用词扫描（`check.ps1`
  第 7 步 a 项）0 命中。
