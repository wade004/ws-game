# 文档代码深度审核（main `8d7057c`）

审计日期：2026-09-07。审计基线为 `D:/workespace/ws-game-main-audit` 的 `main` 提交 `8d7057c049aa623ffd916d61540608550790b801`；本总报告只接受该提交的文件、代码、数据和命令证据。其它分支的未合并提交不视为 main 已交付，后续应先做差异复核再决定是否移植。

执行隔离：当前审计分支为 `codex/main-doc-code-audit-20260907`，工作目录为上述隔离 worktree；`git rev-list --left-right --count main...audit3/fix-waves` 为 `0 9`，即 `audit3/fix-waves` 另有 9 个未合并提交。本报告不把这些提交的修复、样例资产或文档变化归入 main。

## 当前主干结论

本次 main 审计的首要发现是：F1 门禁 helper 存在 stdout/退出码数组误判（P1）；默认存档的 progression、archetype、RNG 注册及 RNG 恢复确定性存在 P1 缺口；读档掉落生命周期也存在 P1 缺口。P2/P3 主要集中在样例资产契约、表现接线、Expr provider、文档副本、回放摘要和模板配置边界。完整原始证据保留在三份分报告，当前总表只做去重索引。

本次 `check.ps1 -SkipUnity -LogFile bin/audit-20260907/check.log` 退出码为 0，脚本汇总 **11 PASS + 6 SKIP**，共 17 步、无 FAIL。该绿灯受 `check.ps1` 输出数组布尔转换缺陷影响，不能独立证明构建、测试、校验器和生成器均可靠通过；失败探针和独立命令证据见本组分报告。Unity、独立版、消费方和 IL2CPP 未在本次运行。

门禁范围的真实边界如下：

| 类别 | 本次状态 | 证据边界 |
|---|---:|---|
| .NET build/test、数据校验、生成器、pytest、禁用词、版本和 `build.ps1 -SkipTests` | 脚本 11 PASS | 受门禁 helper 假阳性风险约束；独立命令需单独看日志 |
| Unity 编译、EditMode、PlayMode | 3 SKIP | 本机探测到 Unity 6000.3.23f1，但未启动全量 Unity |
| 独立版构建/连续冒烟、离散冒烟、消费方演练 | 3 SKIP | 本次未验证发布形态和消费方链路 |
| IL2CPP | 未验证 | 不由 `-SkipUnity` 结果推出 |

直接命令证据：

| 命令 | 退出码/计数 | 证据边界 |
|---|---:|---|
| `dotnet test Core.sln -c Release --no-build` | `0`；6 个程序集合计 1846，跳过 0 | 直接 .NET 证据；不包含 Unity |
| `python -m pytest toolchain/tests -q` | `0`；19 passed | 直接 toolchain 测试证据 |
| `dotnet run --project toolchain/validator -- --data-root data/_framework --json` | `0`；5 tables/122 records/0 errors/1 warning/blocking=false | 直接 framework validator 证据 |

独立验证已保存：[`direct-dotnet-test.log`](../../bin/audit-20260907/direct-dotnet-test.log)、[`direct-pytest.log`](../../bin/audit-20260907/direct-pytest.log)、[`direct-validator-framework.json.log`](../../bin/audit-20260907/direct-validator-framework.json.log)。tracked 结果索引为 [`validation-summary.md`](audit-20260907/validation-summary.md)。门禁原始日志为 [`check.log`](../../bin/audit-20260907/check.log)，快速模式为 [`quick.log`](../../bin/audit-20260907/quick.log)；Quick 实际为 **8 PASS + 9 SKIP**。

## 覆盖矩阵与仓库库存

以下是库存和范围盘点，不表示对 983 个 C# 文件或全部 README 逐文件逐行语义审核；各组报告负责其声明的重点核验。

| 范围 | main 库存/重点核验 |
|---|---:|
| 编号架构正文 `00`～`14` | 15 篇；本组重点 `04`、`11`～`14`、选型、落地计划 |
| ADR | 16 篇（`0001`～`0016`） |
| 模块 README | 81 个（core 63、adapters 4、presentation 12、games 2） |
| schema Markdown | 21 个（`schema/README.md` 16、命名 schema 5） |
| HTML | 16 个（架构图 14、落地计划 1、编辑器产品文档 1） |
| tracked files | 1648；其中 `.cs` 983、`.md` 135、`.html` 16 |
| 数据/资产 | JSON 59；`assets/` 文件 96；placeholder manifest 存在 |

HTML 覆盖范围不等于架构正文镜像：只有落地计划和编辑器产品文档有对应 HTML，14 个架构图页面是独立静态页面。编辑器文档明确实现尚未开始，属于明示预留状态；`IRenderer3D` 的 model 路径由选型和落地计划明确为未交付降级能力，不能以文档声称推导为隐藏缺陷。

## 已确认问题分类

本组完整证据、触发条件、影响、建议、验收标准和代码/文档行号保留在 [`delivery-validation.md`](audit-20260907/delivery-validation.md)。去重时保留原始编号：

| ID | 优先级 | 分类 | 当前结论 |
|---|---|---|---|
| F1 | P1 | 门禁控制流 | stdout 与非零退出码混入数组后可被 `[bool]` 判 PASS，影响多个原生命令检查 |
| F2 | P2 | 工具/参考样例契约 | `_sample` 与 `_placeholder` 交叉检查实跑 16 问题；不等同 16 处运行时资产丢失 |
| F3 | P2 | 文档副本 | 落地计划 HTML 缺少 Markdown 的 J3/K 和当前验证边界，属于同步问题 |
| F4 | P2 | 验证快照 | 历史段有日期；当前 main 快照此前缺失，现补充 1846 测试、11/6 与 8/9 分解 |
| F5 | P2 | README/schema | game state/hook 文档仍写未来接入，代码已实现 registry 优先装配 |
| F6 | P2 | 回放验收盲区 | Digest 未覆盖 HP/伤害 payload；是摘要覆盖缺口，不等同播放器全面失效 |
| P1-01 | P1 | 存档接线 | `player.progression` 必填段未由默认 Gameplay 注册，存在经验静默丢失路径 |
| P1-02 | P1 | 存档接线 | `player.archetype` 必填段未实现/注册，职业与天赋不能由默认存档证明恢复 |
| P1-03 | P1 | 存档接线 | 已有 `RngStreamsPersistable` 未接入默认生产注册 |
| P1-04 | P1 | RNG 确定性 | Load 不清理残留流且不保存 master seed，影响后续流一致性 |
| P2-01 | P2 | Expr 装配 | Targeting/Skill 使用未注入 skill/time provider 的旧工厂，合法表达式可能落默认值 |
| P3-01 | P3 | 输入格式 | `RngStreamState.TryParse` 接受文档不允许的短段文本 |
| GP-PRES-01 | P1 | 读档生命周期 | 掉落先恢复后切场景时被 `ClearAll` 清除，属于静态确定缺口 |
| GP-PRES-02 | P2 | UI 输入契约 | `UiIntents.Move` 的 `dir_x/dir_y` 与消费端 `dx/dy` 不一致；默认键盘路径仍正常 |
| GP-PRES-03 | P1 条件性 | 表现节奏 | 模板离散等待缺少涵盖 merger pending、播放队列和本步待提交动作的综合完成协议；默认 continuous 路径不触发 |
| GP-PRES-04 | P2 | 资源装配 | 模板未把 `IResourceLoader` 注入 VFX/SFX 播放器 |
| GP-PRES-05 | P2 | 表现接线 | sprite 阴影数据存在，但 2D View 没有实际阴影节点/更新链 |
| GP-PRES-06 | P2 | 明示能力落差 | `CharacterRig`、`AnimState`、程序动画原语无运行期入口；需补代码或收窄 09 契约 |
| GP-PRES-07 | P2 | 数据语义 | 锚点文档的 `parent_layer`/`offset_by_direction` 无法进入当前运行期模型 |
| GP-PRES-08 | P3 | 模板文案 | 主菜单标题和兜底按钮硬编码中文，与“数据驱动/本地化”描述不完全一致 |
| GP-PRES-09 | P2 | HUD 覆盖 | 当前 HUD 只有当前行动者、轮次和结束回合，未覆盖完整顺序条和行动点 |
| GP-GAME-01 | P2 接入边界 | 配置消费 | 模板 `DeathPolicy` 暴露并传入，但没有下游消费者；策略切换不改变模板行为，不归咎于 CombatHost |
| X01 | P2 | 文档规则冲突 | 01 对 L5 状态改变有绝对禁令，09 又允许 UI 意图经窄命令契约裁决，需统一例外边界 |
| X02 | P2 | 回放能力边界 | 当前记录的输入类型仅为 Intent/EndTurn，生产录制无默认接线；10 的“整个世界演变”表述超过已证能力 |

以上 ID 已按三份分报告的原始编号聚合：[`delivery-validation.md`](audit-20260907/delivery-validation.md)、[`foundation-rules.md`](audit-20260907/foundation-rules.md) 与 [`gameplay-presentation.md`](audit-20260907/gameplay-presentation.md)；F 系列由本组复核，P/GP 系列保留对应分报告的证据边界，X 系列是跨模块主审发现，未把历史审计待补清单直接转成当前缺陷。

聚合计数：24 项发现，其中 7 项 P1（含 1 项条件性）、15 项 P2（含 1 项接入边界）、2 项 P3。

X01 的精确证据是 `architecture/01_分层与依赖.md:30,66` 及禁止事项第 6 节仍对 L5 改变状态契约作绝对禁止，而 `architecture/09_表现层.md:28,364` 明确允许 UI 意图经窄契约裁决；`UiIntents.Equip/Quest/Buy/Sell` 和对应测试直接调用命令接口。建议 01 改为只读查询与受限用户命令出口并列的规则，与 09 同步。

X02 的精确边界是 `core/foundation/save_system/contracts/Replay.cs:241-265`：ReplayData 同时有 RNG、步长/tick 等元数据，但可驱动的输入记录类型是 `Inputs` 与 `EndTurns`；`core/foundation/save_system/core/ReplayRecorder.cs:46,88-107` 只记录输入、步类型和结束回合。生产源码没有默认构造/接线 ReplayRecorder；而 `presentation/ui/core/UiIntents.cs:85-122` 的 UseItem/Cast/Move 走 `SubmitIntent`，Equip/Quest/Buy/Sell 则直接调用窄契约，`EndTurn` 位于 `:146-159`。因此 10 §8 的“整个世界演变”不能由现有两场战斗测试推出完整用户操作回放；应补命令录制覆盖，或把文案收窄到当前播放器/事件序列能力。

## L4 玩法覆盖与明示能力边界

本轮 L4 盘点覆盖 Loot、Quest、Dialog、Encounter/Level、Difficulty、Achievement、Economy、Spawn/AreaTrigger/WorldState、DeathPolicy。Loot 的生成/拾取单测存在，但读档后的场景生命周期见 GP-PRES-01；该项另有最小机制复现 `loot_after_scene_clear=False`，只证明 `World.ClearAll` 片段会清掉已恢复掉落，不等同完整 Shell/SceneRouter/Unity 端到端。Quest、Dialog、Encounter、Difficulty、Achievement、Economy 主要有机制/单元覆盖，不能由此推出存读档、Unity 或全部内容数据已完成；Spawn/AreaTrigger/WorldState 抽查未见确定断链。HUD 的顺序条/AP 缺口见 GP-PRES-09；DeathPolicy 是确定的模板配置未消费项，见 GP-GAME-01。

明示未实现或预留范围单独记录：`editor/README.md:5` 明示编辑器实现尚未开始，HTML/Markdown 是产品草案；`IRenderer3D`/`kind: model` 路径由选型与落地计划定义为未交付降级能力；`world.map` 部分字段校验、`DisplayMapCoverageRule` 默认登记、affix/anim_set/equip_visual/flag_schema 无示例行等均在 04/05 的范围说明中明示。它们不能与未披露的当前代码断链混为一类。

## 文档更新矩阵

| 发现/决策 | 需要同步的文档或副本 | 更新验收 |
|---|---|---|
| F1、F4 | `check.ps1` 说明、落地计划 Markdown/HTML、README/CI | 失败探针为 FAIL；当前 main 明示 11/6，Quick 明示 8/9；历史段保留日期并与当前快照分栏 |
| F2 | 04、数据 README、asset import 文档、CI | `_sample`/placeholder 参考检查退出 0，或明确 dataset 映射和预期空表边界 |
| F3 | 落地计划 Markdown ↔ HTML | J3/K、最新计数和未验证边界同步，副本漂移可被 CI 发现 |
| F5 | app_lifecycle/hook_registry README 与 `schema/found.*.md` | 说明 `FromRegistry`/`LoadDefinitions`、缺表回退和 framework 默认表 |
| F6 | 10、Replay 契约与回放验收 | 明确 Digest 是否覆盖 HP/伤害等状态，并用夹具验证边界 |
| P1-01～P1-04 | 10、SaveSystem/RNG README、Replay 契约、Gameplay 注册说明 | 存档 JSON 含必填段；Load/Replay 对残留流、新流、master seed 可确定恢复 |
| P2-01 | 04、06、ExprHost/Rules README 与 schema | Targeting/Proc 的 provider 列表与校验/运行时工厂一致 |
| GP-PRES-01/02/03 | 08/09、模板 README、Save/Feedback/Movement README | 分别补掉落场景恢复、UI 参数字段、merger/queue 综合完成协议的代码与验收 |
| GP-PRES-04/05 | ADR-0016、09、Presentation/Unity README | 明确资源 loader 首次引用和 sprite 阴影的实际接线，或收窄契约 |
| GP-PRES-06/07/09 | 09、Presentation README、schema/模板验收 | 分别决定 CharacterRig/动画、锚点方向层语义、HUD 顺序条/AP 是补代码还是降级契约 |
| GP-PRES-08、GP-GAME-01 | 09、模板 README、Combat README | 本地化/数据驱动文案与 DeathPolicy 消费者边界一致 |
| X01、X02 | 01、09、10、Replay 契约和 UI 命令文档 | 统一 L5 只读/受限命令例外；收窄或扩充用户操作回放声明 |

## 修复与复验依赖

先修 F1 并加入“输出一行、退出 7”失败探针，使门禁结果可信；再对 `audit3/fix-waves` 的 9 个 ahead 提交逐项差异复核，决定哪些修复/资产/文档可安全移植，禁止盲目全合并。随后按依赖处理 P1：默认存档 progression/archetype/RNG 注册与 RNG master seed/残留流恢复，接着修掉落恢复与场景清理生命周期。再处理离散 pacing 的 merger/queue 综合完成协议、模板资源加载、Targeting/Skill Expr provider、UI 参数和 DeathPolicy 接入。随后由设计层决定 CharacterRig、锚点、HUD、阴影等能力是补齐还是收窄契约，并处理回放 Digest/X01/X02 边界。最后同步 04/08/09/10、README/schema、落地计划 HTML、CI 和 validation summary，执行直接命令与针对性 Unity/发布形态复验。Unity 全量、独立版、消费方、IL2CPP 需在独占环境另行复验。

## 聚合验收

本总报告已汇入 delivery-validation、foundation-rules、gameplay-presentation 三份分报告的原始 ID；每项结论应回到对应分报告的 main 文件/命令证据。最终验收需保持历史快照、明示未实现、当前代码缺陷和未验证运行时边界分开呈现，并在主审去重后保留跨模块依赖顺序。
