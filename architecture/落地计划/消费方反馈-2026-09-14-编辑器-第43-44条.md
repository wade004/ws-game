# 消费方反馈处理记录（内容编辑器项目，第 43/44 条，2026-09-14）

> 本文档允许出现具体技术名（引擎/语言/框架/工具名），不受 `architecture/00～14` 与
> `architecture/adr/` 正文的禁用词约束（见仓库根 `CLAUDE.md`"硬性规则"）。记录下游"内容编辑器"
> 项目（消费方，仓库 `ws-game-editor`）在其 `docs/上游反馈.md` 第 43、44 条提交的反馈的复现、
> 根因、处理方式与验证结果。此前批次见同目录
> `消费方反馈-2026-09-10-编辑器.md`/`-第二批.md`/`-第三批.md`/`-第四批.md`、
> `消费方反馈-2026-09-11-编辑器-第27条.md`/`-第28-29条.md`/`-第30-32-33-34条.md`/`-第31条.md`/
> `-第35条.md`/`-第36条.md`、`消费方反馈-2026-09-12-编辑器-第37条.md`、
> `消费方反馈-2026-09-13-编辑器-第38-39条.md`、`消费方反馈-2026-09-13-编辑器-第40条.md`、
> `消费方反馈-2026-09-14-编辑器-第41-42条.md`。

**结论先行**：**均已处理并验证，第 44 条有一处已知的既有边界未能覆盖（见下"处理 B 判断记录：
toolchain/validator 侧无法演示同一场景"），已如实告知，不在本批范围内擅自扩大改动。**

第 43 条（低）：`presentation/assembly/ContentValidationAssembly.cs` 新增公开类型
`OptionalRuleDescriptor`（`RuleName`/`CheckName`/`Description` 三只读属性）与
`ContentValidationAssembly.OptionalRules`（固定两项，`CheckName` 直接引用各规则类型自己公开的
`CheckName` 常量，单一来源），`OptionalRuleNames` 保留不变、改由 `OptionalRules` 投影得到；新增
`ContentValidationAssembly.TryGetOptionalRuleByCheck` 按检查名反查描述符；
`ContentValidationRun` 新增 `EnabledOptionalRuleChecks`/`DisabledOptionalRuleChecks`；
`toolchain/validator --json` 在既有 `enabled_optional_rules`/`disabled_optional_rules` 之后追加
`optional_rules: [{rule, check, enabled}]`。

第 44 条（低）：`data/_sample/creature/creature.template.json` 新增
`creature.sample_summon_totem`（`npc_flags` 含 `npc_flag.summon_only`），配套 `display.map`/
`l10n.text` 补齐行，`spawn.table.json` 不引用它（保持示例数据集 0 error 0 warning 基线）；新增
`core/gameplay/spawn/tests/SpawnSummonOnlyCreatureRealSampleDataTests.cs`，用真实
`data/_framework` + `data/_sample` 正式装配加载后，内存追加一条违规 `spawn.table` 行，断言
`SpawnSummonOnlyCreatureRule` 的 Error 分支触发。

全部改动在独立工作树 `wbg/editor43-44` 分支完成，未触碰主树。

---

## 复现确认

原文见 `docs/上游反馈.md`"43　可选规则产出的诊断没有编程可读的"来自哪个可选规则"关联"与
"44　示例数据集 `creature.template` 没有一条 `summon_only` 生物，`SpawnSummonOnlyCreatureRule`
正例分支无法用示例数据验证"。现场复核：

```
grep -n "OptionalRuleNames" presentation/assembly/ContentValidationAssembly.cs
```

改动前命中的 `SpawnSummonOnlyCreatureRuleName`/`DisplayMapCoverageRuleName` 两个私有常量是
PascalCase 类型名字符串（`"SpawnSummonOnlyCreatureRule"`/`"DisplayMapCoverageRule"`）；

```
grep -n "CheckName" core/gameplay/spawn/schema/SpawnValidationRules.cs core/foundation/display_info/core/DisplayMapCoverageRule.cs
```

改动前命中的是两个 `private const string CheckName` snake_case 检查名
（`"spawn_summon_only_creature"`/`"display_map_coverage"`），两组字符串逐字不相等，且全仓库
（`ValidationIssue`/`ContentValidationRun` 均未提供该映射）没有第三处代码把二者关联起来——
确认反馈原文属实。

```
python -c "import json; d=json.load(open('data/_sample/creature/creature.template.json')); print([r.get('npc_flags') for r in d['rows']])"
```

改动前输出 `[None, None]`——`creature.sample_hero`/`creature.sample_beast` 两条记录 `npc_flags`
均未设置，全库不存在任何 `npc_flags` 含 `npc_flag.summon_only` 的生物模板——确认反馈原文属实。

## 处理

### A. 第 43 条：可选规则描述符（规则名 ↔ 检查名，单一来源）

`core/gameplay/spawn/schema/SpawnValidationRules.cs` 的 `SpawnSummonOnlyCreatureRule.CheckName`
与 `core/foundation/display_info/core/DisplayMapCoverageRule.cs` 的
`DisplayMapCoverageRule.CheckName` 由 `private const` 改为 `public const string CheckName`（补
XML 注释：本规则产出的全部 `ValidationIssue.Check` 取值，消费方按检查名过滤时应引用本常量，不要
复制字面量）。两个规则实现内部继续用同一常量，未改变产出的诊断内容。

`presentation/assembly/ContentValidationAssembly.cs` 新增：

- `OptionalRuleDescriptor`（`sealed class`）：`RuleName`（与 `OptionalRuleNames` 取值逐字相同）、
  `CheckName`（直接引用 `SpawnSummonOnlyCreatureRule.CheckName`/`DisplayMapCoverageRule.CheckName`
  公开常量，不是另行抄写的字面量）、`Description`（一句中文说明）三个只读属性，公开三参构造函数。
- `ContentValidationAssembly.OptionalRules`（`IReadOnlyList<OptionalRuleDescriptor>`，固定两项，
  顺序与 `OptionalRuleNames` 一致）——单一来源：规则若改检查名字符串，本清单自动跟着变，不会像
  此前 `OptionalRuleNames` 那样需要消费方另建一份 PascalCase→snake_case 映射表维护。
  `OptionalRuleNames` 保留不变（既有测试 `OptionalRuleNames_IsFixedTwoEntryList` 未改动就通过），
  改为 `OptionalRules.Select(d => d.RuleName).ToList()` 投影得到。
- `ContentValidationAssembly.TryGetOptionalRuleByCheck(string checkName, out OptionalRuleDescriptor descriptor)`：
  按检查名反查描述符，供内容工具（如问题面板）判断"这条诊断是否来自某个可选规则、来自哪个"；
  游戏侧经 `IDataRegistry.RegisterValidationRule` 注册的自定义规则不在本清单内（预期行为，同反馈
  原文判断记录）。
- `ContentValidationRun` 新增 `EnabledOptionalRuleChecks`/`DisabledOptionalRuleChecks`
  （`IReadOnlyList<string>`，与 `EnabledOptionalRules`/`DisabledOptionalRules` 一一对应，构造函数
  是 `internal`——只有本文件的 `Run` 方法调用，改签名不影响 ABI）。

`toolchain/validator/Program.cs` 的 `--json` 输出：在既有 `disabled_optional_rules`/
`enabled_optional_rules` 之后追加

```
"optional_rules":[{"rule":"SpawnSummonOnlyCreatureRule","check":"spawn_summon_only_creature","enabled":false},{"rule":"DisplayMapCoverageRule","check":"display_map_coverage","enabled":true}]
```

`rule`/`check` 直接来自 `ContentValidationAssembly.OptionalRules`，`enabled` 按
`disabledOptionalRules` 是否含该规则名判定；不改动既有两个字段，`--json` 输出对不读取新字段的
调用方保持逐字节兼容。`toolchain/validate_data.py --json` 是"把整份 validator JSON 原样塞进
`result["validator"]`"的透传路径（不解析/不改写任何字段），核实确认新字段无需该脚本任何改动即
自动带上（见下"测试"一节 `test_json_flag_passes_through_optional_rules_field_unmodified`）。
`data/README.md`/`toolchain/validate_data.py` docstring 均未逐字段列出 `--json` 输出结构，核实
后无需同步。

### B. 第 44 条：示例数据补 `summon_only` 生物

`data/_sample/creature/creature.template.json` 新增第三条记录
`creature.sample_summon_totem`（字段顺序核对自 `core/carriers/creature/core/CreatureSchemas.cs`
`CreatureSchemas.Template` 登记顺序）：

```json
{
  "id": "creature.sample_summon_totem",
  "name_key": "l10n.creature.sample_summon_totem.name",
  "level": 1,
  "tier": "creature.tier.sample_normal",
  "base_stats": { "stat.strength": 1, "stat.stamina": 1 },
  "faction_id": "fac.wildlife",
  "npc_flags": ["npc_flag.summon_only"],
  "display_ref": "display.map.sample_summon_totem"
}
```

配套：

- `data/_sample/display/display.map.json` 新增 `display.map.sample_summon_totem`（`category:
  creature`，`logical_id: creature.sample_summon_totem`），复用既有 `sprite.creature.sample_beast`
  精灵集与镜像规则，不新增素材，满足 `DisplayMapCoverageRule` 默认覆盖 `creature.template` 的
  检查。
- `data/_sample/l10n/l10n.text.json` 为 `l10n.creature.sample_summon_totem.name` 补齐
  `zh_cn`/`en_us` 两行译文，满足消费方反馈第 42 条新增的非默认语言缺翻译 Warning。
- `data/_sample/spawn/spawn.table.json` **不引用**这条生物——示例数据集的既有基线是
  `python toolchain/validate_data.py --strict` 0 error 0 warning，若真的引用会触发
  `spawn_summon_only_creature` Error，破坏该基线（反馈原文建议二选一，本次选"不进
  spawn.table"）。
- `data/README.md` 新增"creature 示例数据（summon_only 正例演示）"一节，说明本条新增记录的用途、
  不进 `spawn.table` 的原因，以及下方"正例演示"两处测试的定位方式。
- `toolchain/tests/test_import_sample_assets.py` 的 `EXPECTED_DISPLAY_ROWS`（`display.map`
  行 id/逻辑 id/类别的固定清单回归断言）同步补一行，否则会报"清单外多了一行"。

正例演示：新增 `core/gameplay/spawn/tests/SpawnSummonOnlyCreatureRealSampleDataTests.cs`
（`Tests.Gameplay.csproj` 已引用 `Adapters.Stub`/`Presentation.Common`，同批"CORE114-04"任务留下
的项目引用可以直接复用，未新增项目引用）：

1. 复用 `core/foundation/data_registry/tests/DataRegistryTests.cs`
   `BuildRealFrameworkAndSampleSources` 同一手法，把真实 `data/_framework`/`data/_sample` 整棵
   拷进 `StubFileSystem`，各自包成一个 `FileSystemDataSource`。
2. 用 `Presentation.Assembly.ContentValidationAssembly.CreateRegistry`/`LoadAll`（正式装配入口，
   不是本类自建一套装配顺序）加载；`ICreatureTemplateQuery` 用本文件新增的
   `LazyRegistryCreatureTemplateQuery`——直接读已加载的 `IDataRegistryView`（`npc_flags` 字段），
   不像 `Core.Carriers.Creature.CreatureFactory` 那样预先解析出强类型索引；用
   `Func<IDataRegistryView>` 把"拿 view"延迟到 `LoadAll` 真正跑校验规则那一刻才解引用，打破
   "先要有 registry 才能构造查询、查询又要在建 registry 时就绪"这个顺序依赖。
3. 内存追加一条指向 `creature.sample_summon_totem` 的 `spawn.table` 行（不落盘，走多根合并，
   与真实 `data/_sample/spawn/spawn.table.json` 既有两行不同主键），断言产出的诊断里存在一条
   `Check == "spawn_summon_only_creature"`（同时等于 `SpawnSummonOnlyCreatureRule.CheckName`）、
   `Severity == Error` 的问题，且 `report.IsBlocking == true`。

**判断记录（`toolchain/validator` 侧无法演示同一场景，如实告知的偏离）**：设计原计划还要求在
`toolchain/tests` 补一例 pytest，跑真实 `python toolchain/validate_data.py --strict` 子进程、
往 `spawn.table.json` 加一条违规行，断言退出码非 0 且输出含 `spawn_summon_only_creature`。核实
`toolchain/validator/Program.cs` 现场代码后发现：`SpawnSummonOnlyCreatureRule` 只有
`ContentValidationOptions.CreatureTemplateQuery` 非空时才会注册（见
`GameplaySchemaCatalog.RegisterSpawnSchemas` 判断记录），而 `toolchain/validator`
（一次性命令行进程）按**既有设计**从未接线该依赖——该工具自己的判断记录明确写着"与其注册一条
在未接线时注定产生 Warning 的规则（污染门禁要求的 0 错误 0 警告），不如干脆不注册"。这意味着
`python toolchain/validate_data.py --strict` 这条命令行路径下 `spawn_summon_only_creature`
检查项**恒不生效**，无论示例数据里有没有违规行，都不会在这条路径上产出该诊断——手工验证
`optional_rules` 字段时确认 `SpawnSummonOnlyCreatureRule` 恒为 `enabled: false`（见上"验证"
一节）。这是 `toolchain/validator` 的既有取舍（不是第 44 条反馈引入的缺陷，也不在本条反馈的
"示例数据覆盖面缺口"范围内——要修就要改变该工具"命令行进程不接线 ICreatureTemplateQuery"这条
既有设计决策，属于更大范围的改动，本批不擅自展开），因此**未按原计划新增这例 pytest**，改为把
"规则真的能触发"的演示完整放进上面的 C# 集成测试（用正式装配入口 `ContentValidationAssembly`，
不是自建校验管线，同样具备反馈原文要求的"用真实示例数据、非内存改写既有记录"这一核心诉求）。
已在 `data/README.md` 新增小节里如实写明这条既有边界，供后续消费方查阅时不必重新踩一遍这个坑。

### C. 未涉及的改动范围

- `architecture/00～14` 正文未改——装配入口（`ContentValidationAssembly`）是实现层契约，两条
  新增公开类型/方法均未触及架构文档描述的接口/流程；`04_数据与内容管线.md` 第 5 节校验项清单
  的现有措辞（"检查项固定取值集合"一句）本身未提及"规则名↔检查名映射"这类实现细节，无需同步，
  未发现必须改的措辞。
- `ValidationIssue`（`readonly struct`）未新增字段——第 43 条反馈"建议"部分提到的备选方案之一
  是给 `ValidationIssue` 加 `OptionalRuleName`/`IsOptional` 字段，任务书已明确选定"规则名↔检查名
  描述符清单"这条不改动既有公开签名的路线，未采用该备选方案。
- 未新增技能表 `summon` 效果引用该生物模板的示例——`core/rules/skill/schema/SkillSchemas.cs`
  的 `summon` 效果参数（`creature_template`）本身是低成本的 `FieldKind.Id`，但新增一条
  `skill.def` 记录还需要该记录自己的 `display.map` 覆盖行与 `l10n.text` 译文（`skill.def` 同样
  在 `DefaultDisplayMapCoverageSources` 覆盖范围内），综合成本超出"很低"，按任务书"不低就不加"
  的判断记录不加。

## 测试

`presentation/assembly/tests/ContentValidationAssemblyTests.cs` 新增 4 例：

- `OptionalRuleNames_EqualsOptionalRules_ProjectedByRuleName`：静态投影关系逐项相等。
- `OptionalRules_CheckName_MatchesActualRuleIssueCheck_ViaRun`：**不是**比对两个字面量字符串
  拼写是否一致，而是用真实数据经 `ContentValidationAssembly.Run` 跑出真实诊断，比对
  `OptionalRuleDescriptor.CheckName` 与诊断实际 `Check` 是否相等（两条规则各验证一次）。
- `TryGetOptionalRuleByCheck_HitAndMiss`：命中/未命中两个分支。
- `Run_EnabledDisabledOptionalRuleChecks_CorrespondToRuleNameLists`：`EnabledOptionalRuleChecks`/
  `DisabledOptionalRuleChecks` 与 `EnabledOptionalRules`/`DisabledOptionalRules` 一一对应。

`core/gameplay/spawn/tests/SpawnSummonOnlyCreatureRealSampleDataTests.cs`（新增文件）：见上"处理
B"一节，`RealSampleData_SummonOnlyTotemInSpawnTable_ReportsSpawnSummonOnlyCreatureError`。

`toolchain/tests/test_validate_data_json_output.py` 新增
`test_json_flag_passes_through_optional_rules_field_unmodified`：构造含 `optional_rules` 字段的
伪 validator JSON 输出，断言 `validate_data.py --json` 原样透传（复用同文件既有的
monkeypatch `shutil.which`/`subprocess.run` 手法，不依赖真实 dotnet）。

`toolchain/tests/test_import_sample_assets.py`：`EXPECTED_DISPLAY_ROWS` 补一行
`display.map.sample_summon_totem`（否则 `test_display_map_ids_and_logical_ids_unchanged` 会报
"清单外多了一行"）。

## 验收证据

- `dotnet build Core.sln --artifacts-path <scratchpad>\build_wbg`：0 警告 0 错误。
- `dotnet test Core.sln --artifacts-path <scratchpad>\build_wbg`：六工程全绿——
  `Tests.PresentationCommon.dll` 625 例（含上述新增 4 例）、`Tests.Gameplay.dll` 704 例（含上述
  新增 1 例）、`Tests.Foundation.dll`/`Tests.Numbers.dll`/`Tests.Carriers.dll`/`Tests.Rules.dll`
  与改动前用例数一致，均全绿。
- `python toolchain/validate_data.py --strict`：`tables 64, records 380, errors 0, warnings 0,
  overrides 1`（较改动前 +4 条记录：`creature.template` +1、`display.map` +1、`l10n.text` +2），
  输出行 `optional rules disabled: SpawnSummonOnlyCreatureRule`（与改动前一致，验证了上面"处理
  B 判断记录"所述的既有边界）。
- `python toolchain/format_data.py --schema-order --check`：退出码 0，`共检查 66 个文件，需要
  重排 0 个，跳过版本落后 1 个`——新增行字段顺序符合 schema 登记顺序。
- `dotnet run --project toolchain/validator --artifacts-path <scratchpad>\build_wbg --
  --schema-audit --allowlist toolchain/schema_audit_allowlist.json`：`tables 64, fields 817,
  errors 0, warnings 0`，与改动前一致。
- `python -m pytest toolchain/tests -q`：224 passed, 4 skipped（较改动前 +1 条新用例，
  `EXPECTED_DISPLAY_ROWS` 同步更新后原有失败例恢复通过）。
- 手工核实 `--json` 新字段真实形状（`dotnet run --project toolchain/validator -- --data-root
  data/_framework --data-root data/_sample --json`）：

  ```json
  "optional_rules":[
    {"rule":"SpawnSummonOnlyCreatureRule","check":"spawn_summon_only_creature","enabled":false},
    {"rule":"DisplayMapCoverageRule","check":"display_map_coverage","enabled":true}
  ]
  ```

  `enabled` 取值与同一次输出里的 `enabled_optional_rules`/`disabled_optional_rules` 完全对应。

## 对编辑器的使用建议

1. **问题面板"来自可选规则"标注改用 `ContentValidationAssembly.TryGetOptionalRuleByCheck`/
   `OptionalRules`，不再硬编码映射表**——`Editor.ViewModels.Panels.ProblemsPanelViewModel.
   OptionalRuleCheckNames`（反馈原文点名的硬编码位置）可以整段删除，改为对每条诊断的 `Check`
   调用 `ContentValidationAssembly.TryGetOptionalRuleByCheck(issue.Check, out var descriptor)`，
   命中即标注"来自可选规则：`descriptor.Description`"；框架未来新增第三个可选规则或调整检查名
   拼写时，编辑器这段标注逻辑不需要跟着改一行代码。
2. **"校验设置"面板展示可选规则全量选项，建议改用 `OptionalRules` 而不是 `OptionalRuleNames`**
   ——每项自带 `Description`（一句中文说明），面板可以直接展示，不必自己维护规则名到说明文案的
   映射；`RuleName` 取值仍与 `OptionalRuleNames`/`ContentValidationOptions` 两个接线参数完全
   对应，接线逻辑不需要改。
3. **`toolchain/validator --json` 新增 `optional_rules` 字段是编辑器可选择性消费的"更方便的
   形状"，不是强制迁移**——已经在用 `enabled_optional_rules`/`disabled_optional_rules` +
   自行维护映射表的既有实现继续可用（两个旧字段未改动），迁移到新字段纯粹是省掉那份自维护映射表，
   没有紧迫性。
4. **`SpawnSummonOnlyCreatureRule` 在 `toolchain/validator` 命令行路径下恒为禁用**（见上"处理
   B 判断记录"）——编辑器基础套件若走的是自己的装配路径（如 `Editor.SamplePlugins.
   SampleValidationSourcesPlugin` 一类真正持有 `ICreatureTemplateQuery` 实现的场景）不受影响，
   但如果编辑器某个功能是"调用命令行 `toolchain/validator --json` 拿校验结果"，需要知道这条路径
   下永远看不到 `spawn_summon_only_creature` 诊断——这不是本批改动引入的新限制，是该工具从更早
   版本起就有的既有行为，只是恰好被第 44 条反馈的排查过程重新验证了一遍。
5. **`data/_sample/creature/creature.template.json` 新增的 `creature.sample_summon_totem`**
   可以直接用于编辑器侧"可选规则/summon_only 校验"UI 交互的手工验证或自动化测试固件——真实数据
   集里已经有一条合法的 `summon_only` 生物，编辑器测试如果需要触发 `spawn_summon_only_creature`
   Error，只需要（在测试夹具里、不落盘到真实示例数据）让某条 `spawn.table` 记录引用它，不必再
   自行改写 `creature.sample_beast` 等既有记录的 `npc_flags`。

## 提交

见 WBG 最终汇报正文提交哈希列表（分支 `wbg/editor43-44`，未推送远端）。

## 涉及文件

见 WBG 最终汇报正文"改动文件清单"一节，此处不重复列出以避免与实际提交内容脱节。

## 供 CHANGELOG 1.29.0 文案（草稿，供设计层核对后落笔到 `CHANGELOG.md`）

```
### 新增

- `Presentation.Assembly.ContentValidationAssembly.OptionalRules`/`OptionalRuleDescriptor`/
  `TryGetOptionalRuleByCheck`——可选规则的"规则名 ↔ 检查名"关联单一来源，`SpawnSummonOnlyCreatureRule`/
  `DisplayMapCoverageRule` 的 `CheckName` 常量随之改为公开；`ContentValidationRun` 新增
  `EnabledOptionalRuleChecks`/`DisabledOptionalRuleChecks`；`toolchain/validator --json` 在
  `enabled_optional_rules`/`disabled_optional_rules` 之后追加
  `optional_rules: [{rule, check, enabled}]`（见消费方反馈第 43 条，
  architecture/落地计划/消费方反馈-2026-09-14-编辑器-第43-44条.md）。【编辑器相关契约】
- 示例数据集 data/_sample/creature 新增 summon_only 生物模板 creature.sample_summon_totem
  （不进 spawn.table，保持示例数据集 0 error 0 warning 基线），供内容工具/测试用真实数据验证
  SpawnSummonOnlyCreatureRule 的 Error 分支（见消费方反馈第 44 条，同上文档）。
```

（版本号与发布时机由整合 agent 处理，本条不单独发布；上述文案仅供参考，最终以整合 agent 落笔
为准。）
