# 消费方答复：游戏接入方第四十四批已交付（2026-09-26）

## 结论

阻塞解除，落在 **1.80.0**（标签 `v1.80.0`，发布提交 `12322f2b`，功能提交 `16ca8121` + `def895de`）。ADR-0099。你方给的两个期望都做了，另外把抖动的来源也在核心侧修掉了。

## 修法（三层）

1. **修根（核心）**：沿路径移动时朝向按当前路点段的两个端点算一次，同一直线段内不再逐 tick 用插值位置重算 `Atan2`；路径跟随、追击（ADR-0097）、受控位移三处一致。单段路径 20 tick 的 `Facing` 取值数：修复前实测 6 个 → 现在 1 个。
2. **判据（表现层）**：纸娃娃层重合成只在 `ResolveDirectionSlot` 结果（槽位 id + 镜像标志）变化时发生，不再比较 `RawRadians`；`Direction.Equals` 契约未动。同档位内 60 次末位抖动：修复前重合成 61 次 → 现在 0 次，body 层 0 帧回到静态图。
3. **兜底（表现层）**：`SpriteViewBase` 新增受保护虚方法 `OnLayersComposed`（ABI 加法），任何写入渲染器的纸娃娃层合成——换向、换装、**首次引用资源异步加载完成后的迟到回填**——都经同一出口触发；视图工厂据此把逐层动画的当前帧立即写回，不推进时间轴。槽位真变化的那一帧 body 层已是走路帧。

## 凭据

- 核心：`SingleSegmentPath_TwentyTicks_FacingHasExactlyOneDistinctValue`（6→1）、`TwoSegmentPolyline_FacingHasExactlyTwoDistinctValues`（3→2）。`Tests.Carriers` 659/659。
- PlayMode（真实视图工厂 + 精灵视图 + 逐层走路剪辑）：`SyncPose_SameDirectionSlotRawRadiansJitter_DoesNotRecomposeOrResetAnimatedLayer`（重合成 61→不变、静态帧 0/60）；`SyncPose_RealSlotChangeOrEquipChange_RecomposesAndImmediatelyBackfillsCurrentFrame`（槽位变化/换装/冷加载完成三支均立即回填，修复前实测停在静态层 `layer.…__body`；无动画层不误伤）。定向 PlayMode 26/26；`abi_probe` breaks=0 additions=2；发布门禁见发布提交。

## 你方要做的

升级 1.80.0 后重跑 `01d_walk_screen_jitter` D60 段，`static_body_frames` 应为 0、`facing_distinct` 应为 1（单段直线），然后按你方计划把 `static_body_frames == 0` 加进判定。
