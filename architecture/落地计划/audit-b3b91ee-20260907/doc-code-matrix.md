# 文档—代码矩阵

本表针对固定提交 `b3b91ee624332c56637240fd1bc7941c6b2ebbc5`。`漂移` 是文档对当前代码的字面描述不成立或互相冲突；`待实现` 是当前代码/接线确实缺失；`预留` 是文档已经明确声明的范围，不能升级为隐藏缺陷；`已修历史` 只记录本快照中能看到的修复结果，不代表本轮 Runtime 通过。

## architecture 00–14

| 文档 | 具体漂移或待实现 | 明确预留/边界 | 已修历史或当前一致项 |
|---|---|---|---|
| `architecture/00_架构总则.md` | 本轮未发现新的高置信源码断链；其“单线程确定性、层间边界”仍应作为后续修复约束。 | 本轮不把总则中标明的未来扩展位升级为缺陷。 | 已有总则约束与当前基础模块审查结果相容。 |
| `architecture/01_分层与依赖.md` | 01:30,66,72/185 对 L5 写成对 L0–L4 一律只读、只订阅；09:31,373 允许 UI 输入经窄契约提交玩法命令，代码 `UiIntents` 直接调用 `IQuestHost.Accept`、`Dialog.ChooseOption`、`Economy.Buy/Sell`（`presentation/ui/core/UiIntents.cs:124-132`）。需统一“查询只读、命令请求由下层判定”的边界；不能把这些调用全部判为非法写回。 | 各层共享目录、构建程序集并集的说明已明确；L5 不直接改变逻辑状态仍是铁律。 | 多次模块表名称/事件/装配根勘误已落在文档变更记录；本轮只保留跨文档冲突。 |
| `architecture/02_引擎适配层.md` | 本轮未发现新增接口签名漂移；仍需对 async 资源回调和适配层生命周期执行 Runtime/Conformance。 | `IRenderer3D`/model 形态可声明降级，Unity 实现全部 `NotSupportedException` 是选型边界，不计 bug。 | stub/conformance 与已核对接口保持现状；未以静态构建推出 Unity/IL2CPP 通过。 |
| `architecture/03_运行时骨架.md` | SceneRouter 类型注释仍有“无 Loading→MainMenu”旧注释（`core/foundation/scene_router/core/SceneRouter.cs:22`），而 03:45 已定义加载失败回退；同时 SceneRouter 回调缺 generation（FND-02）。03 的 timer 宿主语义与 `WorldSim.ClearAll` 换实例冲突（FND-04）。 | `atb` 是预留先攻位（03:82）；加载画面具体呈现由游戏层提供。 | AppState、Rng.Reset、Hook 返回句柄、Loading→MainMenu 迁移已在本快照接入；旧 audit 关于这些“未实现”不再成立。 |
| `architecture/04_数据与内容管线.md` | 04:196 把所有时间字段概括成统一解释，未列出召唤拒绝 discrete、离散学派锁未推进、channel 默认 tick 语义等模块例外；应改为字段—消费者矩阵。TurnScheduler fixed_order AP 顺序也与 04:193–204 的步骤冲突（FND-05）。 | 本轮不把未由评审确认的字段校验缺口扩写为新问题。 | 框架数据表、版本 0.2.0、事件常量与 placeholder 检查在本轮有独立命令证据。 |
| `architecture/05_对象模型与世界.md` | WorldSim 清场替换计时器导致旧句柄代际问题（FND-04）；跨图 Encounter 清理未在 05/08 的生命周期图中落成（GP-04）。 | model/投影和地图引擎资源由适配/游戏层完成。 | 掉落读档后的重挂、单位阻挡、实体销毁分发等上一轮问题在当前代码已出现对应接线；不重复报告。 |
| `architecture/06_规则层_属性技能战斗AI.md` | 目标解析步骤 202–214 与代码显式 targets 跳过 chain（RC-10）冲突；stack_category 220 声称按类别叠加，但 `AuraHost` 实际按 `(target, defId, source?)`，`AuraDef.StackCategory` 仅解析/存储未消费；channel 210 的“每己方回合一次”与代码取首个 effect `tick_interval` 不符。控制递减 225 写成可选策略但当前无配置/实现。`weapon_damage_pct`（06:130）被代码当作 BaseValue，不是真实武器基数；落点请求、FindUnits、召唤 discrete、天赋激活、移动效果也仅部分实现（详见 code-review/RC 与下表）。 | `atb`、附魔/宝石等不是当前通用原语；新控制递减可按 12 走扩展。不要把 model 3D 或游戏层自定义目标链算作框架缺陷。 | Charges、离散 action_cost/GCD、time.* provider、race.passive_auras、共享 is_casting 工厂等旧缺口在当前快照已有代码，不能照搬历史审计。 |
| `architecture/07_载体层_物品生物物件.md` | 07:213–216 对召唤跟随/同步/离散操控描述超过当前实现：SummonTick 只连续推进，SyncCombatState 仅主人进战单向；07:227 武器变体输入未区分 Equipment profile 查询与战斗/表现自动消费。装备快照替换缺口见 FND-10。 | 附魔/宝石/耐久/绑定/随机属性是 07:1.6 明确预留；召唤离散生命周期/跟随、weapon_damage_pct、落点和移动插值仍待实现或收窄契约。 | `ISummonHost`、双向 Combat 仇恨、projectile spawner、装备/单位主路径和直接效果 Tags 已有实现；不能用旧“未接线”清单覆盖当前代码。 |
| `architecture/08_玩法层_掉落任务对话关卡.md` | QuestPersistable 合并旧状态（GP-01）、消耗失败仍加进度（GP-07）、Accept 不读已有库存（GP-08）、Dialog 不重验条件（GP-05）、切图不终止旧 Encounter（GP-04）。08:100 的 escort/escort_route_ref 只有数据/通用手动推进，无自动路线、护送死亡流程；08:118 所称激活目标位置查询与 `IQuestHost.GetActiveObjectives` 只返回 targetRef 不符。buy_price_rule 的曲线/物品上下文也未实现（`core/gameplay/economy/README.md:41-54`）。 | 自动存档游戏层接线、地图/任务触发点由 08/10 标为接入责任；不要把“框架不提供默认接线”当作实现错误。 | Loot 场景重挂、DeathPolicy/回档、WorldState/Economy 主流程已在当前代码存在；本轮只报新的任务/遭遇边界。 |
| `architecture/09_表现层.md` | 09:178 已声明 sprite `anim_set`，但 13/14 仍主要把 anim_set 写成 model 资产；需补 sprite 映射、冷加载和关键帧接法。09 的锚点跟随/屏幕容器描述超过 `VfxPlayer` 只在 Spawn 时解析一次坐标的实现。GP-02/03/06/09/10 是默认表现接线缺口；01 与 09 的 UI 命令边界冲突仍需同步。 | model 3D/投影/真实阴影可以保持降级；不将 `ModelCharacterRig` 占位升级为 P1。表现层只能通过逻辑命令请求改变状态，最终判定在 L4/L2。 | CharacterRig/AnimState/程序动画、Feedback.HasPendingPlayback、ResourceLoader 传入、HUD 顺序/AP、sprite 阴影和 merger 探针已有代码；旧的“组件不存在/无 HUD/零事件卡死”不再成立。 |
| `architecture/10_存档与持久化.md` | FND-01/07/09/10 覆盖路径、备份候选、迁移目标版本和快照替换。10:72/save README:132 的 RNG 文本遗漏 `master_seed`/Reset；10:172 的 Replay 格式/README 版本停在旧值；save README:79 固定序遗漏 `player.vitals`；README:99 称缺段警告但 `ComputeReadOrder` 只遍历存档键，该警告路径不可达。10:40 的 game_id 防跨游戏声明与 `SaveSystemOptions` 未检查不一致；加载 meta 交给调用方前已修改段。 | 自动存档由游戏层触发；持久光环按配置可选；不能把这些责任边界写成框架缺陷。迁移允许跨中间版本，但最终必须恰达当前运行时版本。 | progression/archetype/RNG 默认注册、LoadedFromBackup、死亡回档流程是当前快照已修历史；但本轮新增 SaveSystem 槽/迁移/生命周期问题仍须处理。 |
| `architecture/11_工程规范与测试.md` | TOOL-01 表明 `check.ps1` 的 native helper 可在缺程序时误 PASS；反馈 event 的专项 validator 仍未接入（`presentation/assembly/README.md:122` 是当前状态，不是旧文案）。本轮固定 .NET/pytest/数据/资产检查均不替代 Unity/消费方验收。 | 编辑器实现尚未开始（`editor/README.md:5`）；发布形态、Unity、IL2CPP 和消费方演练需显式运行。 | 本轮固定测试计数和工具检查已有独立验证；历史报告中的过期小计不作为当前证据。 |
| `architecture/12_扩展与变更流程.md` | 文档声明新增内容通常“只加数据”，但新原语/新任务目标/新对话动作仍需 ADR、注册表、代码、校验和测试（12:40–60）；应避免把 escort、talent activation、stack category 等未实现消费者误当数据填充即可。 | 明确的扩展位和“数据引用未实现时报未实现”流程有效；不要求本轮实现编辑器或全部未来原语。 | 现有 ADR 记录了时间模型、表现形态、资产管线等历史裁决；本表只指出需同步真实代码边界。 |
| `architecture/13_新游戏接入指南.md` | `games/_template/README.md:41` 称传 `xxxOptions` 决定启用模块，但 `GameplayAssembly.cs:485,503,618` 无条件构造 Economy/Quest/Encounter；13:110,133/14:311 对 anim_set 偏向 model，未说明 sprite 默认工厂和冷加载。 | model 型渲染是可选降级；SampleNewGameStarter 只提供最小地图/位置/模板重置，不能作为完整新局状态证明。 | 13 的自动存档默认值、unit_block、镜头 profile 等已同步到当前代码；不要重报旧文档值。 |
| `architecture/14_资产规格书模板.md` | TOOL-02 暴露 ID→文件名归一化碰撞（sfx/vfx）；14:186–188 声称 anchor 跟随位移/旋转、screen 不随世界缩放，但当前 VFX 只在 Spawn 时解析坐标。14 需补 sprite anim_set 默认映射和 collision-safe 编码。 | 地图导航/碰撞由适配层手工制作；model socket 仅结构规则；扁平文件名是打包后可选形态，不是默认导入布局。 | map 子命令、placeholder 资产和全量 asset check 已在本快照出现，不把旧“无 map 导入”列为当前缺陷。 |

## 模块 README 对照

| README 范围 | 具体漂移/待实现 | 明确预留或已修历史 |
|---|---|---|
| `core/foundation/save_system/README.md`、`scene_router/README.md`、`data_registry/README.md`、`input_map/README.md`、`sim_loop/README.md` | Save README:69/108–109 的备份/NotFound 语义与 FND-01/FND-07 冲突；迁移、固定段序、RNG master seed 文本需同步。SceneRouter README:116 仍称 hook 为 void，而 03/接口已返回 `SubscriptionHandle`；并补 generation 语义。DataRegistry README 需说明加载错误持久性。 | EventBus/Expr/Localization/DisplayInfo 主实现抽查未发现本轮新 P1；旧 AppLifecycle/Hook/Rng.Reset 记录已修。 |
| `core/rules/skill/README.md`、`combat/README.md`、`targeting/README.md`、`ai/README.md`、`expr_host/README.md` | skill README:85–89 把 GCD 统一写在读条开始，实际瞬发/读条/引导时点不同；75–78 把 FindUnits 描述为“未注入空间查询才空”，而 `SkillHost.cs:113-120` 始终为空；129–139 称 EffectDispatcher 无 tags 已过期。与 RC-01/04/07/08/10、06 的目标/stack/channel/weapon 语义一起更新。 | 直接效果 Tags、Charges、共享 is_casting provider 等旧缺口已修；targeting fallback cycle 有 validator，不等于显式 targets 可绕过最终过滤。 |
| `core/carriers/item/README.md`、`unit/README.md`、`summon/README.md`、`projectile/README.md`、`creature/README.md`、`gobj/README.md` | item README 需写快照替换和来源计数；summon README 与 07:213–216 一样应区分连续已实现和离散跟随未实现；weapon profile 是查询接口，未自动接入战斗/表现。projectile README 需说明整步射程截断验收。 | enchant/affix 等扩展位、gobj 内置行为 ADR 例外保留；projectile spawner、Ai/Aura 的 entity.destroyed 清理已修，不覆盖 Combat 的 RC-02。 |
| `core/gameplay/quest/README.md`、`dialog/README.md`、`encounter/README.md`、`economy/README.md`、`loot/README.md`、`assembly/README.md` | quest README:5 称八种事件驱动，但无 escort/路线完成/护送死亡宿主；economy README:41–54 的 buy_price_rule 未提供 item_level/quality 上下文；dialog README 需补选择时重新校验；encounter README 需补 LeaveMap 终止实例。对应 GP-01/04/05/07/08。 | Loot 重挂、DeathPolicy、默认 pacing/persistable 接线已在当前快照；自动存档和游戏内容由游戏层提供。 |
| `presentation/README.md`、`render/README.md`、`view_binding/README.md`、`feedback_binder/README.md`、`vfx_sfx/README.md`、`ui/README.md`、`assembly/README.md`、`common/README.md` | GP-02/03/06/09/10：默认动画完成/Forget、冷剪辑加载、VFX.Update、异步 pending 完成门、模板 Rig.Update；`presentation/common/README.md:94` 仍称 interpolation alpha 未接通，已由 `GameplayAssembly.InterpolationAlpha`/宿主消费；`presentation/assembly/README.md:52` AnchorResolver 留空已过期（代码 `PresentationAssembly.cs:329` 已传入），但 :122 FeedbackRuleValidator 未接入是当前待办。14 的锚点跟随承诺也需收窄。 | CharacterRig/AnimState/程序动画/merger/HUD 顺序/AP/ResourceLoader 接线已存在；不把旧“无组件/无 HUD/无 merger”重新计数。 |
| `toolchain/README.md`、`games/_template/README.md`、`adapters/unity/README.md`、`adapters/stub/README.md`、`adapters/conformance/README.md`、`data/README.md` | TOOL-01/02 与模板选项接线漂移；模板 README:41 的模块启用说明需改为真实构造边界；toolchain 需记录 collision-safe 命名和缺程序失败探针。Unity README 的 model 降级应保持；不要把静态桩测试当 Unity Runtime。 | map/placeholder/全量资产检查在当前快照已落地；UnityRenderer3D 全 NotSupported 与 editor README 的未开始状态是明确边界。 |

## 已修历史与本轮排除

当前快照已看到但本轮不重复的修复包括：progression/archetype/RNG 默认注册与恢复、DroppedLoot 场景重挂、`UiIntents.Move` 改用 `dx/dy`、三入口 ResourceLoader 注入、Feedback merger pending 探针、sprite 阴影、HUD 顺序/AP、DeathPolicy/死亡回档、Charges/action_cost/GCD/time provider、`race.passive_auras`、Target/Skill/AI 共享 provider、双向 Combat 仇恨、projectile spawner 与 Summon 连续 ShareThreat。它们仍需按 validation 中的运行边界重新验收，但不能作为本轮静态缺陷。

## 当前能力的未实现或部分实现清单

下表只列评审已核对的实际能力边界。处理栏分三类：过时事实/API 签名直接勘误；确定的错误行为修代码并补回归；计划能力标明当前状态和消费者责任。`weapon_damage_pct` 与 code review 的 RC-11 是同一问题，不另计数。

| 能力 | 现在已有 | 缺少的实际消费者/宿主 | 源码锚点 | 处理 |
|---|---|---|---|---|
| 技能落点 `TargetPoint` | 输入形状声明含 `point`，静态 teleport 可读 `params.point` | `SkillTickHandler` 到 `CastSkill` 的生产入口不传本次玩家/AI 落点；动态目标点未贯通 | [SkillTickHandler.cs:13](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/SkillTickHandler.cs:13)、[EffectDispatcher.cs:275](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/EffectDispatcher.cs:275) | 区分静态内容坐标与每次输入目标点；补 `TargetPoint` 消费链或收窄 06/README。 |
| `ISkillHost.FindUnits` | 接口方法与 `Shape/origin/filter` 参数存在 | 当前实现即使存在空间索引也直接告警并返回空列表，无真实查询消费者 | [SkillHost.cs:113](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/SkillHost.cs:113) | 修复为注入 `ISpatialQuery` 的真实查询并补 AOE 验收，或明确该方法未实现。 |
| 天赋树激活 | `ArchetypeRegistry` 可解析树节点、前置、cost、grants | 无激活/点数消费/撤销 grants 的运行宿主；奖励装配 `talentPointGranter` 为 null | [ArchetypeRegistry.cs:88](D:/workespace/ws-game-review-b3b91ee/core/numbers/archetype/core/ArchetypeRegistry.cs:88)、[ArchetypeRegistry.cs:154](D:/workespace/ws-game-review-b3b91ee/core/numbers/archetype/core/ArchetypeRegistry.cs:154)、[GameplayAssembly.cs:478](D:/workespace/ws-game-review-b3b91ee/core/gameplay/assembly/GameplayAssembly.cs:478) | 标为数据读取已具备、激活宿主待实现；补 L4/L1 依赖和存档验收后再宣称天赋可用。 |
| 光环 `stack_category` | Schema/解析和 `max_stacks` 字段存在 | 运行时槽位按 `(target, defId, source?)` 处理，未消费类别级互斥/叠加语义 | [SkillDefCache.cs:259](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/SkillDefCache.cs:259)、[Defs.cs:97](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/Defs.cs:97)、[AuraHost.cs:76](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/AuraHost.cs:76) | 06/skill README 改为“同定义叠加已支持；类别级语义待实现”，补 ADR/代码或收窄文档。 |
| 控制收益递减（diminishing returns） | 控制标记/免疫和部分施法锁路径存在 | 本范围没有可配置的控制收益递减策略，`AdvanceSchoolLocks` 的离散推进另见 RC-07 | [CastPipeline.cs:282](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/CastPipeline.cs:282) | 作为未来扩展或补配置/实现；不要把 06:225 的“可选策略”写成当前能力。 |
| 召唤离散生命周期/跟随 | 连续 `SummonTickHandler` 推进 duration、owner 和跟随；玩家控制查询已接 | Discrete 分支直接返回，离散 duration/失主/跟随未推进 | [SummonTickHandler.cs:53](D:/workespace/ws-game-review-b3b91ee/core/carriers/summon/core/SummonTickHandler.cs:53) | 标为连续已实现、离散待实现；补离散 Tick 与等待输入接线后验收。 |
| charge/leap/knockback 轨迹 | 效果能计算最终落点并写入单位位置 | 没有插值、路径和碰撞过程；只保证瞬时最终位置 | [EffectDispatcher.cs:283](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/EffectDispatcher.cs:283)、[EffectDispatcher.cs:294](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/EffectDispatcher.cs:294)、[EffectDispatcher.cs:301](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/EffectDispatcher.cs:301) | 将当前能力写为瞬时落点；需要路径/碰撞时，补移动消费者和专项测试。 |
| `weapon_damage_pct` | 效果类型和武器 profile 查询存在 | Resolver/EffectDispatcher 未读取武器基础伤害；默认链把 pct 当基础值 | [EffectDispatcher.cs:145](D:/workespace/ws-game-review-b3b91ee/core/rules/skill/core/EffectDispatcher.cs:145)、[EquipmentHost.cs:254](D:/workespace/ws-game-review-b3b91ee/core/carriers/item/core/EquipmentHost.cs:254) | 见 RC-11：注入武器基础伤害，计算 base×pct，定义无武器策略。 |
| escort 任务 | 任务目标/事件和手动 `UpdateProgress` 存在 | 无 escort route 行走、路线完成、护送目标死亡自动宿主 | [QuestHost.cs:172](D:/workespace/ws-game-review-b3b91ee/core/gameplay/quest/core/QuestHost.cs:172)、[quest/README.md:5](D:/workespace/ws-game-review-b3b91ee/core/gameplay/quest/README.md:5) | 将 08/README 改为数据解析+手动推进已具备，自动护送标待实现。 |
| Vendor 曲线/物品价上下文 | `buy_price_rule` 可解析并以 Expr 数值求价 | 没有 `item_level/quality` 的物品 Expr provider，也没有曲线引用消费 | [EconomyHost.cs:15](D:/workespace/ws-game-review-b3b91ee/core/gameplay/economy/core/EconomyHost.cs:15)、[economy/README.md:43](D:/workespace/ws-game-review-b3b91ee/core/gameplay/economy/README.md:43) | 限定当前为简单 Expr 数值，曲线和物品上下文列为待实现/需新契约。 |
| VFX anchor/screen 跟随 | Spawn 时可解析 anchor 或 screen 到世界坐标，并有实体位置兜底 | 播放后没有跟随实体/锚点的持续更新或独立 screen 容器 | [VfxPlayer.cs:341](D:/workespace/ws-game-review-b3b91ee/presentation/vfx_sfx/core/VfxPlayer.cs:341)、[VfxPlayer.cs:396](D:/workespace/ws-game-review-b3b91ee/presentation/vfx_sfx/core/VfxPlayer.cs:396) | 将合同收窄到生成时坐标解析，或补持久在线挂接宿主；不与 GP-03 重复计数。 |
| Unity 3D/model | `UnityRenderer3D` 与 `ModelCharacterRig` 接口/占位存在 | 所有 Unity 3D 方法均 NotSupported，未提供真实模型/骨骼/挂点消费 | [UnityRenderer3D.cs:18](D:/workespace/ws-game-review-b3b91ee/adapters/unity/Packages/com.gamefoundation.adapter.unity/Runtime/EngineAdapter/UnityRenderer3D.cs:18) | 维持明确降级；新游戏选择 model 时，必须在游戏/适配层另行实现并做 PlayMode。 |
| 编辑器与模块启停 | 编辑器产品文档和 `GameOptions`/Assembly 参数存在 | `editor/README` 明示实现尚未开始；模板 Options 并非模块启停开关，GameplayAssembly 无条件构造 Economy/Quest/Encounter | [editor/README.md:5](D:/workespace/ws-game-review-b3b91ee/editor/README.md:5)、[GameplayAssembly.cs:485](D:/workespace/ws-game-review-b3b91ee/core/gameplay/assembly/GameplayAssembly.cs:485)、[GameplayAssembly.cs:503](D:/workespace/ws-game-review-b3b91ee/core/gameplay/assembly/GameplayAssembly.cs:503)、[GameplayAssembly.cs:618](D:/workespace/ws-game-review-b3b91ee/core/gameplay/assembly/GameplayAssembly.cs:618) | 直接勘误模板 README 的启用语义；编辑器继续标预留，需要裁剪时使用自定义 composition root。 |
