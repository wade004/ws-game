# b3b91ee 深度审计报告（2026-09-07）

审计基线：`D:/workespace/ws-game-review-b3b91ee`，提交
`b3b91ee624332c56637240fd1bc7941c6b2ebbc5`，分支
`codex/deep-review-b3b91ee-20260907`。本目录是新增审计产物；生产代码、既有文档与既有测试未修改。

## 总体结论

主模块已存在且基础测试通过，架构分层和模块边界具备复用基础，但当前仍有正确性缺口和默认接线缺口。本轮不批准生产就绪，也不提供虚构的完成百分比。P1 首先集中在存档/场景生命周期、任务与表现生命周期、战斗施法生命周期，以及事件触发链；P2 集中在数据错误保留、回合/计时器/输入边界、目标与派生数值、异步表现和导入工具碰撞。

去重后共 33 条：P1 9 条、P2 24 条。逐条证据、触发、影响、建议和验收见 [code-review.md](code-review.md)。

修复建议按依赖顺序分为四组：

1. **基础生命周期与存档完整性（P1）**：先修 SaveSystem 的槽/备份隔离、正式档缺失回退、迁移目标版本、装备/任务快照替换；SceneRouter 回调 generation；WorldSim 计时器代际；再验证跨槽、切图、读档和销毁。
2. **规则与战斗安全（P1/P2）**：修 Proc 事件链预算、Combat/Cast 对销毁与死亡的清理，再处理技能目标解析、AP 扣除、装备技能来源、派生值重算、学派锁和召唤离散语义。
3. **玩法、输入与表现默认链（P1/P2）**：修旧遭遇切图、任务条件/库存/消耗、Dialog 重验；补默认动画瞬态结束/复活销毁回收、冷资源加载与离散门、VFX 生命周期、模板 Rig 时间轴。
4. **工具和文档收口（P2）**：修门禁缺失程序假阳性与资产 ID 碰撞；同步 04/06/07/08/09/10/13/14 及相关 README 的真实边界，保留明确预留与已修历史，最后重跑验证和 Runtime/Unity 专项。

## 证据边界

本报告中的源码问题均以固定提交静态核对为基础；有界复现链接仅把对应条目升级到该复现边界，不得解读为 Unity/完整 Runtime 通过。TOOL-01 的 AST 提取复现和 TOOL-02 的真实 `sfx_cmd.run` 复现仅证明各自触发路径，不替代完整 CI 或 Unity 验收。历史审计日志只作为背景，未当作本轮执行证据。

本轮没有执行 Unity/EditMode/PlayMode/IL2CPP、独立发布构建或消费方运行；因此不对渲染观感、异步资源真实时序、跨场景 Runtime 行为和发布产物作通过结论。明确未实现/预留见 [doc-code-matrix.md](doc-code-matrix.md)，验证命令与局限见 [validation.md](validation.md)。

## 报告索引

- [code-review.md](code-review.md)：33 条去重问题，含绝对源码路径与行号。
- [doc-code-matrix.md](doc-code-matrix.md)：architecture 00–14、模块 README 的漂移、待实现、预留和已修历史。
- [validation.md](validation.md)：本轮固定验证、工具复现索引与未验证范围。
