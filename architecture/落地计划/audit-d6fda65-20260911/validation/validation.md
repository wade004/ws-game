# Frozen validation record

Frozen checkout: `D:\workespace\ws-game-audit-d6fda65-20260911`, commit `d6fda65cd6b00ede5c5f6f606f724d2ab0f60603`. The source checkout `D:\workespace\ws-game` was read only. Tracked file count in the frozen checkout is 3,182; the module breakdown is in [tracked-modules.md](tracked-modules.md).

`check.ps1 -SkipUnity` ran with an isolated ignored output directory, `bin\_validation_d6fda65`, and exited **1**. It recorded 24 steps: 16 PASS, 1 FAIL, and 7 SKIP. The only recorded failure was `python -m pytest toolchain/tests -q`; the independent rerun completed **186 passed, 4 skipped** under Python 3.13.9. The original gate result remains FAIL in the evidence; the rerun is separate corroboration. See [raw/check-skipunity.log](raw/check-skipunity.log) and [raw/pytest-toolchain-rerun.log](raw/pytest-toolchain-rerun.log).

The .NET build passed with 0 warnings and 0 errors. The six test projects passed 3,208 tests: Foundation 956, Numbers 132, Carriers 401, Rules 485, Presentation.Common 577, and Gameplay 657. Data validation, schema audit, event constants, placeholder assets, sample asset references, schema ordering, forbidden-word scans, EOL, version consistency, DLL synchronization, and package manifest checks passed. Unity compilation, EditMode, PlayMode, standalone build/smoke, IL2CPP, and consumer smoke were **NOT_RUN** because `-SkipUnity` was specified. The ABI probe in this gate was a visible **SKIP** because the frozen worktree has no local `dist\ws-game-1.12.0.zip`; the original read-only checkout does contain that historical baseline, so this gate result does not claim the ABI probe was impossible outside the frozen checkout.

The current formal release was read from `D:\workespace\ws-game\dist` without rebuilding or modifying it. Its SHA256 identity and the byte/hash comparisons for the six core DLLs, `Adapters.Stub.dll`, `Validator.dll`, `MANIFEST.txt`, lock fields, and package versions are in [identity.json](identity.json). Formal ZIP SHA256 is `1700390fcfc7a86272cc151f49e50d5ca05f7d878d85e8217aeb4b78716bd721`; formal lock SHA256 is `3122cf47e621a9e89cd09a8512707402e38e48f818af9b8747c462597ef9bfbf`.

An independent formal ABI rerun used the historical 1.12 baseline and current formal 1.18 package: the old consumer exited 0 against both, assembly hashes matched, and the surface comparison reported 0 breaks and 275 additions. This is separate from the gate's baseline-missing SKIP and is recorded by the audit evidence supplied alongside this validation directory.

The release repair reproduction is under [release-repair](release-repair). The YAML excerpt records the source branch at `.github/workflows/release.yml` lines 396–535; the lock reconstruction is equivalent to lines 489–513. The formal lock has `headless_dlls`, `validator_dlls`, and `samples`. This isolated reproduction models the DLL lock branch and intentionally omits the separately conditional samples regeneration; it demonstrates the specific missing `headless_dlls`/`validator_dlls` fields, while making no claim that the full branch always omits `samples`. Against one copied ZIP with one byte appended to `Adapters.Stub.dll`, the original lock caused `get_framework.ps1 -FromLocalDist` to exit 1 and left no target; the reconstructed DLL-only lock exited 0, skipped both optional DLL checks, and landed the package. This is direct evidence that the repair branch can publish a lock that fails to preserve current DLL integrity coverage. See [get-framework-original.log](release-repair/get-framework-original.log), [get-framework-repaired.log](release-repair/get-framework-repaired.log), and [repair-build.log](release-repair/repair-build.log).

The tracked Markdown link scan checked 719 file links after excluding HTTP/mail links, anchors, directories, and historical `audit*` paths; it found 0 broken links. See [markdown-links.md](markdown-links.md).
