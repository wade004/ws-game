# 第十八轮审核修复跟进核实（2026-09-11）

基线：`d6fda65cd6b00ede5c5f6f606f724d2ab0f60603`（v1.18.0）。本文档核实 [AUDIT_REPORT.md](AUDIT_REPORT.md)
问题汇总表 7 项 P2（PRES-118-CAMERA/CORE-118-QUEST/CORE-118-CAST/PRES-118-SFX/PRES-118-VIEW/
TOOL-118-ABI/TOOL-118-LOCK）、`grid_snap` 通用能力决策、以及
[docs-project/doc-code-matrix.md](docs-project/doc-code-matrix.md)"文档更新清单"九项文档问题
（DOC-118-01～09）的实际修复结果。

修复由多个并行 agent 分工：本 agent（分支 `wy/tool18`，`toolchain/**`、`.github/workflows/**`、
`build.ps1`（若需）、各 README、`architecture/README.md`、根 `README.md`、`architecture/13`/`14`
的勘误、`editor/docs/**`、`architecture/落地计划/**`、`CHANGELOG.md` 仅本 agent 负责各项的条目）
负责 TOOL-118-ABI、TOOL-118-LOCK 两项 P2，以及九类文档更新中除 `grid_snap`
（DOC-118-04 里 grid_snap 通用实现/延期决策部分，由另一 agent 处理）外的全部行，另负责本轮证据
归档与本文档。PRES-118-CAMERA/PRES-118-SFX/PRES-118-VIEW 由负责 `presentation/**`/`adapters/**`
的并行 agent（`wx/pres18`）处理；CORE-118-QUEST/CORE-118-CAST 与任务/施法相关的文档勘误由负责
`core/**` 的并行 agent（`ww/core18`）处理；`grid_snap` 通用实现/延期决策由负责格子吸附的并行 agent
（`wz/gridsnap`）处理。本文档由本 agent 落笔核实自己负责的条目；其余行按任务要求写
`<待整合回填>`，留给整合 agent 按其余 agent 的实际提交回填。

提交哈希表：

| 提交 | 标题 | 范围 |
|---|---|---|
| `349d484dc7ff26b665eba73f551fa1cbfff7563b` | 第二十方深度审核修复（codex 第十八轮，工具链与文档）：ABI 属性/索引器/事件访问器补齐 static/virtual/abstract/final 编码，发布锁重建逻辑收进共用脚本并对 headless_dlls/validator_dlls 缺字段收紧版本阈值，九类文档勘误（ADR 计数、ATB/teleport 分类、编辑器文档版本同步、导入工具责任、武器风格状态拆分、战斗暴击叠加语义），归档与跟进核实 | TOOL-118-ABI（`toolchain/abi_surface/{SurfaceDumper,SurfaceCompare}.cs`、`toolchain/tests/test_abi_surface_compare.py`）+ TOOL-118-LOCK（新增 `toolchain/_lock_writeback.ps1`、`build.ps1`、`.github/workflows/release.yml`、`toolchain/get_framework.ps1`、`toolchain/tests/{test_get_framework_path_boundary,test_get_framework_with_samples,test_get_framework_lock_source_no_local_path,test_lock_writeback_repair_parity}.py`）+ DOC-118-01～03/07～09（`architecture/README.md`、根 `README.md`、`editor/README.md`、`editor/docs/编辑器产品文档.html`、`architecture/13_新游戏接入指南.md`、`architecture/14_资产规格书模板.md`、`architecture/06_规则层_属性技能战斗AI.md`、`core/rules/combat/README.md`、`core/rules/combat/tests/{CombatTestSupport,ResolverHitTableTests}.cs`）+ 归档（`architecture/落地计划/audit-d6fda65-20260911/`）+ `CHANGELOG.md`（`[Unreleased]` 追加本 agent 负责条目），本 agent（wy）负责范围合并为一个提交 |
| `003f73d29697d64dc1f12705c7d8a41ae8738a9f` / `82cbad139c1c312efca522740b4833b55f3d7267` / `6a9b1cf7a69294f461ac31d4d22401a480292436` | PRES-118-CAMERA（场景加载完成后镜头默认重新跟随玩家单位）/ PRES-118-SFX（三个生产装配入口统一逐帧驱动 Sfx.Update）/ PRES-118-VIEW（读档后重建留存 View 的插值快照）三个独立提交 | `wx/pres18` 分支，`--no-ff` 合入 main（合并提交 `f5fb6a3`），范围：`presentation/camera/**`、`presentation/assembly/**`、`presentation/vfx_sfx/README.md`、`presentation/view_binding/**`、`adapters/unity/.../Bootstrap/GameFoundationBootstrap.cs`、`.../Shell/FrameworkResidentHost.cs`、`games/_template/**`、`architecture/09`/`13` 勘误 |
| `907c15da3e8ea2b9e923a18484b9ffd68242830a` / `d40938c36e761fad8ac8356f7517f8efc776c90d` | CORE-118-QUEST（任务定义热重载删除/恢复导致运行态异常根治）/ CORE-118-CAST（施法者死亡/销毁延后派发时终结事件被静默丢弃根治）两个独立提交 | `ww/core18` 分支，`--no-ff` 合入 main（合并提交 `96db5a6`），范围：`core/gameplay/quest/**`、`core/rules/skill/core/CastPipeline.cs`、`core/rules/skill/README.md`、`architecture/06` 第 3.6 节勘误（DOC-118-05 部分） |
| `638d62ebbd925a3a7b15c4a98a2ad90b08211498` / `b436226bd6ec5f6f176419d1713bd091b5603616` / `aba64cda8756d75e84ce0b37e6eacf3e2db8e11c` | grid_snap 通用实现：契约与策略层（`IGridSnapPolicy`/`GridSnapShapeQuery`/`ShapeGeometry`/`TimeModelDefinition.GridSnapCellSize` + schema 登记）/ 运行时接线（离散步移动吸附 + `nearest_in_shape`/`all_in_shape`/`ISkillHost.FindUnits` 按格子中心采样）/ 文档勘误（04/13/`sim_loop/README.md`）三个独立提交 | `wz/gridsnap` 分支，`--no-ff` 合入 main（合并提交 `932c349`），范围：`core/foundation/common/**`、`core/foundation/engine_adapter/**`、`core/foundation/sim_loop/**`、`core/carriers/unit/**`、`core/rules/targeting/**`、`core/rules/skill/**`、`core/gameplay/assembly/GameplayAssembly.cs`、`toolchain/schema_audit_allowlist.json`（DOC-118-04 决策部分：选择"实现"而非"延期"） |

## 核实表

| 条目 | 判断 | 复现 | 修复位置 | 验收 | 判断记录 |
|---|---|---|---|---|---|
| PRES-118-CAMERA 进图后 CameraHost 不重新 Follow | **成立，已修复** | `presentation-review.md` PRES118-01：`CameraHost` 默认 `ResetFollowOnSceneLoadFinished=true`，`scene.load_finished` 到达时清空跟随目标，但三个生产装配入口都未在场景加载完成后重新 `Follow`，新游戏进图/切图/读档重进后镜头静止不再跟随。 | `presentation/camera/contracts/CameraHostOptions.cs`（新增 `FollowTargetResolverOnReset` 字段，新增三参构造函数重载，不改已发布双参构造函数签名）；`presentation/camera/core/CameraHost.cs`（`OnSceneLoadFinished` 重置后同一次处理内立即调用该解析函数重新指定跟随目标，不依赖调用方在 `SceneRouter` PostLoad 钩子与 `load_finished` 事件之间抢时序）；`presentation/assembly/PresentationAssembly.cs`（`AutoConfigureCameraFromFirstProfile` 打开、调用方未显式装配该解析函数时默认补"继续跟随同一玩家单位"的解析函数，三个生产入口全部自动获得）；`games/_template/Runtime/GameBootstrap.cs`（`BuildPresentationOptions` 把此前从未真正透传的 `GameOptions.CameraResetFollowOnSceneLoadFinished` 接入 `CameraHostOptions`）。 | Unity PlayMode 契约测试 `PRES118_CameraAndSfxMaintenanceTests`、`PRES118_TemplateContractTests` 覆盖三个生产装配入口；顺带根治测试基础设施缺口——`games/_template` 测试套件 `GlobalTemplateTestSetup` 补齐存档目录隔离（此前遗留 `game.sample.*`/裸 `slot.*` 前缀存档跨轮累积命中 `SaveSystemOptions.MaxSlots` 上限，导致新增验收用例间歇性失败），新增 `GlobalTemplateTestSetupTests` 钉住该隔离行为。`dotnet test Core.sln -c Release`：本次整合阶段全量重跑 6 工程 3282 用例全绿（含本条相关的 `Tests.PresentationCommon`）。 | 走查代码确认三处生产装配根（`GameFoundationBootstrap`/`FrameworkResidentHost`/`games/_template GameBootstrap`）均已通过 `AutoConfigureCameraFromFirstProfile` 的默认解析函数或模板显式接线获得"场景加载完成后重新跟随"行为，与提交描述一致；`CameraHostOptions` 新增字段走新增构造函数重载，未破坏已发布双参构造函数，ABI 兼容（随本次全量 `abi_probe.ps1` breaks=0 一并验证）。 |
| CORE-118-QUEST 热重载删除任务后恢复同 ID 异常 | **成立，已修复** | 接取任务并取得进度后 `Reload` 删除该定义，`GetActiveObjectives(unit)` 等单位级枚举/事件消费路径直接 `_definitions[questId]` 索引，抛 `KeyNotFoundException`；随后以增加目标数的新定义恢复同一 `questId`，`UpdateProgress` 对合法新索引抛 `IndexOutOfRangeException`——`MigrateProgressAfterReload` 原依赖 `Reload` 每次现取的"当前 `_definitions`"快照做新旧目标数组配对，中间只要插入过一次整体删除该 `questId` 的 `Reload`，快照就找不到旧形状。 | `core/gameplay/quest/core/QuestHost.cs`：`GetActiveObjectives` 与全部事件驱动进度处理方法改用新增 `TryGetDefinition`，定义缺失时该条进度本次跳过（不枚举、不推进、不抛异常），进度本身继续保留在 `_progress` 里；迁移锚点从 `Reload` 临时快照改为每条 `QuestRuntimeState` 自带的 `LastKnownObjectives`（由 `Accept`/`MigrateProgressAfterReload`/`ReplaceAllProgress` 维护），不再受中间隔了多少次删除/恢复 `Reload` 影响。 | 新增 `core/gameplay/quest/tests/CORE118_QuestHostDefinitionDeletionTests.cs`（9 例）：删除后单位级跟踪/多类事件消费不抛且进度保留、恢复为目标增/减/重排三种形状变化均不越界、跨多次无关 `Reload` 的恢复、恢复为同形状后完成态正确重算；提交描述记录这些用例在根治前代码上 6/9 失败，证明确实覆盖该缺陷。整合阶段全量重跑 `dotnet test Core.sln -c Release`（含 `Tests.Gameplay`）：0 failed。 | `core/gameplay/quest/README.md` 判断记录同步更新，纠正"调用者不该查询被删 id"这一不成立的旧前提；公开签名只新增 `TryGetDefinition`，不删改既有方法。 |
| CORE-118-CAST 施法者死亡后终结事件延后派发 | **成立，已修复** | 施法者死亡/销毁已生效（`IUnitAccess.IsAlive`/`Exists` 已反映新状态），但对应的 `unit.died`/`entity.destroyed` 事件要等本次 `SkillHost.Update` 结束后的批处理派发才真正送达时，`CastPipeline.AdvanceOne`/`FinishCast` 的防御性重验分支先摘除 `CastState`、不发任何事件；等死亡事件真正派发到 `Interrupt` 时状态已被摘除，无从补发——当前读条与排队请求都收不到 `Interrupted`/`QueueCleared`（`interrupted=0,queueFailed=0`；死亡事件先派发时正常为 `1,1`）。旧判断记录"理论上不会在正常事件顺序下触发"的前提不成立。 | `core/rules/skill/core/CastPipeline.cs`：新增私有方法 `TerminateCast(casterId, state, interrupterId, lockSchool, lockDuration)`，把 `Interrupt`（正常打断/自我打断路径）与 `AdvanceOne`/`FinishCast` 两处防御分支全部收敛到同一条终结路径——三处调用点各自先用 `TryGetValue+Remove` 唯一摘除一次 `CastState` 再调用 `TerminateCast`，`Dictionary` 同一个 key 只能被其中一处先摘到，天然幂等，不会对同一次施法/排队请求重复发送终结事件。 | 新增 `core/rules/skill/tests/CORE118_CastPipelineDeathTerminationTests.cs`（7 例）：死亡事件先/后派发两种时序均得到 `Interrupted=1,QueueCleared=1` 且各自携带正确 `CastInstanceId`、无重复事件、实体销毁、`FinishCast` 兜底、队列替换与正常打断回归；提交描述记录 4/7 在根治前代码上失败，证明确实覆盖该缺陷。整合阶段全量重跑 `dotnet test Core.sln -c Release`（含 `Tests.Rules`）：0 failed。 | `architecture/06` 第 3.6 节补充"施法终结事件保证"勘误段（本次整合合并时与 `wy/tool18` 的 DOC-118-09 同日勘误行并列保留，见提交 `b84af22`）；`core/rules/skill/README.md` 判断记录同步更新。 |
| PRES-118-SFX 重复缺资源播放请求未及时清理 | **成立，已修复** | `presentation-review.md` PRES118-02：`GameFoundationBootstrap.cs`、`games/_template/Runtime/GameBootstrap.cs` 两个生产入口的 `OnFrameTick` 此前只手工罗列了 `Feedback.Update`/`Vfx.Update` 两步，漏抄了 `Sfx.Update`（对照 `FrameworkResidentHost.OnFrameTick` 有调用）——连续两次播放同一个缺失音效资源时，第二次请求没有任何未来回调可等，只能靠 `Sfx.Update` 内部的超时扫描清理，没有生产入口驱动它时永久卡在 pending。 | `presentation/assembly/PresentationAssembly.cs`：新增 `UpdatePlaybackMaintenance(dt, stepRunner)` 方法，把 `Feedback`/`Vfx`/`Sfx` 三步逐帧维护收敛为一处；可选 `stepRunner` 供 `FrameworkResidentHost`/`GameFoundationBootstrap` 传入各自的 `RunPresentationStep` 保留"拍板 12"单步异常隔离粒度，`games/_template GameBootstrap` 无此机制、不传时直接调用，行为与改动前一致；三个生产装配入口统一改为调用该方法，不再各自罗列播放器清单。 | 新增 `PresentationAssemblyTests` 三条 .NET 单测覆盖驱动效果与异常隔离；新增 Unity PlayMode 契约测试 `PRES118_CameraAndSfxMaintenanceTests`（`FrameworkResidentHost`/`GameFoundationBootstrap` 两入口）与 `PRES118_TemplateContractTests`（模板入口）验证真实 Unity 运行时下 pending 音效请求会在超时后自动清零；两处新增测试专用数据叠加开关（`ForceSfxMissingResourceOverlayForTest`）默认关闭，对既有场景/冒烟流程零行为影响。整合阶段全量重跑 `dotnet test Core.sln -c Release`（含 `Tests.PresentationCommon`）：0 failed。 | 三个生产装配入口统一收敛到同一方法，避免未来再次"抄漏一项"；`architecture/09`、`presentation/assembly`、`presentation/vfx_sfx` README 补勘误说明。 |
| PRES-118-VIEW 同图读档后既有 View 插值快照未刷新 | **成立，已修复** | `presentation-review.md` PRES118-03：同图直接 `SaveSystem.Load`（不经 `SceneRouter` 卸载重建场景）时，继续存活的既有 View 的插值快照（`_prevPositions`/`_currPositions`）不会被刷新——`OnTickFinished` 只在下一次 `sim.tick_finished` 才重新采样，离散模拟等待输入/暂停时不会立即有下一个 tick（ADR-0013 §4），`GetInterpolatedPosition` 因此继续插值出读档前的旧坐标，直到某次无关的下一次 tick 才"顺带"刷新，画面与刚恢复的逻辑位置不同步。 | `presentation/view_binding/core/ViewBinder.cs`：`OnSaveLoaded` 补第四段处理——对本次触发前后身份都未变的每个存活 View，直接用 `ISimSnapshot.GetPosition` 的恢复后位置重设 `prev=curr`（与 `OnEntityCreated`"避免第一帧跳变"同一手法），不发起任何 `IWorldSim` tick、不改变任何逻辑结算，只重建表现层自己的插值起点；`existingIds` 的计算不再依赖是否装配了 `equipmentVisualSource`（插值快照重建与装备外观对账是两件独立的事）。 | 新增 `presentation/view_binding/tests/PRES118_ViewSaveLoadPositionSnapshotTests.cs` 三条用例：无额外 `world.Tick` 的同图读档、重复读档、显式补一次 tick 的控制组，验证 `alpha` 取 0/0.5/1 任意值都落在恢复点、View 身份保持不变；装备外观同图读档对账（P2-08）已有现成回归 `EquipmentVisualSaveLoadResetTests` 覆盖，提交记录确认其未改动该路径逻辑、仍随本次改动全绿。整合阶段全量重跑 `dotnet test Core.sln -c Release`（含 `Tests.PresentationCommon`）：0 failed。 | `presentation/view_binding` README 补勘误说明；不影响任何逻辑结算，只重建表现层插值起点。 |
| TOOL-118-ABI 属性调用形态（实例↔静态等）ABI 漏检 | **成立，已修复** | 端到端负例（真实 `dotnet build`）：baseline 库 `public int Value { get; set; }`（实例属性）、current 库同名同类型改 `public static int Value { get; set; }`；一份只针对 baseline 编译一次、不重新编译的旧 consumer 读取该属性——换上 current DLL 后运行期实测 `MissingMethodException`（复现 `docs-project/abi-property/consumer-new.log` 现场证据）；修复前 `abi_surface dump`+`compare` 对同一对 DLL 判 `breaks=0`（`docs-project/abi-property/surface-report.txt`）。 | `toolchain/abi_surface/SurfaceDumper.cs`（属性/索引器分支新增 `static`/`abstract`/`virtual`/`sealed-override` 编码，get/set 任一访问器 `IsStatic` 判定；事件分支同样补齐三态编码，此前只记 `static`）；`toolchain/abi_surface/SurfaceCompare.cs`（`SplitPropertyIdentity` 的"身份"从只含 `abstract` 扩到含全部非可见性 token，避免"可见性放宽 + static 化"复合变化被误判为纯放宽而漏判） | `toolchain/tests/test_abi_surface_compare.py` 新增 8 个用例：实例↔静态双向、virtual→sealed-override、abstract→非 abstract、静态属性无变化正例、可见性放宽与 static 化复合场景专项回归、事件 abstract 翻转、端到端真实 `dotnet build` 负例（`test_property_instance_to_static_negative_oracle_end_to_end_via_real_dll`，复现 `MissingMethodException` 且 `compare` 判 `RESULT=BREAKING`）；单文件 `pytest -q`：41 passed、1 skipped（`test_real_baseline_end_to_end_via_abi_probe` 本机无 1.13.0 之外基线时的既有 skip 语义未变）；用新版工具对 `dist/ws-game-1.12.0.zip`（完整 `abi_probe.ps1` 探针）、`dist/ws-game-1.13.0.zip`（`abi_surface` 直接 dump+compare，既有已知边界见 `toolchain/README.md`）两份历史基线重跑当前工作树六个 DLL：均 `breaks=0`（additions 分别 275、186），未发现历史真实破坏。全仓 `python -m pytest toolchain/tests -q`：185 passed、4 skipped。 | 属性 static 化前 breaks=0（漏判）、修复后同一对 DLL breaks>0（判破坏），负例前后对比与真实 consumer 运行结果一致；新增维度：属性/索引器 `static`/`abstract`/`virtual`/`sealed-override`，事件 `abstract`/`virtual`/`sealed-override`（此前事件只记 `static`）。索引器复用属性分支同一套编码，无需单独改动。 |
| TOOL-118-LOCK 缺附件修复分支重建的 lock 丢失 headless_dlls/validator_dlls | **成立，已修复** | `toolchain/tests/test_lock_writeback_repair_parity.py::test_tampered_headless_dll_blocks_both_normal_and_repair_style_locks`：构造 fixture zip 后篡改 `Adapters.Stub.dll`，分别用"正常路径风格"锁（`New-WsGameLockObject`，哈希取自篡改前原始内容）与"修复分支风格"锁（`New-WsGameLockObjectFromZip`，同样取自篡改前原始 zip）针对篡改后的 zip 跑 `get_framework.ps1 -FromLocalDist`——修复前，修复分支风格锁因缺 `headless_dlls`/`validator_dlls` 字段触发向后兼容跳过分支，篡改的 DLL 未被检出、内容落地成功；修复后两者同等阻断（exit≠0，未落地任何内容），复现 `validation/release-repair/get-framework-repaired.log` 记录的"根治前跳过校验"现场。 | 新增 `toolchain/_lock_writeback.ps1`（`New-WsGameLockObject`/`Write-WsGameLockFile`/`New-WsGameLockObjectFromZip`/`Get-WsGameZipEntrySha256`），`build.ps1` 与 `.github/workflows/release.yml`"缺附件修复"步骤均改为 dot-source 调用同一份实现（后者不再手写只覆盖六个核心 DLL 的抽取循环，`New-WsGameLockObjectFromZip` 内部同时抽取 `Adapters.Stub.dll`/`Validator.dll`）；`toolchain/get_framework.ps1` 新增版本阈值判定（`$HeadlessValidatorDllsRequiredSinceVersion = 1.15.0`）：锁文件 `version >= 1.15.0` 时 `headless_dlls`/`validator_dlls` 任一字段缺失直接判定锁文件损坏、拒绝落地；`version < 1.15.0` 仍走既有"旧版本跳过该项校验"兼容分支。 | `toolchain/tests/test_lock_writeback_repair_parity.py` 新增 3 个用例：真实 `dist/ws-game-1.18.0.zip` 通过 `New-WsGameLockObjectFromZip` 重新生成的锁文件与仓库已发布 `dist/ws-game-1.18.0.lock` 逐字段（`version`/`git_commit`/`dlls`/`headless_dlls`/`validator_dlls`/`samples`）核对一致（本机有该 zip/lock 时执行，本 agent 工作树内手动验证过一次，逐字段相同；无本机 dist 产物的环境按既有约定 skip，不算测试失败）、篡改副本正常/修复分支风格锁同等阻断、`version < 1.15.0` 缺字段仍走兼容跳过（exit 0，不误伤真正的旧版本锁文件）。同批修复现有三个 `toolchain/tests/test_get_framework_*.py` 文件的 fixture（`_write_lock` 默认补上 `headless_dlls`/`validator_dlls`，`_build_fixture_zip` 补对应 zip 条目）——这几个文件原本的 fixture 版本号（如 `9.9.9`/`8.8.x`）均 >= 1.15.0，新的硬性校验会让它们缺字段而失败，补齐后不放宽既有断言。`python -m pytest toolchain/tests -q`：185 passed、4 skipped；`.github/workflows/release.yml` 与 YAML 语法校验通过（`python -c "import yaml; yaml.safe_load(...)"`），修改的 `run:` 正文逐行核对为纯 ASCII。 | 共用脚本落点：`toolchain/_lock_writeback.ps1`（新文件，UTF-8 BOM、LF）。lock 字段对照：`dlls`/`headless_dlls`/`validator_dlls`/`samples` 四类哈希映射字段顺序与 `build.ps1` 原实现完全一致（`ConvertTo-Json -Depth 5` + 无 BOM UTF8 写出）；`New-WsGameLockObject` 的三个 DLL 哈希映射参数均为 PowerShell `Mandatory` 参数，结构上杜绝未来再有调用点漏传其中一个。版本阈值 1.15.0 的选取依据：`headless_dlls` 字段自 1.13.0 起随 ADR-0018 决策 3 引入，`validator_dlls` 字段自 1.15.0 起随消费方反馈 E1 引入（`CHANGELOG.md` 对应版本小节可查），取两者中更晚的 1.15.0 作统一阈值，简化判断口径（不逐字段分别维护阈值），符合任务书原话。 |
| grid_snap 是否实现通用消费或走延期决策 | **走实现路线，已落地** | 外部审核 audit-d6fda65-20260911 第 2 节：ADR-0013 决策 6、04 第 3.1 节声明的 `grid_snap`/`cell_size` 与"范围形状按格子中心采样"只有字段登记、无运行时消费，`sim_loop/README.md` 已承认该差距、能力总索引漏列。 | 契约与策略层：`core/foundation/common/contracts/IGridSnapPolicy.cs` + 默认实现 `GridSnapPolicy`（连续坐标换算成所属格子中心点，可替换策略）；`core/foundation/engine_adapter/contracts/ShapeGeometry.cs`（`Contains` 几何判定，从 `core/gameplay/encounter` 私有实现提升为 L1 可复用）、`Shape.Expand`（按外扩边距放大形状，供广相位查询）、`GridSnapShapeQuery.QueryShapeAtCellCenters`（两阶段查询：外扩广相位 + 候选格子中心点精确重判）；`core/foundation/sim_loop/contracts/TimeModelDefinition.GridSnapCellSize` + `TimeModelSchema.Table` 按 ADR-0019 登记 `grid_snap` 子结构（`cell_size: Number`，`WithRange(>0)`），`toolchain/schema_audit_allowlist.json` 移除对应豁免条目。运行时接线：`core/carriers/unit/core/MovementTickHandler` 离散步位移推进写回 `SetPosition` 前吸附到格子中心（方向类与目标类两条位移路径共用）；`core/rules/targeting/core/BuiltinTargetStrategies` 的 `nearest_in_shape`/`all_in_shape`、`core/rules/skill/core/SkillHost.FindUnits` 均改用 `GridSnapShapeQuery.QueryShapeAtCellCenters`；`core/gameplay/assembly/GameplayAssembly` 按 `CombatModel.GridSnapCellSize` 统一回填 `Carriers.MovementOptions`/两个 L2 Options（"构造后回填"手法，与 `resolvedSkillOptions` 一致）。连续模式或未声明 `grid_snap` 时行为逐字节不变。 | 新增/修改测试：`GridSnapPolicyTests`、`GridSnapShapeQueryTests`、`TimeModelGridSnapTests`、`MovementTickHandlerTests`（离散步方向/目标两类位移吸附、连续模式与未声明 `grid_snap` 时不吸附）、`TargetHostTests`/`ISkillHost_FindUnitsTests`（"原始坐标在范围内、格子中心在范围外"与反向两组边界用例，各自对照未启用格子吸附时的结果）、`GameplayAssemblyDiscreteWiringTests`（端到端验证数据声明经真实装配链路回填到 `Carriers.MovementOptions`）。整合阶段全量重跑 `dotnet test Core.sln -c Release`（6 工程 3282 用例）、`python toolchain/validate_data.py --strict`、`validator --schema-audit`：均 0 error。 | 决策：选择"实现"而非"延期"——此前 `sim_loop/README.md` 第 7 条"ADR-0019 F1c 判断记录（`grid_snap` 不登记子结构）"改写为"格子吸附 `grid_snap` 已落地"；04/13 变更记录同步补勘误行（04：说明两处运行时消费与移除的 allowlist 豁免；13：说明第 4 节口味清单"网格吸附"一行的"开"现在是真实可运行的选项，取值本身不变）；`架构落地计划/落地方案与分阶段计划.md` 能力索引"未实现"栏删除格子吸附条目、"已实现"栏补充落点说明，随本轮整合阶段独立提交（见本文档"提交哈希表"整合阶段回填提交行）。 |
| DOC-118-01 ADR 总清单仍写 23 条 | **成立，已修复** | `architecture/README.md` 文件清单段"共 23 篇"、根 `README.md` 两处"23 条 ADR"；实际 `architecture/adr/` 下已有 `0001`～`0024` 共 24 篇（`ls architecture/adr/*.md \| wc -l` = 24）。 | `architecture/README.md`"变更记录"表新增一行、文件清单补 `adr/0024` 一行并把"共 23 篇"改"共 24 篇"；根 `README.md` 两处"23 条 ADR"改"24 条 ADR"。 | 走查 `architecture/adr/` 目录文件集合与改写后的两处文档计数一致；`adr/README.md` 本身索引此前已正确，未重复改动。 | 只改计数，不改任何 ADR 内容本身。 |
| DOC-118-02 根 README ATB/teleport 分类过期 | **成立，已修复** | 根 `README.md` 落地状态摘要段把 ATB 归入"明确非目标"示例、把 teleport 元素结构笼统归入"未实现"；实际（`architecture/落地计划/落地方案与分阶段计划.md` 第 1366/1371 行，第十七轮已修复）ATB 分类"延期/预留"、teleport 元素结构"已登记"，未提供的只是目标引用完整性检查。 | 根 `README.md` 该段改写：ATB 从"明确非目标"示例移出，单列"延期/预留"分类示例（`TimeModelSchema` 已登记合法预留枚举值、`TurnScheduler.Configure` 遇到时抛 `NotSupportedException`）；teleport 一行改为"`teleport_points` 引用目标完整性校验"（元素结构 `{id?, position?}` 已登记，未提供的是跨表引用完整性检查）。 | 走查改写后措辞与 `落地方案与分阶段计划.md` 第 1366/1371 行（第十七轮已修复的权威表述）、`architecture/adr/0013` 原文一致。 | 落地计划文档本身两行在第十七轮（DOC-162-03/04）已修复；本轮遗留问题仅在根 README 的摘要段未同步，本次只改 README，不改落地计划文档。 |
| DOC-118-03 编辑器文档 md/html/README 版本不同步 | **成立，已修复** | `editor/docs/编辑器产品文档.md` v2.5，同名 `.html` 版本头仍写 v2.2（变更记录表缺 v2.3/v2.4/v2.5 三行，第 4.1 节契约面清单表未反映 `Range`/`field_meta`（`group`/`unit`/`reference_table`/`reference_domain`/`free_ids`/`map_key`/`map_value`）/`TryGetAll`/`TryQuery`/`OverrideDiagnostic` 新字段/`ExprNode`/`ExprIssue` 区间/`KnownKeys`/`KnownGroups` 等新契约），`editor/README.md` 称二者同步至 v2.1。仓库内无 md→html 自动转换脚本（`grep` 未命中任何生成脚本）。 | 无生成脚本，按 md 版内容手工同步 HTML：版本头 v2.2→v2.5、日期同步、变更记录表补 v2.3/v2.4/v2.5 三行（内容与 md 版对应行一致，转成 HTML 表格/`<code>`/`<a>` 标记）、第 4.1 节契约面清单表 `TableSchema`/`FieldSchema` 行、`DataRegistry` 行、`Expr 解析器` 行三处按 md 版同步补全新增字段说明；HTML 头部新增一句注明"由 md 手工同步生成、无自动转换脚本"及同步范围（变更记录表 + 第 4.1 节，其余章节未随 v2.3～v2.5 改动、保持原样）。`editor/README.md` 两处版本号改 v2.5，并补充说明 HTML 同步方式。 | Python `html.parser` 对改写后的完整 HTML 文件解析无错误（结构未破坏）；人工核对三份文档版本号（v2.5/v2.5/v2.5）与第 4.1 节新增字段描述文字逐句比对一致。 | 未逐节重排 HTML 全文（该文件近 3600 行，第 5～11 章内容 v2.1 起未变化，全量重排超出本条目"关键契约面章节同步"的任务边界）——按任务书"没有就据 md 手工同步关键契约面章节并注明生成方式"执行，属有意为之的范围收窄，已在文件内与 README 中注明。 |
| DOC-118-04 grid_snap 通用能力索引缺失登记 | **成立，已修复（随 grid_snap 实现路线同批完成）** | 见上方 grid_snap 行"复现"列；`sim_loop/README.md` 第 7 条判断记录、能力总索引均未反映 `grid_snap` 已具备真实运行时消费。 | `core/foundation/sim_loop/README.md` 第 7 条从"ADR-0019 F1c 判断记录（`grid_snap` 不登记子结构）"改写为"格子吸附 `grid_snap` 已落地"，列出子结构登记与两处运行时消费的具体落点；`architecture/04_数据与内容管线.md`、`architecture/13_新游戏接入指南.md` 变更记录各补一行；`架构落地计划/落地方案与分阶段计划.md` 能力索引条目回填随本轮整合阶段完成。 | 与 grid_snap 行验收共用；整合阶段人工核对 04/13 变更记录行与 `sim_loop/README.md` 改写内容跟 `wz/gridsnap` 三个提交实际改动逐一对应。 | `wz/gridsnap` 明确记录"架构落地计划/落地方案与分阶段计划.md 的能力索引条目改动留给整合 agent，文案已写入交付报告"（见 `aba64cd` 提交说明），本次由整合 agent 落笔完成该项。 |
| DOC-118-05 Quest 热重载注释/施法终结假设过期 | **成立，已修复（随 CORE-118-QUEST/CAST 同批完成）** | `core/gameplay/quest/README.md`、`core/rules/skill/README.md` 判断记录分别写有"调用者不该查询被删 id"（与 CORE-118-QUEST 缺陷矛盾）、"理论上不会在正常事件顺序下触发"（与 CORE-118-CAST 缺陷矛盾）两条已被复现推翻的过期假设。 | `core/gameplay/quest/README.md` 判断记录同步更新，纠正"调用者不该查询被删 id"这一不成立的旧前提；`core/rules/skill/README.md` 判断记录同步更新，纠正"理论上不会在正常事件顺序下触发"这一不成立的旧前提；`architecture/06_规则层_属性技能战斗AI.md` 第 3.6 节补充"施法终结事件保证"勘误段。 | 与 CORE-118-QUEST/CORE-118-CAST 两行验收共用。 | 不是独立改动，是两项 P2 代码修复的配套文档同步，随 `ww/core18` 同一批提交一起完成，整合阶段合入 06 号文档时与 `wy/tool18` 的 DOC-118-09 同日勘误行并列保留（提交 `b84af22`），未互相覆盖。 |
| DOC-118-06 ABI 工具描述/release.yml 注释随 A/B 更新 | **成立，已修复**（随 TOOL-118-ABI/TOOL-118-LOCK 同批完成） | 见上方 TOOL-118-ABI/TOOL-118-LOCK 两行"复现"列。 | `toolchain/README.md`（新增"`abi_surface` dump/compare 覆盖范围补齐（TOOL-118-ABI 根治）"一节，含维度表、`SplitPropertyIdentity` 判断记录、两份历史基线重跑结果）；`.github/workflows/release.yml`"缺附件修复"步骤 YAML 注释同步改写为说明"从这一处共用实现重建 lock，不会再漏 headless_dlls/validator_dlls"。 | 与 TOOL-118-ABI/TOOL-118-LOCK 两行验收共用。 | 不是独立改动，是两项 P2 代码修复的配套文档同步，随同一批提交一起完成。 |
| DOC-118-07 14 号文档导入工具责任写反 | **成立，已修复** | `architecture/14_资产规格书模板.md`"基础架构提供 / 游戏层提供"表"导入工具的输入输出契约与校验项定义"一行，游戏层列写"导入工具的具体实现、具体校验阈值"——把通用工具实现的责任划给游戏层；实际按 [ADR-0014](../../adr/0014-资产契约导入工具与UI套件是框架交付物.md) 与当前 `toolchain/` 落地状态，通用导入工具本身是框架交付物。 | 该行改写为"是（通用资产导入工具本身是基础架构交付物，覆盖各类资产的标准导入/校验流程）"／"具体资源素材、具体参数取值、具体校验阈值、超出通用流程覆盖范围的游戏专属扩展"，未使用具体技术/工具名（14 属 `architecture/00～14` 范围，`check.ps1` 第 729 行门禁注释确认覆盖到 14，措辞保持中性）；补一行变更记录。 | `禁用词扫描：architecture 正文不出现引擎/语言/框架/工具名` 门禁步骤范围覆盖 00～14 + `adr/`（`check.ps1:729`），本行改写内容未出现任何具体技术名，人工复核通过。 | 只改责任归属表述，不改本文档其余章节结论；ADR-0014 本身未改动。 |
| DOC-118-08 13 号文档武器风格状态混列 | **成立，已修复** | `architecture/13_新游戏接入指南.md` 第 6 步"命中帧同步（09 第 4.3 节 `anim_keyframe_driven` 策略）与武器风格两项都是可选口味配置项，默认关闭/未接线"一句，把三种不同状态笼统合并；实际走查三处生产装配根（`adapters/unity/.../Bootstrap/GameFoundationBootstrap.cs`、`.../Shell/FrameworkResidentHost.cs`、`games/_template/Runtime/GameBootstrap.cs`）：`EquipmentWeaponStyleSource` 均无条件默认构造（数据来源接线已默认完成）；`GameOptions.HitFrameSyncEnabled` 默认 `false`（命中帧开关确实默认关闭）；`WeaponStyleResolver`（`presentation/vfx_sfx/core/`）三处装配根均未调用（Swing/Impact 特效消费"已实现未默认调用"）。 | 第 6 步该句拆成三条独立描述：命中帧开关（默认关闭）、武器风格数据来源接线（默认已构造，非"未接线"）、Swing/Impact 特效消费（已实现未默认调用）；补一行变更记录。 | 走查三处生产装配根源码确认 `EquipmentWeaponStyleSource` 构造调用无条件存在、`HitFrameSyncEnabled` 默认值、`WeaponStyleResolver` 类型存在但三处装配根均无调用点，与改写后的三段描述逐一对应。 | 只改文档措辞，不改任何生产装配根代码；三处状态的源码事实在本轮审核前就已如此，是文档滞后未拆分记录。 |
| DOC-118-09 combat README 暴击/偏斜互斥说明与实现不符 | **成立，已修复** | `core/rules/combat/README.md` 判断记录 4 称"偏斜/格挡命中时不再参与暴击判定（`isCrit` 恒 false）"；实际 `core/rules/combat/core/Resolver.cs`：`DetermineHit` 内 `isCrit` 由独立掷骰决定（`context.CanCrit && table.Crit.Enabled` 分支，不受 `special` 是否已是 glancing_blow/block 影响），`Resolve` 方法体内先应用偏斜系数/格挡固定减免（步骤 2）、再应用暴击倍率（步骤 3），两者依次相乘、可以同时生效；扁平 `HitResult` 标签只反映 `DetermineHit` 优先序（报告哪个分支名），不代表伤害计算互斥。 | `core/rules/combat/README.md` 判断记录 4 改写为"偏斜/格挡与暴击默认可叠加"，说明扁平标签与实际乘算互不冲突；`architecture/06_规则层_属性技能战斗AI.md` 第 4.1 节结算管线后补一段勘误，写明"是否互斥由消费方策略决定，默认可叠加"，不改变固定步骤顺序本身。**不改算法**（设计层决定"以实现为准"：可叠加是通用策略，互斥属游戏口味配置，见任务书）。 | `core/rules/combat/tests/CombatTestSupport.cs` 新增两条 hit table fixture（`glancing_and_crit_forced`/`block_and_crit_forced`）；`core/rules/combat/tests/ResolverHitTableTests.cs` 新增两条测试 `Resolve_GlancingAndCritBothForced_StacksGlancingPercentAndCritMultiplier`（`FinalAmount=100×0.4×3.0=120`，扁平标签仍是 `GlancingBlow`，`CombatDamageDealtEvent.IsCrit=true`）、`Resolve_BlockAndCritBothForced_StacksFlatBlockReductionAndCritMultiplier`（`FinalAmount=(100-30)×2.0=140`，扁平标签 `Block`，`IsCrit=true`）钉住该行为。`dotnet test core/rules/tests/Tests.Rules.csproj -c Release`：487 passed（含新增 2 条），0 failed。 | 06 号文档本身未规定偏斜/格挡与暴击必须互斥，本轮不是"改算法迁就文档"，而是"文档描述改为与既有算法一致"；是否需要互斥留给消费方战斗结算策略/口味配置决定，不在本轮改动范围内实现。 |

## 本 agent 负责范围全量验收结果（2026-09-11）

1. `python -m pytest toolchain/tests -q`：185 passed、4 skipped（含本轮新增 TOOL-118-ABI 8 个用例、
   TOOL-118-LOCK 3 个用例，及三个既有 `test_get_framework_*.py` 文件 fixture 补齐后仍全绿）。
2. `dotnet test core/rules/tests/Tests.Rules.csproj -c Release --artifacts-path <scratchpad>/build_wy`：
   487 passed、0 failed（含 combat 新增 2 个用例）。
3. `dotnet build Core.sln -c Release --artifacts-path <scratchpad>/build_wy`：0 警告、0 错误。
4. 1.12.0 基线重跑（`toolchain/abi_probe.ps1 -BaselineVersion 1.12.0 -BaselineZip
   D:\workespace\ws-game\dist\ws-game-1.12.0.zip -ArtifactsPath <scratchpad>/build_wy`）：exit 0，
   `abi_surface compare breaks=0 allowed=0 additions=275 RESULT=OK`。
5. 1.13.0 基线重跑（直接对提取自 `dist/ws-game-1.13.0.zip` 的六个基线 DLL 与当前工作树六个 DLL
   做 `abi_surface` dump+compare）：`breaks=0 allowed=0 additions=186 RESULT=OK`。
6. `dist/ws-game-1.18.0.zip` 通过修复后 `New-WsGameLockObjectFromZip` 重新生成的锁文件与仓库已
   发布 `dist/ws-game-1.18.0.lock` 逐字段核对一致（手动验证，本 agent 工作树无本机 dist 产物，用
   主检出 `D:\workespace\ws-game\dist\` 下的正式产物核对，不修改该产物）。
7. `.github/workflows/release.yml` YAML 语法校验通过（`python -c "import yaml;
   yaml.safe_load(open(...))"`），修改的 `run:` 正文逐行核对为纯 ASCII。
8. 归档 `architecture/落地计划/audit-d6fda65-20260911/`：对本目录做不区分大小写的具体游戏代号
   禁用词全文扫描（与 `check.ps1` 全量步骤同一扫描口径），未发现任何命中；
   全部文本文件核对本就是 LF 行尾（未发现需要转换的 CRLF）、UTF-8 编码（未发现 UTF-16 BOM）；
   已排除 `evidence.zip`、全部 `bin`/`obj` 目录、`*.dll`/`*.exe`/`*.pdb`/`*.deps.json`/
   `*.runtimeconfig.json`、单文件超 2MB 的内容，以及与仓库/`dist`/Unity 工程内容逐字节重复的
   源码或落地目录镜像（详见归档 `README.md`"原件未归档"表）。

## validation.md 首次 pytest 失败根因排查

[validation/validation.md](validation/validation.md) 记录：`check.ps1 -SkipUnity` 首次运行的
`python -m pytest toolchain/tests -q` 步骤失败（仅 2.7 秒、输出只有两个 `.` 后即报"失败"，未捕获到
任何异常/回溯文本），同一份记录里独立重跑（`validation/raw/pytest-toolchain-rerun.log`）与本 agent
自己的多次重跑（含一次完整模拟 `check.ps1` 调用方式：`$env:PYTHONUTF8="1"; Push-Location <repo>;
python -m pytest toolchain/tests -q`）均是 186/185 passed（视本轮新增用例数而定）、0 failed，全绿。

排查结论：**未能复现，判断为环境瞬时问题，不是我们代码/测试本身的缺陷**——理由：

1. 失败仅耗时 2.7 秒，而完整套件正常需要 30～75 秒；结合只打印两个 `.` 就中断、且 `check.ps1`
   的 transcript 里完全没有捕获到 pytest 通常会打印的失败摘要（`FAILED ...`/回溯），更像是 pytest
   子进程本身被外部因素（例如实时防病毒扫描对刚创建的临时目录/刚启动的 `powershell.exe` 子进程
   的拦截、系统资源瞬时争用）异常中断，而不是某条断言真的失败——真正的断言失败会有明确的
   `FAILED`/`AssertionError` 文本，这次完全没有。
2. 同一份 `validation.md` 记录里，独立重跑立即就是全绿（186 passed，4 skipped，见
   `validation/raw/pytest-toolchain-rerun.log`），审核过程本身已经区分记录了"首次失败"与"重跑通过"
   两个独立事实，未把首次失败掩盖成"整体 PASS"。
3. 本 agent 在自己的工作树内按 `check.ps1` 的确切调用方式（含 `$env:PYTHONUTF8="1"`、
   `Push-Location` 到仓库根）额外重跑一次，同样全绿，进一步排除是本轮改动引入的新回归。
4. 前几轮审计已经处理过一类相关但不同的问题（"第九轮审计工具链条目"：本机 GBK 控制台代码页下
   `subprocess` 输出解码），`check.ps1`/相关测试已加 `PYTHONUTF8=1`/显式 `encoding="utf-8"` 双保险
   （见 `check.ps1` 第 703-718 行判断记录）——本次现象（进程提前中断、无回溯文本）与该类编码问题
   的表现（`UnicodeDecodeError`/乱码）不同，不是同一根因的复发。

**处理方式**：不 skip、不删测试、不新增兜底重试逻辑（没有可复现的具体故障点可修）；如实记录在
本文档，供后续如再次出现时比对现象是否一致（若再现且能捕获到具体异常文本，应作为新证据另开条目
根治）。

## 异常与判断记录

- 执行过程中一度误将 `toolchain/abi_surface/SurfaceDumper.cs` 的编辑落到主检出
  `D:\workespace\ws-game`（而非本 agent 工作树 `wy/tool18`）——命令行工具默认工作目录复位导致；
  发现后立即 `git -C D:\workespace\ws-game checkout -- toolchain/abi_surface/SurfaceDumper.cs`
  取回主检出该文件在基线提交下的原始内容，确认 `git -C D:\workespace\ws-game status --short` 只
  剩其余并行 agent（`wx/pres18`）正在修改的 `presentation`/`adapters`/`games` 路径（未触碰、未
  还原，那是其他 agent 的正常工作产物）后才继续；本轮最终提交只来自工作树分支 `wy/tool18`。此后
  全部文件编辑改用绝对路径直接定位到工作树内文件，Bash/PowerShell 命令逐条显式 `cd`/`-C` 到工作树
  路径，未再复发。
- TOOL-118-ABI 的属性/事件新增编码维度用新版工具重跑 1.12.0/1.13.0 两份历史基线均 `breaks=0`，
  未发现任何历史真实破坏——本轮新增覆盖项（属性/索引器/事件的 static/abstract/virtual/
  sealed-override）纯属"门禁能力补强"，不代表 1.18.0 当前六个核心 DLL 曾经发生过对应类型的未声明
  破坏。
- TOOL-118-LOCK 的复现场景（正常/修复分支风格锁针对篡改副本同等阻断）是本 agent 在自己工作树内
  用 fixture zip 构造的独立回归测试，不依赖 `validation/release-repair/` 下的大体积 zip 副本（已
  按 2MB 规则排除出归档），复现方法与结论详见上方核实表 TOOL-118-LOCK 行与
  `toolchain/tests/test_lock_writeback_repair_parity.py` 源码。
- DOC-118-04（`grid_snap`）、DOC-118-05（Quest/施法相关文档勘误）两行按任务边界不在本 agent
  改动范围内，未读取/未推测其余并行 agent 工作树内容，原样写"待整合回填"。
