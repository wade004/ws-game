# 文档代码一致性审计（2026-09-05）

> 本文件允许出现具体技术名（属于"落地计划"目录，见 CLAUDE.md 硬性规则），记录本次审计任务
> （框架收官前的文档/数据/代码一致性核查）逐条对照结果："已修"表示已在对应架构文档按
> `12_扩展与变更流程.md` §5 做了细节勘误（版本号不变，变更记录加一行"勘误："）；"无需修"表示
> 核对后文档与实现一致，未改动；"代码待补"表示文档已正确描述目标形态，但实现尚未跟上，本次
> 不改代码，只如实记录留待后续任务。审计范围：`architecture/00～14` 正文 + `data/README.md` +
> `games/_template/data/README.md`，不含 `adapters/unity/**`、`games/_template/{Runtime,Tests,Editor}`、
> `build.ps1`、`presentation/ui`（另一 agent 并行改动范围）。

## 一、`found.time_model` 数据归属改动

- **改动**：`data/_framework/found/found.time_model.json` → `data/_sample/found/found.time_model.json`
  （普通文件移动，未做 git 操作）；`games/_template/data/game/found/found.time_model.json` 新增
  （探索/战斗均 `mode: continuous`）；模板 `stat.definition.json`/`l10n.text.json` 移除随之不再需要
  的 `stat.strength` 占位行（原为满足 `found.time_model.combat.initiative_stat` 硬引用而补）。
- **判断依据**：`Core.Gameplay.Assembly.TimeModelSwitch` 构造期只要求 `found.time_model` 存在
  `scope: exploration` 一条（缺失抛异常），`scope: combat` 缺失时 `CombatModel` 为空、恒不切换
  离散模式（"缺表 = 全连续"，代码判断记录原句）。两条行的具体取值（`continuous`/`discrete`、
  `initiative_stat` 指向哪个属性）是 13 §4 口味项的具体决策，按 `data/README.md`"框架级数据表"
  判定规则（"该表的行由框架代码引用或生成"）不满足，不应放 `data/_framework/`；此前放在那里是
  一处未经该判定规则审核的遗留错放。
- **文档改动**：`data/README.md`"两类目录"一节补 `found.time_model` 判断记录；
  `games/_template/data/README.md` 表清单行改写（不再要求 `stat.strength` 占位行）；
  `architecture/01_分层与依赖.md`"实现级共享目录"补 `data/_framework`/`games/_template`
  两处目录说明。
- **验证**：`python toolchain/validate_data.py`（合并根，56 表 248 行）、
  `--data-root data/_framework --skip-dotnet`（2 表）、
  `--data-root games/_template/data/game --framework-root data/_framework`（16 表 121 行）
  三种校验均 0 错误（详见任务汇报"验收"一节）。

## 二、逐文档审计结果

### `01_分层与依赖.md` §4 模块清单

| 项 | 结论 | 说明 |
|---|---|---|
| L5 `render` 行契约接口名 `RenderConventionHost` | 已修（说明补充） | 类型名本身对，但该类型实为无内部状态的纯函数 host（`presentation/render/core/RenderConventionHost.cs`：无 `IEventBus` 字段，"全部方法都是纯函数"），不直接订阅事件；实际由 `ViewBinder`/`SpriteViewBase` 按帧调用。原"订阅 unit.moved、unit.state_changed"改写为"不直接订阅；经 ViewBinder 间接驱动"。 |
| L5 `vfx_sfx` 行契约接口名 `VfxSfxHost` | 已修 | 代码里不存在这个门面类，实为 `VfxPlayer`/`SfxPlayer`/`WeaponStyleResolver` 三个独立类型（`presentation/vfx_sfx/core/`）。数据表补 `display.weapon_style`（`VfxSfxSchemas.cs` 已登记，索引未列）。事件列改为"不直接订阅；由 feedback_binder 调用"（`CompositeFeedbackSink` 持有 `IVfxPlayer`/`ISfxPlayer` 直接调用 `Play`）。 |
| L5 `ui` 行契约接口名 `UiFrameworkHost` | 已修 | 代码里不存在这个门面类，实为 `UiDataSource`/`UiIntents`/`UiPanelRegistry` + 十个 `XxxViewModel`（`presentation/ui/core/`）。事件列补充"各 ViewModel 经 UiDataSource.Subscribe 各自按需订阅"。 |
| L5 `feedback_binder` 行主要数据表 | 已修 | 补 `feedback.floating_text_style`（`FeedbackSchemas.cs` 已登记，索引未列）。 |
| L5 `camera`/`view_binding` 行契约接口名与表名 | 无需修 | `CameraHost`/`ViewBinder` 类型名、`camera_profile` 表名与实际代码一致。 |
| L5 `view_binding` 行订阅事件 | 已修 | 补 `sim.tick_finished`（位置快照插值同步，含原 `unit.moved` 语义，见 `ViewBinderOptions` 判断记录）与 `ViewBinderOptions.ForwardedEventKeys` 转发清单（skill/combat/aura/unit/item/gobj 六域）。 |
| L5 `shell` 行订阅事件 | 已修 | `ShellHost` 实际订阅 `scene.load_started`、`scene.load_finished`、`app.state_changed`，**不**订阅 `save.completed`（存档完成后的 UI 状态由 `NewGame`/`LoadGame`/`OverwriteSlot` 等方法的返回值直接同步给调用方，不经事件总线）。 |
| "实现级共享目录"段 | 已修 | 补 `games/_template`、`data/_framework` 两条（见"一、`found.time_model`"节）。 |
| L0～L4 模块表其余行 | 未逐行复核 | 近期多次"缺口收敛"提交已密集勘误 L0～L4 各行（见变更记录 2026-09-05 之前若干条），本次审计精力集中在 L5（任务书点名重点），L0～L4 未发现需要修的新问题，但未做逐行地毯式复核，留作已知局限。 |

### `03_运行时骨架.md` §9 核心类接口签名

| 接口 | 结论 | 说明 |
|---|---|---|
| `SimClockHost`/`WorldSim`/`TurnScheduler`/`ViewBinder`/`SceneRouter`/`InputMapHost` | 无需修 | 文档列出的方法在对应 C# 契约接口（`ISimClockHost`/`IWorldSim`/`ITurnScheduler`/`IViewBinder`/`ISceneRouter`/`IInputMapHost`）里全部存在且语义一致；代码侧另有若干文档未列的实现级便利方法（`WorldSim.AddEntity`/`AllocateEntityId`/`RegisterPhaseHandler`/`ClearAll`、`SceneRouter.Update`、`InputMapHost.Update`/`ResetBindings`/`ExportBindings`/`ImportBindings`/`GetBindings`），按文档"中立接口记法，非逐方法穷举"的定位不要求补齐，仅在此存档。 |
| `AppStateHost` | 已修 | 补 `onSubStateChanged`（`IAppStateHost.OnSubStateChanged`，子状态栈变化单独订阅）与 `requestExit`（`IAppStateHost.RequestExit`，`ShellHost.Quit()` 直接依赖）。两者是已实现、被实际调用方依赖的能力，此前未列入接口签名。 |
| `RngHost` | 已修 | 补 `reset(masterSeed)`（`IRngHost.Reset`，全部流按主种子复位，供新开局/回放起点使用）。 |
| `HookRegistry`/`L10nHost`/`DisplayInfoRegistry` | 无需修（记录） | 代码侧各有 1～3 个未文档化的便利方法（`DeclareHookPoint(HookPointDefinition)` 重载、`DeclareFromDefinitions`、`CallbackCount`、`HasText`、`Reload`），均为对已文档化能力的实现级补充，不改变语义，未列入本次改动。 |

### `04_数据与内容管线.md` §1.1 总索引

- **已修**：补 4 行已落地但未登记的表——`feedback.floating_text_style`、`camera_profile`、
  `ui_layout_definition`、`shell_menu_definition`（均 L5，字段详见 09）。
- **代码待补（记录，非本次范围）**：`found.game_state`、`found.hook` 两表索引已登记多时，但
  至今没有对应 C# `TableSchema` 注册（`grep` 全仓库无匹配），与 `data/README.md` 既有判断记录
  一致（"两张表尚未有实际 JSON 数据文件落地……对应模块目前只提供内存态默认配置"）。
- **无需修**：`display.anim_set`、`display.equip_visual`、`item.affix`、`item.set`、
  `world.flag_schema` 五表已有 C# schema 登记（`DisplaySchemas`/`ItemSchemas`/`WorldStateSchemas`），
  只是暂无示例数据行（`data/_framework`、`data/_sample` 均未落数据），索引描述不受影响。
- 实测 `data/_framework` + `data/_sample` 共 56 张表、248 行记录，与索引条目数一致（逐条 diff 后
  仅上述 4 行差异）。

### `09_表现层.md`

- **已修**：§5.3 `VfxPlayer`/`SfxPlayer` 接口伪代码块补 `update(dt)`/`setLayerMuted(layer, muted)`
  （§5.4/§5.5 正文早已描述这两个能力，只是接口块本身漏列对应签名）。
- **无需修**：VFX/SFX 表字段（§5.1/5.2）、反馈绑定（§6）、UI 框架（§7）、镜头（§8）、Shell（§9）
  核对与 `presentation/` 实际代码（`IVfxPlayer`/`ISfxPlayer`/`FeedbackSchemas`/`IUiDataSource`/
  `CameraSchemas`/`ShellSchemas` 等）一致。

### `10_存档与持久化.md` §3 固定段序 vs `GameplayAssembly.RegisterPersistables`

- **已修**：步骤 7a 里的 `world.spawn_state` 改为 `spawn_state`（`Core.Gameplay.Spawn.SpawnHost.SectionKey`
  实际值不带 `world.` 前缀；本文档 §2.3 字段表原本就写的是 `spawn_state`，只是 §3 汇总顺序那一处
  写错了前缀，是文档内部自相矛盾，非新发现的代码/文档分歧）。
- **无需修**：`sim.turn_state` 已在此前一次提交（`b027862`）里补录进步骤 7b，本次复核仍然一致；
  `RegisterPersistables` 实际登记的全部段（`world_state_flags`/`world.current_map_id`/
  `world.current_position`/`player.inventory`/`player.equipment`/`player.known_skills`/
  `player.skill_bindings`/`player.currencies`/`world.vendor_stock`/`player.quest_state`/
  `player.achievement_state`/`spawn_state`/`world.dropped_loot`/`world.difficulty`/
  `sim.turn_state`（离散模式装配时）与 `SaveSections.KnownOrder`/自定义段分类核对一致，未发现
  漏登记段。

### `11_工程规范与测试.md` §1 目录约定

- **已修**：目录树 `data/` 一行改列实际的 `_framework/`/`_sample/`/`<game_name>/` 三类（原文只写
  笼统的 `<game_name>/` 一类）；`games/` 补 `_template/`；末尾 `build/` 改为 `dist/`（构建产物
  实际输出到 `dist/<version>/`，仓库内从未出现过 `build/` 目录，`build.ps1` 的 `-Dist` 参数与
  `dist/0.1.0`、`dist/0.1.1-test` 两个既有快照均印证这一点）；`toolchain/` 一行补齐事件常量/
  占位资产生成器与自身测试目录的归属说明。
- **已修**：§8 提交门槛清单开头补一句"以仓库根门禁脚本一键跑通以下各项为准"（不写脚本语言名，
  呼应本次新增的 `check.ps1`）。

### `13_新游戏接入指南.md` §4 口味配置项清单 vs `games/_template/Runtime/GameOptions.cs`

- **无需修**：逐行核对，文档 28 行口味项与 `GameOptions.cs` 字段注释"13 §4 第 N 行"逐条一一
  对应（含明确"无专用框架 Options 字段，待游戏层使用"的项，如外形类型、场景做法、影子方式、
  画布分辨率、单位间移动阻挡等），编号 1～28 连续无缺漏、无多余，是本次审计里对齐度最好的一份
  文档；该文件本身不在本次写入范围（属另一 agent 独占的 `games/_template/Runtime`），只读核对，
  未发现需要向该文件反馈的问题。

## 三、统计

- 勘误处数：8 处文档改动（`01`、`03`、`04`、`09`、`10`、`11` 六份文档，`01` 含 2 类改动合并计 1 条
  变更记录行，其余各 1 条变更记录行，合计 6 条变更记录 / 覆盖约 20 处具体字段级差异）。
- 代码待补：1 项（`found.game_state`/`found.hook` 两表仍无 C# schema，与既有判断记录一致，非
  本次新发现，不改代码）。
- 无需修：约 15 项（详见上表），多为"实现比文档更丰富，但文档的中立接口记法不要求逐条穷举"。
- 未覆盖：L0～L4 模块表逐行地毯式复核（近期"缺口收敛"系列提交已密集勘误，本次审计精力集中在
  任务书点名的 L5 部分与总索引/存档段/目录约定/口味清单四处结构性核对）；`06`/`07`/`08` 正文
  未逐节核对（任务书未点名，且体量大，超出本次可交付范围）。

## 四、第二轮（01 L0～L4 / 07 / 08）

> 本轮补齐上一轮"未覆盖"里点名的两块：`01_分层与依赖.md` 第 4 节 L0～L4 模块表逐行核对、
> `07_载体层_物品生物物件.md`/`08_玩法层_掉落任务对话关卡.md` 正文逐节核对。范围与上一轮相同，
> 不含 `adapters/unity/**`、`games/_template/{Runtime,Tests,Editor}`、`core/`、`data/`、
> `architecture/04、05、06、10`（另一 agent 并行改动范围，涉及 `combat.entered` 新增
> `hostileId`、新增 `core/carriers/projectile`、`found.game_state`/`found.hook` 表内容的条目一律
> 标记"待复核"，不据此判文档错误）。

### 4.1 `01_分层与依赖.md` 第 4 节 L0 基础层（11 模块）

| 模块 | 结论 | 说明 |
|---|---|---|
| app_lifecycle | 待复核 | `found.game_state` schema/数据文件、`AppStateMachineConfig.FromRegistry` 均为另一 agent 本地未提交改动，待入库后复核；其余列一致。 |
| sim_loop | 已修 | "契约接口名"列 `PacingPolicy` 改为 `IPacingPolicy`（代码只有接口 `IPacingPolicy` 与两个实现类 `ImmediatePacingPolicy`/`WaitForPlaybackPacingPolicy`，无字面 `PacingPolicy` 类型）。 |
| data_registry | 无需修 | 一致。 |
| expr | 无需修 | 一致。 |
| event_bus | 无需修 | 一致。 |
| rng | 无需修 | 一致。 |
| save_system | 已修 | "主要数据表"列 `save_slot_meta` 改为"存档 meta 段（`SaveMeta`/`sections.meta`，见 10 第 2.1 节）"（该字符串在代码与 10 文档均不存在，10 称"meta 段"，代码类型为 `SaveMeta`）。 |
| input_map | 无需修 | 一致。 |
| localization | 无需修 | 一致。 |
| display_info | 无需修 | 一致。 |
| scene_router | 无需修 | 一致。 |
| hook_registry | 待复核 | `found.hook` schema/数据文件同样是另一 agent 本地未提交改动，待入库后复核；其余列一致。 |

### 4.2 `01_分层与依赖.md` 第 4 节 L1 数值层（5 模块）/ L2 规则层（4 模块）

全部 9 行核对结论均为**无需修**：L1 `progression`/`stat_block`/`power_set`/`archetype`/`faction`、L2
`skill`/`combat`/`ai`/`targeting` 的契约接口名、主要数据表、主要事件（对照 `found.event_catalog.json`）
均与代码一致。`combat` 行 `combat.entered` 新增 `hostileId` 字段（可空追加字段）为另一 agent 本地
未提交改动，标记**待复核**，不影响本行"无需修"结论；`core/carriers/projectile` 尚未入库，与
L0～L2 无关。

### 4.3 `01_分层与依赖.md` 第 4 节 L3 载体层（4 模块）/ L4 玩法层（10 模块）

| 模块 | 结论 | 说明 |
|---|---|---|
| item（L3） | 无需修 | 一致。 |
| creature（L3） | 已修 | "契约接口名"列由"无独立契约"改为"`ICreatureTemplateQuery`（只读模板查询，经 DataRegistry；运行期状态经 06 Unit 相关契约）"（代码实际有独立 `contracts/` 目录）；"主要数据表"列补 `creature.tier_definition`（强度分档表，已有实际示例数据）。 |
| gobj（L3） | 已修 | "策略配置项"列锁的判定方式补第三种"技能检定"（`item_key`/`world_flag`/`skill_check` 三种，原文只列前两种）。 |
| summon（L3） | 已修 | "契约接口名"列由"复用 AiHost / CombatHost，附加 ownerId 查询"改为"`ISummonHost`（召唤/取消/拥有者查询）；生成实体复用生物模板契约，AI/战斗行为经生物自身的 AiHost/CombatHost"（"召唤/管理"本身有独立命名契约，不是单纯复用）。 |
| loot（L4） | 无需修 | 一致。 |
| quest（L4） | 无需修 | 一致。 |
| dialog（L4） | 无需修 | 一致。 |
| encounter（L4） | 已修 | "契约接口名"列由"`EncounterHost`"改为"`EncounterHost`、`LevelHost`"（对应该行已列出的 `encounter.level` 表，此前遗漏负责关卡自动推进的契约）。 |
| difficulty（L4） | 无需修 | 一致。 |
| achievement（L4） | 无需修 | 一致。 |
| economy（L4） | 无需修 | 一致。 |
| world_state（L4） | 无需修 | 一致。 |
| area_trigger（L4） | 无需修 | 一致。 |
| spawn（L4） | 无需修 | 一致。 |

`core/carriers/projectile` 尚未入库，L3 表当前也未新增该行，二者一致；待其入库后 L3 表需补一行，
届时一并核对契约/数据表/事件（**待复核**）。

### 4.4 `07_载体层_物品生物物件.md`

逐节核对，共 8 处勘误，均已改为最小幅度的文档修订（版本号不变、变更记录加一行"勘误："）：

| 位置 | 结论 | 说明 |
|---|---|---|
| §1.1 模板字段表 | 已修 | 补 `name_key`（必填）、`requirements`（可选，装备等级需求）两个字段，代码/示例数据均已使用。 |
| §1.3 `InventoryHost.listItems` | 已修 | 返回类型 `List<ItemStack>` 改为 `List<ItemInstance>`（`ItemStack` 不带实例 id，无法配合 `removeItem` 的 `itemInstanceId` 参数，属逻辑缺口而非笔误）。 |
| §2.1 模板字段表 | 已修 | 补 `name_key`（必填）一行。 |
| §2 事件（"每个模块的补充信息"表 Creature 行） | 已修 | 事件列由"复用 Unit 相关事件"改为"`creature.spawned`、`creature.despawned` + 复用 Unit 相关事件"（代码实际发布独立 `creature` domain 事件，非单纯复用）。 |
| §3.1 类型数据 | 已修 | 补 `gobj.template` 顶层 `name_key`（必填）、`tags`（可选，标签集合）两个字段。 |
| §3.2 `LockDef` struct | 已修 | 补 `consumeKey: Bool`（默认 false，开锁是否消耗钥匙物品）字段。 |
| §5 种族描述 | 已修 | 由"种族 = 被动光环……同样复用 Skill 系统"改为拆分"直接属性修正（不经 Skill 系统）+ 可选被动光环引用（复用 Skill 系统）"两种机制，原描述掩盖了两条不同实现路径。 |
| "每个模块的补充信息"表 召唤与宠物行 | 已修 | "目录名"列由"复用 `core/carriers/creature`"改为 `core/carriers/summon`（该模块本身是独立目录，内部才复用 creature 模板生成契约）；"契约"列补 `SummonHost`（`Summon`/`Dismiss`/`GetOwner`/`GetSummons`）。 |

代码待补 → **已接入（收边 I3）**：`SummonOptions.ShareThreat` 已经 `ICombatHost.GetThreatTable`
（收边 I1 落地）接入最小语义——`SummonTickHandler` 每 tick 把 owner/召唤物各自仇恨表条目按来源合并
（同源取较大值）后回写双方，效果上等价"共享一张仇恨表"（见 `SummonTickHandler.ShareThreatWithOwner`
判断记录）；`SummonOptions.PlayerCanControl` 已经 `SummonHost.IsControllableByOwner` 接入——供游戏层/
输入系统查询"该召唤物是否允许玩家直接输入"，并接入 `GameplayAssembly` 的 `IsPlayerActor` 判定（开启
时该召唤物在离散模式下与玩家本人同等对待，轮到它时等待外部提交意图，不会被 AI 自动接管）。

待复核（进行中，不判文档错误）：`combat.entered` 的 `hostileId` 字段变更、`core/carriers/projectile`
模块新增与 07 全文无直接交集，未发现需要因此调整的表述。

### 4.5 `08_玩法层_掉落任务对话关卡.md`

逐节核对，共 7 处勘误：

| 位置 | 结论 | 说明 |
|---|---|---|
| §1.1 `loot.table` | 已修 | 补充嵌套 `loot.table` 引用下 `countRange` 的语义（表示把被引用的整表再抽取该次数，而非重复同一次抽取结果）。 |
| §2.1 `quest.def` 字段表 | 已修 | 补 `title_key`（必填）、`description_key`（可选）；`QuestObjective` 结构补 `descriptionKey`。 |
| §3.2 剧情对话树 | 已修 | 补一句"起始节点固定为 `nodes` 数组第一个元素"。 |
| §5.1 `diff.tier` 字段表 | 已修 | 补 `name_key`（必填）、`sort_weight`（可选，内容管线排序用，运行期不读取）。 |
| §6.1 `achv.def` 字段表 | 已修 | 补 `name_key`（必填）；`Criterion` 结构补 `filter`（六种类型均可选携带的补充匹配条件）。 |
| §7.1 `econ.currency` 字段表 | 已修 | 补 `name_key`（必填）。 |
| §7.2 `econ.vendor` 字段表 | 已修 | 补 `name_key`（必填）、`map_id`（可选，`on_map_enter` 补货策略用）；`sell_items` 结构补 `restockTimer`（`timer` 补货策略下必填的补货周期）。 |

代码待补 → **已接入（收边 I3）**：
1. `core/gameplay/encounter`：`EncounterDefinition.CombatModeOverride`/`InitiativeOverride`
   （对应 08 §4.1）现由 `EncounterHost.TryGetModeOverride` 暴露、`GameplayAssembly` 订阅
   `encounter.started`/`encounter.won`/`encounter.lost` 三个事件接线到
   `TimeModelSwitch.SetPendingOverride`（遭遇开始时落定覆盖，胜利/失败时清空，见该处判断记录
   "遭遇结束清除"）；`TimeModelSwitch.SetPendingOverride` 签名同时改为三态 `bool?`
   （`PendingCombatModeOverride`，见其判断记录），修正了此前"覆盖只能强制切到离散、不能在场景
   默认已是 discrete 时强制维持连续"的单向缺陷；`initiative_override.{policy,params}` 一并接入
   （`EffectiveInitiativeStat`、`TurnScheduler.Configure` 的 policy/`action_points_per_turn`）。
2. `core/gameplay/encounter`：`EncounterTickHandler`（对应 08 §4.3"离散模式下阶段切换于回合结束
   时生效"一句）现按 `SimStep.Kind` 区分——离散 Act 步（`TurnScheduler.NextStep` 只产生这一种离散
   步）本身不再求值，改为构造期订阅 `sim.turn_ended`/`sim.round_ended` 事件驱动求值；连续模式
   行为不变（仍按 tick 频率）。

待复核（进行中，不判文档错误）：08 §3.2 `performanceHookRef` 依赖的 `found.hook` 表内容目前只有
2 条场景生命周期钩子，尚不含对话演出相关项，待另一 agent 扩充入库后复核是否需要补充示例。

### 4.6 第二轮统计

- 勘误处数：18 处具体字段/措辞改动，合计 4 条变更记录行（`01` 一条、`07` 一条、`08` 一条，另 `01`
  L0 两处已并入第一轮已有变更记录格式单独成行）——`01` 2 处（sim_loop 契约名、save_system 数据表）
  + 5 处（creature 契约名+数据表、gobj 策略项、summon 契约名、encounter 契约名，跨 L0/L3/L4 共
  7 处字段级差异合并 2 条变更记录行）、`07` 8 处、`08` 7 处。
- 代码待补：4 项（`core/gameplay/encounter` 的 `CombatModeOverride`/`InitiativeOverride` 未接线、
  `EncounterTickHandler` 未按离散/连续区分求值时机；`core/carriers/summon` 的 `ShareThreat`/
  `PlayerCanControl` 两个占位配置项未接入实际逻辑），均记录、不改代码。
- 无需修：约 30 项（L0～L4 模块表其余 27 行 + 07/08 中核实无误的接口/事件/字段）。
- 待复核（进行中变更，等另一 agent 入库后复核）：`app_lifecycle`/`hook_registry`/`combat`（L0～L2
  模块表）、07 全文、08 §3.2 共 5 处标注，均不计入本轮"勘误"或"代码待补"。

## 五、"待复核"复核（第二轮遗留，2026-09-05 收边 I3）

> 第二轮标注的 5 处"待复核"里，`combat.entered.hostileId` 变更与 `found.hook` 表内容此时均已
> 入库（收边 I1，`8bfd329`）——本节按任务书范围只复核 07/08 两处正文相关的标注（`07` 全文、`08`
> §3.2），`01` L0 模块表的 `app_lifecycle`/`hook_registry`/`combat`/L3 `projectile` 行 4 处仍未复核
> （不在本次任务范围，留待后续波次；`combat` 行"待复核"注记本身已在 4.2 节说明"不影响本行'无需修'
> 结论"，实质结论不受影响）。

| 位置 | 第二轮标注 | 复核结论 | 说明 |
|---|---|---|---|
| `07` 全文 | 待复核：`combat.entered.hostileId` 字段变更、`core/carriers/projectile` 模块新增是否与 07 全文有交集 | **确认成立，无需修** | 逐项核实：`combat.entered` 事件、`hostileId` 字段全文零匹配（`grep -n "combat\." 07_载体层_物品生物物件.md` 无结果）；`projectile`/`抛射物` 字样全文零匹配。07 覆盖物品/生物/物件三类载体，`core/carriers/projectile`（抛射物实体+效果）是新增的第四类载体，07 正文目前未描述它——这不是"文档写错了"（07 定稿时该模块确实不存在），而是"文档尚未覆盖新落地的模块"，按 12 第 5 节"新增内容不算勘误"应走独立的文档扩展任务（补一节"抛射物"+"每个模块的补充信息"表新增一行），不在本次"复核既有表述是否仍准确"的范围内，故本条结论维持第二轮"无需因此调整现有表述"，`projectile` 一节的补充留作已知缺口（见下）。 |
| `08` §3.2 `performanceHookRef` | 待复核：`found.hook` 表内容扩充后是否需要补充对话演出相关示例 | **确认成立，无需修** | `data/_framework/found/found.hook.json` 已入库，实际内容仍是 `found.hook.scene_pre_unload`/`found.hook.scene_post_load` 两条场景生命周期钩子（03 第 6 节步骤 4/6 用途），不含任何对话/演出相关项——`performanceHookRef: Optional<Id>` 字段本身的类型/语义描述（"进入该节点时调用的 found.hook，用于过场/运镜等演出"）不依赖 `found.hook` 现有哪些具体行（该字段允许游戏数据作者引用任意游戏自己登记的 hook id，框架级 `found.hook` 是否预置对话专用钩子是另一件事，不影响本字段的契约描述）；08 §3.2 的字段级表述与代码/数据现状一致，无需修。 |

已知缺口（非本次任务范围，供后续波次参考）：`architecture/07_载体层_物品生物物件.md` 目前没有描述
`core/carriers/projectile`（抛射物实体、`IProjectileSpawner` 依赖倒置、`projectile` 效果类型）——
这是"文档扩展"而非"勘误"，建议后续波次按 07 现有"每个模块的补充信息"表体例新增一行，并在正文
合适位置（07 载体总览）补一节简述。

## 六、第三轮（06，2026-09-05 收边 I3）

> 逐节核对 `06_规则层_属性技能战斗AI.md` 全部 8 节（StatBlock、PowerSet、Skill、Combat、
> Targeting、AI、契约签名汇总、事件词汇表）：接口签名 vs `core/numbers`/`core/rules` 的
> `contracts`、字段表 vs `schema` + `data/_sample`、效果类型表 vs `EffectDispatcher`（含新落地的
> `projectile`）、`AuraEffect` 类型表 vs `AuraEffectKind`、施法失败原因码 vs `CastFailureReason`、
> 死亡复活策略 vs `RespawnPolicy`、行为状态机 vs `BehaviorState`、事件表 vs
> `data/_framework/found/found.event_catalog.json` 与对应事件类（`core/rules/common/contracts/
> Events.cs`）、Expr 分组提及 vs `RulesExprSchema`/`ExprGroups`、结算管线九步顺序 vs
> `core/rules/combat/core/Resolver.cs` 注释。

### 6.1 逐节结论

| 节 | 结论 | 说明 |
|---|---|---|
| §1 StatBlock | 已修（1 处） | `StatModifier` 结构缺 `multGroup` 字段（见 6.2）。契约 `getStat`/`addModifier`/`removeModifiersBySource` 与 `IStatHost` 一致（额外的 `RegisterUnit`/`UnregisterUnit`/`IsRegistered`/`SetBase`/`GetBase`/`GetModifiers` 属"契约必要展开"，惯例同 03 审计"不逐方法穷举"，无需修）。 |
| §2 PowerSet | 已修（1 处） | 事件遗漏 `power.depleted`（见 6.2）。契约 `getPower`/`getPowerMax`/`modifyPower` 与 `IPowerHost` 一致（额外方法同上惯例，无需修）。 |
| §3 Skill | 无需修 | `SkillDef` 16 个字段与 `SkillSchemas.Def` 逐一对应；Effect 原语表 19 项与 `EffectKind`/`EffectKindNames` 取值、顺序完全一致（含收边 I1 新落地的 `projectile`，`EffectDispatcher` 全部 19 个 `case` 分支齐备）；`AuraEffect` 类型表 10 项与 `AuraEffectKind` 取值、顺序完全一致；Proc 字段表与 `SkillSchemas.ProcDef` 一致；`SpellModDef` 结构与 `SkillSchemas.SpellModDef`（含 `SpellModDimensionValues`/`SpellModOpValues` 六/二枚举）一致；施法管线固定 9 步顺序与 `CastPipeline.cs` 实现顺序一致，失败原因码"示例"（`SILENCED`/`STUNNED`/`ON_COOLDOWN`/`GCD_ACTIVE`/`INSUFFICIENT_POWER`/`NO_VALID_TARGET`/`OUT_OF_RANGE`/`LINE_OF_SIGHT`）与 `CastFailureReason` 对应值齐全，该枚举额外 5 项（`None`/`UnknownSkill`/`PassiveSkill`/`NoCharges`/`Busy`）已在类型自身注释标注"补充"，06 原文措辞本就是"示例"不是穷举，无需修。 |
| §4 Combat | 无需修 | 结算管线固定步骤（`判定→基础值→暴击→施法者乘区→减免→目标乘区→免疫吸收→落地→后置`）与 `Resolver.cs` 顶部注释逐字一致；命中表六项默认开关与 `HitTableConfig`/`combat.hit_table_config` 一致（该表另有 `crit_multiplier_stat`/`crit_multiplier_base`/`glancing_damage_pct`/`block_value_stat` 等曲线参数字段，06 §4.2 的表格本就只谈"六项默认是否开启"，不是字段全集，不需要为此补充，惯例同 04 审计对 04 索引表"字段详见 09"式引用的处理）；`ThreatTable` 三方法与 `IThreatTable` 一致（额外的 `GetThreat`/`SetThreat`/`GetAll` 属契约展开，无需修）；死亡复活三策略与 `RespawnPolicy`（`RespawnPoint`/`ReloadSave`/`Permadeath`）取值、默认值（`RespawnPoint`）一致。 |
| §5 Targeting | 已修（1 处） | `TargetChainDef` 结构缺 `shape`/`maxTargets` 两字段（见 6.2）。`source` 枚举文档用"..."标注非穷举，与 `TargetSchemas` 把 `source` 声明为自由 `String`（非 `Enum`，供游戏层扩展策略）的设计意图一致，6 个内置策略（含文档未提及的 `AllInShape`/`ThreatTop`）不需要逐一列举，无需修。 |
| §6 AI | 已修（1 处） | `ai.behavior_profile` 字段列表缺 `rotation_ref`/`decision_interval`/`transitions` 三字段（见 6.2）。行为外壳 7 态（`idle/patrol/chase/combat/return/flee/dead`）与 `BehaviorState` 枚举取值、顺序完全一致；`RotationEntry`/`ai.rotation` 结构与 `AiSchemas.Rotation` 一致；`IAiHost` 三方法（`evaluate`/`getBehaviorState`/`forceState`）与代码一致（额外 `SetRotation` 是 §6.4"Boss 阶段换 Rotation"描述能力的直接实现，属契约必要展开，无需修）；`ai.patrol_path` 结构（有序点列表 + `loop`/`pingpong`）与 `AiSchemas.PatrolPath` 一致。 |
| §7 契约签名汇总 | 无需修 | 是 §1/§4/§6 契约的重复摘录，与各自小节结论一致，不重复计入。 |
| §8 事件词汇表 | 已修（1 处，与 §2 同一处） | 25 行事件逐条核对 `found.event_catalog.json` 对应行 + `core/rules/common/contracts/Events.cs` 对应事件类：字段名、`domain` 全部一致（含 `combat.entered.hostileId` 已入库一致、`proc.triggered` 的 `domain: skill`"原文如此"一致）；唯一缺口是 `power.depleted` 未登记为独立一行（该表格是 06 自身"统一登记"的权威事件词汇表，`power.depleted` 已在 `found.event_catalog.json` 单独登记且描述明确指向"见 06 第 2 节"，06 本节反而没有对应行，判定为遗漏，见 6.2）。 |

### 6.2 勘误清单（4 处字段/事件级遗漏，均为文档遗漏、代码已正确实现，不改代码）

1. §1.2 `StatModifier` 结构补 `multGroup: Optional<String>`（独立乘区分组名）。
2. §2.2 事件补 `power.depleted { unitId, powerType }`；§8 事件词汇表同步补一行。
3. §5 `TargetChainDef` 结构补 `shape: Optional<Shape>`、`maxTargets: Optional<Int>`。
4. §6.1 `ai.behavior_profile` 字段列表补 `rotation_ref`（必填）、`decision_interval`（可选）、
   `transitions`（可选）。

以上 4 处合并为 06 变更记录一条"勘误："（版本号不变，见本文件"变更记录"表 2026-09-05 第五行）。

### 6.3 第三轮统计

- 勘误处数：4 处字段/事件级遗漏（合并 1 条变更记录行）。
- 代码待补：0 项——本轮核对的全部契约/字段/事件/枚举，代码侧均已正确实现，未发现"文档承诺、代码
  未跟上"的情形（与本任务步骤 1 的 (a)(b)(c)(d) 代码缺口不同来源：那 4 项是此前审计已发现、本任务
  接线补齐的已知缺口，不属于本轮 06 逐节核对新发现）。
- 无需修：约 20 项（详见 6.1 表，多为"实现比文档更丰富但文档中立记法不要求逐条穷举"，惯例与前两轮
  一致）。
- 06 全篇除 §7（纯摘录）外，7 节均已逐节核对完毕，无遗留"未覆盖"标注。
