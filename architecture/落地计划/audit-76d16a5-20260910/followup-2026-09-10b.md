# 第十五轮审核修复跟进核实（2026-09-10）

基线：`76d16a54e54f0f11204d97d7460563c8a0dc8cd8`（v1.14.0）。本文档核实
[AUDIT_REPORT.md](AUDIT_REPORT.md) 六项 P2（PJ114-01/02、CORE114-01～04）+ 一项静态工具风险
（PJ114-03）+ 一项已确认 P3（`RewardSchemaFields` 注释漂移）+ 一项未确认候选（Equipment set
cache）的实际修复结果。

修复由并行执行的两个 agent 分工：WA 负责 ABI 相关三项（PJ114-01/02/03：17 参数兼容构造 façade、
通用"公开 API 表面差异"门禁、缺基线可见 SKIP/发布模式严格、CI 下载基线包、输出目录安全边界）；
WB 负责核心运行时四项（CORE114-01～04）+ P3 注释勘误 + 套装缓存负例补充。本文档现由整合 agent
（2026-09-10）核实并回填，核实方法：逐条对照下方"复现"列摘录的 trigger/actual/expected，独立走查
提交的实际代码改动是否命中该描述、独立重跑相关测试与探针（不采信任一方的自我报告作为唯一依据）。

提交哈希表：

| 提交 | 标题 | 范围 |
|---|---|---|
| `c35543d` | 根治 PJ114-01: SkillHost/ViewBinder 旧二进制 ABI 断裂 | `core/rules/skill/core/SkillHost.cs`、`presentation/view_binding/core/ViewBinder.cs` |
| `f0e5cd8` | 根治 PJ114-02: 新增通用公开 API 表面差异工具 toolchain/abi_surface（同批含 PJ114-03 OutDir 安全） | `toolchain/abi_surface/`（新增）、`toolchain/abi_probe.ps1`、`toolchain/abi_probe/`、`toolchain/README.md`、`toolchain/tests/test_abi_surface_compare.py`、`toolchain/tests/test_abi_probe_outdir_safety.py` |
| `b3c898e` | 根治 PJ114-02 门禁语义: check.ps1 ABI 步骤可见 SKIP/FAIL、-AbiStrict、CI 拉基线 | `check.ps1`、`build.ps1`、`.github/workflows/ci.yml` |
| `89b1b69` | 第十七方深度审核修复(核心侧): CORE114-01～04 + P3 + 套装缓存候选 | `core/gameplay/quest/core/QuestHost.cs`、`core/gameplay/economy/core/EconomyHost.cs`、`core/numbers/stat_block/core/StatHost.cs`、`core/gameplay/common/contracts/{ExprValueJson.cs,RewardSchemaFields.cs}`、`core/carriers/item/core/EquipmentHost.cs`，及各自 `tests/` 新增回归 |

## 核实表

| 条目 | 判断 | 复现 | 修复位置 | 验收 | 判断记录 |
|---|---|---|---|---|---|
| PJ114-01 SkillHost 旧二进制构造签名不兼容 | **成立，已修复** | 条件：消费方只替换正式 1.14 DLL、不重建 consumer。实际：当前 `SkillHost` 只有含新增 `IFactionMatrix? factions` 的 18 参数构造器；1.13.0 正式 DLL 编译的旧 consumer 只替换五个 Core DLL 为正式 1.14.0 后，退出码由 0（旧 DLL，`ArgumentNullException=dataRegistry`）变为 11（`MissingMethodException`），与 `CHANGELOG.md` 声明的"无需重编译即可运行"冲突。预期：17 参数入口须可解析并转发到 18 参数实现，旧 consumer 换 DLL 后应退出 0；若接受破坏性变更须走版本/迁移契约，不能只改 changelog 文字。 | `core/rules/skill/core/SkillHost.cs`（新增 17 参数 `[Obsolete]` 构造函数，全部参数不带默认值避免重载二义性，转发到 18 参数实现、`factions` 传 `null`）；额外发现并同批修复 `presentation/view_binding/core/ViewBinder.cs`（新增 7 参数 `[Obsolete]` 构造函数，`EquipmentVisualSource` 传 `null`）——均在提交 `c35543d` | 整合阶段独立重新验证（未采信原始审计或修复提交的自我报告）：用当前 HEAD（含本轮修复）重新构建 `Core.sln -c Release`；复用审计归档 `docs-project/api-compat/run-skillhost-abi.ps1` 同一方法论构造消费方 consumer（17 参数构造调用）——对未打补丁的正式 `dist/ws-game-1.14.0.zip`（修复前构建产物）实跑复现 `NEW_FORMAL_RUN_EXIT=11`、`MissingMethodException`；随后只把同一个 consumer 程序集（未重新编译）的五个 Core 依赖 DLL 替换为本轮修复后新构建的版本，重跑退出码变为 0、`SKILLHOST_LEGACY_SIGNATURE_FOUND ArgumentNullException=dataRegistry`（与 17 参数旧签名的预期行为一致）。六工程 `dotnet test` 全绿（见下方"全量门禁"）。 | 独立复现证实条目成立且已修复。同时发现一处数值差异：审计归档 `AUDIT_REPORT.md`/`docs-project/project-findings.md`/`docs-project/validation.md`/`docs-project/logs/api-compat/api-compat.log` 四处一致记录消费方 consumer 程序集 SHA256 为 `13500df60474618a7960649f8ff1241a2cad460cfe576e30bbd033fd5457e00a`，而本次整合任务书给出的核对值 `f523296d9545eb705e7815c8468ac738c3f486c2d3b54db1e170a92b5f2dbf99` 与归档不符——已逐一核对四份归档文件确认口径一致，判定任务书给出的哈希系转录笔误，本文档以审计归档记录的 `13500df6...` 为权威值。本次整合验证未复用归档中那份具体程序集文件（该文件未随源码树保留，仅归档记录了其哈希），而是按同一方法论独立重新构建了一个 consumer 程序集用于替换前后对比，二者共享同一方法论关键点（consumer 程序集本身在"替换前"与"替换后"两次运行之间完全不重新编译，只替换 Core DLL），足以独立判定条目成立且已修复。额外发现的 ViewBinder 7 参数构造 façade 缺失，经走查 `c35543d` 的实际 diff 确认属实，是同一根因（新增可选参数使公开构造函数物理签名收窄）的同构问题，未独立编号。 |
| PJ114-02 ABI 门禁缺基线时假 PASS | **成立，已修复** | 条件：`check.ps1 -SkipUnity` 执行 ABI 步骤应在基线存在时跑真实 consumer probe。实际：冻结树不存在 `dist\ws-game-1.12.0.zip`，`abi_probe.ps1` 默认 `SkipIfBaselineMissing=$true`，缺包时输出警告后 exit 0；`check.ps1` 对该子进程 `| Out-Null`，完整 transcript 中该步骤显示 `PASS`，没有 ABI 运行证明。预期：基线缺失应显式 `SKIP`/无资格，不能记为 PASS；发布模式对兼容承诺缺证据应阻断；探针产物须保存 old/new exit、consumer hash、DLL hash。 | `toolchain/abi_probe.ps1`（`SkipIfBaselineMissing=$true` 且基线缺失时退出码由 0 改为 3；探针结果统一落盘 `OutDir/summary.txt`，含基线/consumer/DLL 各项 sha256、新旧运行退出码、`toolchain/abi_surface` 表面差异报告路径）+ 新增 `toolchain/abi_surface`（独立 dump/compare 工具，提交 `f0e5cd8`）；`check.ps1`"2b. ABI 探针"步骤（子进程输出改落盘 `$ArtifactsPath\abi_probe.log`、PASS/SKIP 时不刷屏失败时回显；`Invoke-CheckStep` 新增可见 Skip 结果形状，退出码 3 记为 SKIP、0 记为 PASS、其余记为 FAIL；新增 `-AbiStrict` 开关透传 `-SkipIfBaselineMissing:$false`，`build.ps1 -Release` 第 5 步固定携带；`.github/workflows/ci.yml` 新增下载基线包步骤，下载失败不崩溃但输出 `::warning::` 提示，提交 `b3c898e`） | 整合阶段独立重跑 `python -m pytest toolchain/tests/test_abi_probe_outdir_safety.py toolchain/tests/test_abi_surface_compare.py -q`：13 passed（见下方"全量门禁"）；走查 `check.ps1` 源码确认 `Invoke-CheckStep` 对探针 exit 3 的分支不再落入 PASS 判定，且探针脚本本身对 exit 3 路径不再执行任何 consumer 实跑（符合"缺基线即不产出虚假通过证据"的预期）。 | 走查确认退出码语义按 0=PASS/3=SKIP/其余=FAIL 三态实现，`check.ps1` 与 `abi_probe.ps1` 两侧对该三态的解释一致；`-AbiStrict` 确实是把 `-SkipIfBaselineMissing` 取反透传，`build.ps1 -Release` 固定携带该开关（走查 `build.ps1` 第 5 步调用点确认）。CI workflow 下载基线包这一步骤未在本地环境实跑验证（无法在沙盒内模拟 GitHub Actions release 下载网络环境），改为源码走查确认下载逻辑存在、`::warning::` 提示存在，以及提交信息自述已用 `python -c "import yaml; yaml.safe_load(...)"` 做过语法校验（未在整合阶段重复该项，采信该项为已完成的静态检查，风险低）；判定"成立、已修复"基于门禁语义本身正确接线与本地可验证部分，CI 环境实际下载行为未独立复现。 |
| PJ114-03 ABI 输出目录无边界删除（静态工具风险） | **成立（静态），已修复** | 条件：调用者传入已存在的 `-OutDir`。实际：`abi_probe.ps1:116-117` 对已存在的显式 `$OutDir` 直接 `Remove-Item -Recurse -Force`，未验证目标位于本轮唯一临时目录，未拒绝用户已有目录；本轮未用已有资料目录复现，属源码审计结论。预期：默认新建唯一目录；显式已有目录直接拒绝，或验证其位于专用临时根且带唯一前缀；用 sentinel 证明边界。 | `toolchain/abi_probe.ps1`（默认 OutDir 改为时间戳+随机后缀的全新临时目录；显式 `-OutDir` 已存在且非空直接拒绝 exit 1；任何分支都不再对已存在目录执行 `Remove-Item -Recurse`；OutDir 校验提前到基线解析之前，独立于基线是否存在都能验证）——提交 `f0e5cd8` | 整合阶段独立重跑 `python -m pytest toolchain/tests/test_abi_probe_outdir_safety.py -q`：通过（见下方"全量门禁"，用例覆盖非空既有 OutDir 拒绝且哨兵文件不受影响、空既有 OutDir 复用、默认 OutDir 新建三种分支）。 | 走查 `abi_probe.ps1` 当前源码逐行核对，确认已不存在任何无条件 `Remove-Item -Recurse -Force` 分支，OutDir 相关校验确已提前到基线解析之前；测试用哨兵文件方法论（预置文件、验证拒绝后哨兵仍在）与判断记录要求的验收方式一致，未发现描述与实现不符之处。 |
| CORE114-01 QuestHost reload 后目标数组越界 | **成立，已修复** | 条件：启用 Gameplay QuestHost，活跃任务 reload 定义后须安全处理后续进度。实际：旧定义一条 kill 目标、已有进度，reload 同 id 为两条目标后，再更新新目标 index 1 抛 `IndexOutOfRangeException`。预期：迁移/调整旧计数数组，或原子拒绝不兼容 reload；目标增加/减少/重排均须有明确结果，不能让合法更新越界。 | `core/gameplay/quest/core/QuestHost.cs`：`Reload`（134 行起）调用新增的 `MigrateProgressAfterReload`（209 行起，按 `(Type, TargetRef)` 把新目标数组与旧目标数组逐一配对迁移旧计数，先到先得避免同一条旧目标被两个新目标重复认领，未匹配到的新目标按 `Collect` 非消耗型重算或置 0）；抽出的单目标收尾规则 `ResolveMigratedObjectiveCount`（280 行起）同时被存档读入迁移（`ReplaceAllProgress`，630 行调用点）复用；`ReevaluateCompletionState`（769 行起）统一处理迁移后完成态重判——均在提交 `89b1b69` | 整合阶段独立重跑 `dotnet test Core.sln -c Release --no-build`：六工程全绿（Tests.Foundation 748、Tests.Numbers 127、Tests.Carriers 385、Tests.Rules 439、Tests.PresentationCommon 548、Tests.Gameplay 650，见下方"全量门禁"）；独立数出 `core/gameplay/quest/tests/CORE114_01_QuestHostReloadMigrationTests.cs` 实际为 9 个 `[Fact]`（无 `[Theory]`），与任务书给出的线索数字一致，未发现需更正之处。 | 独立走查 `QuestHost.cs` 该提交的实际 diff，确认 `MigrateProgressAfterReload` 的配对/先到先得/未匹配重算逻辑与提交信息描述一致；额外确认 `ResolveMigratedObjectiveCount` 同时服务两条路径（reload 迁移与存档读入迁移）这一点属实，且顺带修正了存档 `ObjectiveCounts` 未 clamp 到当前 `Count` 上限、以及快照变长时新增目标未按库存持有量重算这两处衍生问题——这两点在原始审计报告与任务书线索中均未提及，是本次核实中独立发现的、提交范围内但未被显式记录的额外正确性收益，如实记录，不影响"成立"判断。迁移范围只覆盖 Active/ObjectivesComplete 两个非终态，终态不会再被任何按当前定义索引的路径触碰，走查确认符合该边界声明。 |
| CORE114-02 EconomyHost none→timer 未建计时器 | **成立，已修复** | 条件：启用 EconomyHost，既有 vendor/item reload 为 timer 定义必须建立可用 timer。实际：旧 none（TimerRemaining 恒为 null）库存耗尽为 0，reload 同一商品为 timer 策略后，旧 StockState 原样保留、TimerRemaining 仍是 null，Update 因 `!state.TimerRemaining.HasValue` 直接跳过，库存永久卡死不再补货。预期：定义转换须初始化/重采样 timer，明确 none/on-map-enter/timer 转换语义并守恒已消费库存；旧 host 到期应按新 `stock_limit=2` 恢复。 | `core/gameplay/economy/core/EconomyHost.cs`：新增 `ReconcileStockStateWithNewDefinition`（192 行起），`ReloadFromRegistry` 对已存在的 `(vendorId, itemId)` 组合调用该方法对账（143 行调用点）——提交 `89b1b69` | 整合阶段独立重跑 `dotnet test`：全绿（见上一行同一次运行，下方"全量门禁"汇总）；独立数出 `core/gameplay/economy/tests/CORE114_02_EconomyReloadStockPolicyTransitionTests.cs` 实际为 5 个 `[Fact]`，与任务书给出的线索数字一致。 | 独立走查确认该方法覆盖 none/on_map_enter/timer 两两转换（转入 timer 且此前无计时器则按新周期起算，转出 timer 则清空计时器，timer→timer 周期变化则 clamp 到新周期）与 `StockLimit` 独立对账（新上限存在则 `Math.Min` 旧值与新上限，旧值为 null 则按新上限满库存起算，上限撤销则 Remaining 置回 null）——覆盖范围与提交信息描述一致，测试数量核实无误。 |
| CORE114-03 StatHost 派生缓存未随 reload 失效 | **成立，已修复** | 条件：启用 StatHost，未显式覆盖的派生值下一次访问应使用新 definition。实际：两个相同、都未显式 SetBase 过的单位，reload 前只查询过其中一个（缓存下 0），reload default_base 为 77 后，先查询过的那个仍返回缓存里的旧值 0，另一个第一次查询直接现算得到 77——同一条规则下两个未修改单位因查询顺序分叉出不同结果。预期：失效派生缓存或显式区分 base/derived 并提供一致重算；未显式覆盖的 A/B 都应为 77，只有显式 runtime base 才保留差异。 | `core/numbers/stat_block/core/StatHost.cs`：新增 `RecomputeAllCachedStatsAfterReload`（123 行起），`Reload` 内调用（92 行）——提交 `89b1b69` | 整合阶段独立重跑 `dotnet test`：全绿（同上）；独立数出 `core/numbers/stat_block/tests/CORE114_03_StatHostReloadCacheInvalidationTests.cs` 实际为 4 个 `[Fact]`，与任务书给出的线索数字一致。 | 独立走查确认该方法只重算"已经算过"（已被 `GetStat` 现算并写入 Cache）的属性，未被查询过的属性留给下次 `GetStat` 自然用新定义现算，不会分叉；显式 Base 覆盖值不受影响；定义被 reload 删除的属性直接从 Cache 移除且不发事件；值变化时补发 `StatChangedEvent`——均与提交信息描述一致，未发现需更正之处。 |
| CORE114-04 ExprValueJson 非法 Id 使正式校验冒泡异常 | **成立，已修复** | 条件：正式 `ContentValidationAssembly` 对 Quest world flag value 的错误形状必须产生可定位报告。实际：`{"$id":"BAD"}` 进入 `ExprValueJson.IsValid` 时冒泡 `ArgumentException`，Quest 正式链没有把异常转换为 report。预期：`IsValid` 捕获非法 Id（或先 `Id.TryParse`），正式 Quest/Dialog validation 输出 blocking 字段错误；合法 Bool/Number/String/Id 仍通过。 | `core/gameplay/common/contracts/ExprValueJson.cs`：`Parse` 内 `$id` 分支改用 `Id.TryParse`，失败时统一转译为携带原始 `"$id"` 文本的 `FormatException`；`IsValid` 保持只捕获 `FormatException`（不扩大捕获范围掩盖其他编程错误）——提交 `89b1b69` | 整合阶段独立重跑 `dotnet test`：全绿（同上）；独立按 xunit 实际生成的测试用例数（`[Fact]` 计 1、`[Theory]` 按 `[InlineData]` 行数展开）核实三个相关测试文件：`core/gameplay/common/tests/ExprValueJsonTests.cs` 5 个 `[Fact]` + 1 个 `[Theory]`（6 条 `[InlineData]`）= 11 个测试用例；`core/gameplay/quest/tests/CORE114_04_QuestWorldFlagInvalidIdFormalValidationTests.cs` 2 个 `[Fact]` + 1 个 `[Theory]`（4 条 `[InlineData]`）= 6 个测试用例；`core/gameplay/dialog/tests/CORE114_04_DialogSetFlagInvalidIdFormalValidationTests.cs` 1 个 `[Fact]` + 1 个 `[Theory]`（5 条 `[InlineData]`）= 6 个测试用例；合计 11+6+6=23，与任务书给出的线索数字"(11+6+6)"一致。 | 独立走查确认 `IsValid` 未扩大捕获范围；为覆盖正式装配端到端行为新增的 `Tests.Gameplay.csproj` 对 `Presentation.Common` 的测试专用引用只出现在测试专用 `<ItemGroup>` 节内，未改变生产装配的层次依赖方向（已核对该 csproj 的实际 diff）。核实过程中特别注意：若只按 `[Fact]`/`[Theory]` **属性个数**简单计数（不展开 `[InlineData]`），会得到 6/3/2 这组不同的数字——本文档采用与任务书线索一致的口径（按 xunit 实际生成的测试用例数计），两种计数口径都如实记录在此，避免后续读者按错误口径复核时产生"数字对不上"的误会。 |
| P3 `RewardSchemaFields` 注释过时 | **成立（文档层），已勘误** | `core/gameplay/common/contracts/RewardSchemaFields.cs:9-13` 注释仍称 `FieldSchema` 不能表达嵌套，与当前 Object/复合 schema 登记能力（ADR-0019）冲突。预期：改为描述当前登记范围、Object/union 边界和未登记字段，不能用注释掩盖 validator 运行现状。 | `core/gameplay/common/contracts/RewardSchemaFields.cs`（注释改写）——提交 `89b1b69` | 走查该提交在此文件的实际 diff：只涉及注释块（43 行改动均为注释文本，无可执行代码变更），`dotnet build` 无警告无错误（见下方"全量门禁"）。 | 独立走查确认改写后的注释准确描述当前状态：`FieldSchema` 自 ADR-0019/F1b 起已支持 `Fields`/`Item`/`Variants`，能表达本类型描述的嵌套结构；注释同时澄清本类型 `Rewards` 帮助方法目前无调用方（保留是为了不破坏"三表共用同一入口"的公开 API 形状），以及 `world_flags[].value` 联合类型校验、`items`/`currency` 数值范围校验为何仍留在解析期/各表 `ContentValidationRule` 而非 `FieldSchema` 登记阶段——与线索描述的"改为描述当前登记范围"要求相符，未发现遗留的过时表述。 |
| 套装缓存候选（Equipment set cache，未确认） | **候选确认为真实缺陷，已修复** | 存在静态审查候选（缓存模式与本轮已根治的 CORE114-03 StatHost 同构），但本轮审计没有完成合法 `item.template` 引用下 resident equip→reload→unequip 的独立负例，不能列为确认 P2，只作候选登记。预期：若排查后确认存在同构缺陷，比照 CORE114-03 的处理方式失效/重算缓存并补独立负例测试；若排查后确认不存在，记录排查结论与依据。 | `core/carriers/item/core/EquipmentHost.cs`：`RecomputeSetBonuses`（880 行起）新增遍历新定义之前先扫描 `_appliedSetBonuses` 中已记录、但新 `bonuses` 里已不存在的"孤儿门槛"并无条件释放光环句柄的逻辑——提交 `89b1b69` | 整合阶段独立重跑 `dotnet test`：全绿（同上）；独立数出 `core/carriers/item/tests/CORE114_EquipmentSetBonusReloadOrphanTests.cs` 实际为 2 个 `[Fact]`，与任务书给出的线索数字一致；未在本次整合中回退代码重新验证"修复前 FAIL"（会引入额外风险且六工程全绿已确认修复后行为正确），采信提交信息自述的探针复现描述作为"修复前确实存在此缺陷"的证据，但已独立走查代码逻辑确认修复后能正确覆盖该场景。 | 排查结论：**候选成立**——`EquipmentHost.RecomputeSetBonuses` 此前只按当前 `item.set` 定义（`bonuses`）逐门槛比较 `isApplied`，天生看不到 `_appliedSetBonuses` 里已 applied、但新定义里已不存在这个门槛 key 的情况，与 CORE114-03（StatHost 派生缓存未随定义变化重算）属同一类"定义变了、派生的运行期簿记未跟着失效"的问题，已比照处理方式修复。已知局限如实记录：孤儿门槛的清理只在 `RecomputeSetBonuses` 被调用时发生（装备/卸下变化，或 `ReapplySetBonuses` 跨图重放时），reload 那一刻本身不会主动为全部已注册单位重算——这一点与 CORE114-03（StatHost 在 `Reload` 内部主动为全部已注册单位立即重算）在"收敛时机"上不完全同构：StatHost 是 reload 当下主动收敛，EquipmentHost 是下一次相关操作时才收敛。核实时特意指出这一差异，不因两者"同构"的整体判断而掩盖具体收敛时机上的不同，供后续如果需要更严格保证时参考。 |

## 全量门禁验收结果（整合阶段，2026-09-10）

1. `dotnet build Core.sln -c Release --nologo --artifacts-path <scratchpad>/build_wd`：0 警告、
   0 错误。
2. `dotnet test Core.sln -c Release --no-build --artifacts-path <scratchpad>/build_wd`：六个测试
   工程全绿——Tests.Foundation 748、Tests.Numbers 127、Tests.Carriers 385、Tests.Rules 439、
   Tests.PresentationCommon 548、Tests.Gameplay 650，合计 2897 个测试全部通过，0 失败、0 跳过。
3. `python -m pytest toolchain/tests/test_abi_probe_outdir_safety.py toolchain/tests/test_abi_surface_compare.py -q`：
   13 passed。
4. PJ114-01 独立复现（见上表判断记录）：基于 `docs-project/api-compat/run-skillhost-abi.ps1`
   同一方法论，用本轮修复后重新构建的 Core DLL 替换未重新编译的 consumer 程序集，退出码由 11
   （修复前，对未打补丁的 `dist/ws-game-1.14.0.zip`）变为 0（修复后）。
5. 本文档未执行的项（留待步骤 6，不在本次整合范围内提前断言）：`toolchain/abi_probe.ps1`
   完整门禁路径实跑（含基线包 1.12.0/1.13.0 下的 consumer probe 与 `abi_surface` compare）、
   `check.ps1` 全量步骤（含 Unity EditMode/PlayMode）、`validate_data.py`/`--schema-audit`、
   `python -m pytest toolchain/tests` 全量（本次只重跑了与本轮改动直接相关的两个测试文件，未跑
   全部 132+ 个用例）。这些项目留给"步骤 6"（全量提交门槛脚本）统一执行并记录结果。

## 异常与判断记录

- **PJ114-01 消费方程序集 SHA256 数值核正**：本次整合任务书给出的核对值
  `f523296d9545eb705e7815c8468ac738c3f486c2d3b54db1e170a92b5f2dbf99` 与审计归档四处一致记录的
  `13500df60474618a7960649f8ff1241a2cad460cfe576e30bbd033fd5457e00a` 不一致；已逐一核对
  `AUDIT_REPORT.md:28`、`docs-project/project-findings.md`（PJ114-01 一节）、
  `docs-project/validation.md:55`、`docs-project/logs/api-compat/api-compat.log` 四处口径一致，
  判定任务书给出的哈希系转录笔误，本文档统一以审计归档记录的 `13500df6...` 为准，已在上表判断
  记录中说明，未按错误值誊抄。
- 其余条目均判定为"成立"，无"部分成立"/"不成立"的情形。
- CORE114-04 测试数量存在两种计数口径（按 `[Fact]`/`[Theory]` 方法数 6/3/2，按 xunit 实际生成的
  测试用例数 11/6/6），已在上表判断记录中同时记录，避免歧义。
