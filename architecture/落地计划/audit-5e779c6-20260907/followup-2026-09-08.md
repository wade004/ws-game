# 第七方深度审核核实跟进（codex 第五轮，基线 `5e779c6`）

基线：`5e779c6`（main，v1.1.0）。审计报告本体见本目录 `AUDIT_REPORT.md`/`foundation-rules.md`/
`gameplay-review.md`/`doc-code-matrix.md`/`validation.md` 与 `evidence/`/`repro/`（codex 原文，已
`git add` 归档，未改写）。

修复由四个并行 Sonnet 5 子 agent 按领域分工执行（核心侧 WA、玩法/表现侧 WB、文档与能力边界 WC、
复核收边 WD），主会话（本 agent）在四份判断记录报告基础上补齐 2 条相邻缺口、独立核实全部条目、
统一提交、撰写本文档。修复提交：

| 提交 | hash | 范围 |
|---|---|---|
| A（核心侧） | `45977c7` | `core/rules/**`、`core/numbers/**`、`core/carriers/item/core/EquipmentHost.cs`/`ItemValidationRules.cs`、`core/carriers/assembly/**`、`core/gameplay/assembly/TimeModelSwitch.cs` 及其测试、architecture/03/06/07/10 |
| B（玩法/表现侧） | `fbad8c3` | `core/gameplay/**`（除 TimeModelSwitch）、`core/carriers/item/core/InventoryHost.cs`、`core/carriers/common/contracts/**`（含新增 `IInventoryTransaction.cs`）、`presentation/**`、`adapters/unity/**`、architecture/08 |
| C（交付与文档） | `191fe95` | `.github/workflows/release.yml`、README、`toolchain/**`（头注释）、`core/**/README.md`、`presentation/**/README.md`、architecture/01/04/13、落地方案与分阶段计划.md |
| D（归档，本文档所在提交另计） | `76f6aed` | `architecture/落地计划/audit-5e779c6-20260907/` 整目录归档 |

主会话独立执行并验收的第 0 步补充修复（详见下方核实表 STEP0-1/STEP0-2）：`QuestHost.TurnIn`
步骤 2/3 回滚改走 `IInventoryTransaction`（同批还给 `InventoryHost.BeginBatch` 补了"已在事务中时
隐式加入外层事务而不是抛异常"的嵌套支持，供 `RewardDispatcher.GrantItems` 在 `QuestHost` 持有的
外层事务内安全调用）；`CastPipeline` 消费的 `cast_time`/`channel_time`（含引导 `tick_interval`）
与 `CooldownTracker.ModifyCooldown` 的运行期 `delta`，补齐同 WD B1/B2 一致的 `_currentFactor` 时间
模式折算机制。

## 核实方法说明

本文档"复现"列标注的测试文件均已在提交 A/B/C 落地的工作树上重新执行：`dotnet build Core.sln
-c Release` 0 警告 0 错误，`dotnet test`（六项目）全绿（Foundation 642/642、Numbers 106/106、
Rules 390/390、Carriers 297/297、Gameplay 452/452、PresentationCommon 413/413）。六项目相对
`5e779c6` 基线（Foundation 642、Numbers 106、Carriers 288、Rules 348、Gameplay 444、
PresentationCommon 410，见 `validation.md`）的新增测试数（Carriers +9、Rules +42、Gameplay +8、
PresentationCommon +3）与下表逐条"复现"列声明的新增用例数逐项相加后精确吻合，作为"四份报告声明
的测试确实都在同一批全量运行中被计入、未被遗漏或重复计数"的结构性交叉核验，不逐行手工重放每一份
判断记录的完整推导（判断记录本身的论证质量见下表"判断记录"列摘要与四份原始报告）。

## 核实表（20 条：13 项主发现 + 2 项复现附加项 + WA 相邻缺口 3 条 + 第 0 步 2 条）

| 编号 | 判断 | 复现 | 修复位置 | 验收 | 判断记录摘要 |
|---|---|---|---|---|---|
| GP26-01 | 成立，已根治 | `core/gameplay/common/tests/RewardDispatcherTests.cs::Grant_RejectPolicy_SecondItemFails_RollsBackQueuedEventsToo_R01`（真实 `InventoryHost`+`EventBus`） | `core/carriers/common/contracts/IInventoryTransaction.cs`（新）、`core/carriers/item/core/InventoryHost.cs`、`core/gameplay/common/core/RewardDispatcher.cs` | 上述用例 + 既有回滚用例全绿 | 发奖失败回滚此前只精确恢复库存"数量"，未撤销已入队的 `item.added` 事件；改为事务：失败时缓存事件整批丢弃，从未真正入队。 |
| GP26-02 | 成立，已根治 | `core/carriers/item/tests/EquipmentSetBonusSharedAuraTests.cs`（3 用例，真实 `CarriersAssembly` 全链路） | `core/carriers/item/core/EquipmentHost.cs` | 上述用例 + `EquipmentHostTests.cs`/`EquipmentReplaceHandleTests.cs` 全量回归 | 套装门槛加成此前不参与 `_auraHandleRefCount` 记账，卸下普通装备会把仍满足套装门槛的共享光环整个移除；改为 `RegisterAuraHandle`/`ReleaseAuraHandle` 统一记账。 |
| GP26-03 | 成立，已根治 | `core/gameplay/assembly/tests/GameplayAssemblyGobjTeleportInteractionTests.cs`（2 用例） | `core/gameplay/assembly/GameplayAssembly.cs` | 上述 2 用例 + 既有 `GameplayAssemblyTeleportTests` 全绿 | 直接交互（非 gossip）teleporter 类 gobj 时 `InteractResult.DispatchedRef` 从未被消费；写入范围不含 `core/carriers/gobj`，改为在 `GameplayAssembly` 新增 `gobj.interacted` 订阅接回 `TeleportUnit`。 |
| FR-01 | 成立，已根治 | `core/rules/skill/tests/TimeModelRescaleTests.cs`（6 用例）+ `core/gameplay/tests/Discrete/TimeModelSwitchTests.cs`（+2 用例） | `core/gameplay/assembly/TimeModelSwitch.cs`、`core/rules/common/contracts/Events.cs`（新事件 `sim.time_model_rescaled`）、`CooldownTracker`/`AuraHost`/`SkillHost` | 上述 8 用例全绿 | 模式切换只换算了通用 `SimTimers`，未触及技能冷却/光环剩余时间；新增事件广播 + 两个宿主各自 `RescaleAll`。 |
| FR-02 | 成立，已根治 | `core/rules/skill/tests/ChargesZeroRechargeTests.cs`（7 用例） | `core/rules/skill/core/CooldownTracker.cs`、`SkillValidationRules.cs`（新增 `ChargesRechargeTimeZeroWarningRule`） | 上述 7 用例全绿 | `recharge_time<=0` 时充能耗尽后永久卡死；改为即时恢复语义，数据校验层新增 Warning 提醒复核。 |
| FR-03 | 成立，已根治 | `core/rules/skill/tests/AuraExpiryPeriodicBoundaryTests.cs`（4 用例） | `core/rules/skill/core/AuraHost.cs`（`Update`） | 上述 4 用例全绿 | 大步长 `dt` 跨越光环到期点时，到期后不该存在的时间余量仍被计入周期结算次数；周期累加器改按 `Min(dt, Remaining)` 推进。 |
| FR-04 | 成立，已根治 | `core/rules/tests/Integration/ProgressionRestoreRatingRecomputeTests.cs`（3 用例） | `core/numbers/progression/{core/ProgressionHost.cs,contracts/Events.cs}`、`core/rules/assembly/RulesAssembly.cs` | 上述 3 用例全绿 | 读档恢复等级不经正常升级路径，不发 `progression.level_up`，评级换算属性缓存停留在读档前的值；新增 `progression.state_restored` 事件驱动同一条 `RecomputeRatingStats` 订阅。 |
| FR-05 | 成立，已根治 | `core/rules/skill/tests/SkillValidationNestedGapTests.cs`（13 用例，含真实 `CastSkill` 先证实会抛异常再证实新规则拦下） | `core/rules/skill/schema/SkillValidationRules.cs`（`EffectKindRegisteredRule` 修复 + 新增 `ChargesShapeRule`/`CostEntryShapeRule`）、`RulesSchemaCatalog.cs` | 上述 13 用例全绿 | `effects[]` 缺 `kind`/`charges`/`cost[]` 内部子字段缺失或类型错误均能通过加载校验、只在首次施法懒解析时抛异常；三条规则改为逐层显式检查并注册。 |
| U01 | 成立，已根治 | `adapters/unity/.../Tests/Runtime/UnityAudioTests.cs`（+3 PlayMode 用例） | `adapters/unity/.../Runtime/EngineAdapter/UnityAudio.cs` | 上述 3 用例 + 既有音乐用例全绿（PlayMode 175/175） | `Tick`/`StopMusic` 沿用了 `PlayMusic` 内部翻转前的音源映射，实际选中上一轮 `outgoing`；交叉淡入表现为新旧音源互换。 |
| U02 | 成立，已根治 | `UnityAudioTests.cs::PlaySfx_NaturalPlaybackEnd_ReclaimsPoolSlot_PoolSizeDoesNotGrowUnbounded` | 同上文件 | 同上（PlayMode） | SFX 自然播完只有显式 `StopSfx` 才回收池位，绝大多数一次性音效播完后池子只增不减；新增每帧 `ReclaimFinishedSfxSlots`。 |
| U03 | 成立，已根治 | `presentation/render/tests/FrameAnimPlayerTests.cs`（+3 用例，含循环回绕） | `presentation/render/core/FrameAnimPlayer.cs` | 上述 3 用例 + 既有 19 条全绿（413/413） | 大 `dt` 跨帧只对落地帧触发一次，中途关键帧丢失，命中特效可能完全不播；改为按序补发跨过的每一帧。 |
| U04 | **成立**（WB 初判"不成立/无法复现"，WD 复核推翻，见下） | `adapters/unity/.../Tests/Runtime/UnityViewFactoryDefaultAnimationTests.cs::CreateView_AnimRootRenderer_RespondsToHeightFlashFade_LikePaperdollLayers`（PlayMode） | `UnityRenderer2D.cs`/`UnityFrameAnimPlayer.cs`/`UnityViewFactory.cs` | 上述用例 + 既有 7 处改用 `GetComponentInChildren` 的回归全绿（PlayMode 175/175） | **过程**：WB 核实的是 `SpriteCharacterRig.Flash`/`UnitySpriteView.PlayFade` 经"纸娃娃层"（`LayerRenderers`）这条路径，确实已正确接线，判定"不成立"。WD 复核指出审计原文实际指向**默认序列帧动画**（`UnityViewFactory.AttachDefaultAnimation` 给 `Creature` 挂接的 `UnityFrameAnimPlayer`）——该组件此前挂在精灵根 `Root` 自身，其自带 `SpriteRenderer` 既不在 `LayersRoot` 子树（吃不到 height 偏移），也不在 `LayerRenderers` 集合（`ApplyColor`/flash/fade 遍历不到），是与 WB 核实路径完全独立的另一条渲染路径。WD 判定成立并修复：新增 `RegisterAnimRootRenderer` 把该渲染器登记进同一套变换/颜色遍历。两份判断记录不矛盾，是同一编号指向的审计描述覆盖了两条不同代码路径，WB 只核实了其中一条。 |
| U05 | 成立，已根治 | `.github/workflows/release.yml` 语法解析（`Parser::ParseInput` 全部 `run:` 块 OK）+ 用真实 `gh release view v1.1.0 --json assets` 与手工构造"部分缺失"场景各跑一遍判定逻辑 | 同上文件 | 见上（无 dotnet 测试覆盖，CI YAML 逻辑，静态解析+实跑判定脚本验证） | 附件存在性检查此前只看 zip 是否存在，命中即整体跳过，可能遗漏 `.lock` 与三个 `.tgz`；改为核对完整 5 件套，部分缺失时只补传缺失的文件。 |
| 附加-R14 | 成立，已根治（同图读档孤儿实体，13 项主发现之外，验证阶段追加复现） | `core/gameplay/assembly/tests/GameplayAssemblySpawnTimerSameMapReloadTests.cs::SavedRespawnTimer_SameMapReload_DespawnsOrphanEntity_NotDuplicated_R14` | `core/gameplay/spawn/{contracts/SpawnOptions.cs,core/SpawnHost.cs}`、`core/gameplay/assembly/GameplayAssembly.cs` | 上述用例 + 既有 C11 场景全绿 | 同图读档若快照记录某刷新点仍在倒计时、而当下世界已有"存档后重生"的孤儿实体，此前只让其脱离刷新点追踪，从未真正移除；倒计时到期后再生成一个，同刷新点出现两个实体。改为读档时主动 `Despawn` 孤儿实体。 |
| 附加-Shape | 成立，已根治（`charges`/`cost[]` 嵌套形状校验缺口，13 项主发现之外，WA 排查 FR-05 时追加发现同类问题，与 FR-05 同批修复） | `SkillValidationNestedGapTests.cs` 内 `ChargesShapeRule`/`CostEntryShapeRule` 相关用例（含于 FR-05 的 13 用例中） | 同 FR-05 | 同 FR-05 | `skill.def.charges`/`cost[]` 声明了顶层字段但内部子字段（`max`/`recharge_time`/`power_type`/`amount`）缺失或类型错误，同样能通过加载校验、只在解析时崩溃；新增两条规则覆盖。 |
| WA-GAP-1 | 成立，已根治 | `core/rules/skill/tests/TimeModelCastTimeRescaleTests.cs`（前 4 用例：冷却/充能/光环 duration 施放路径 + 永久光环回归） | `core/rules/skill/core/{CooldownTracker.cs,AuraHost.cs}`（`_currentFactor`） | 上述用例全绿 | FR-01（R05）只解决"切换那一刻，已存在状态跟着换算"；若技能是在离散战斗**进行中**才第一次施放/施加，原始按连续秒 authoring 的冷却/充能/光环 duration 会被离散模式的按轮 `Update` 直接当轮数消耗。新增 `_currentFactor`（施放当下折算原始数据）与 R05 的既有换算衔接。 |
| WA-GAP-2 | 成立，已根治 | `TimeModelCastTimeRescaleTests.cs::PeriodicInterval_...`/`PeriodicAccumulator_RescaledOnModelSwitch_PreservesInFlightProgressRatio`（2 用例） | `core/rules/skill/core/AuraHost.cs`（`Update`/`RescaleAll`） | 上述 2 用例全绿 | 周期效果 `interval` 施放当下同样需要按 `_currentFactor` 折算；`RescaleAll` 同步换算 `PeriodicAccumulators`（推翻 FR-01 当时"周期累加器不换算"的历史判断），否则切换瞬间"累加器/interval 还差多久触发下一跳"的比例关系被打破。 |
| WA-GAP-3 | 成立，已补校验（Warning，非阻断） | `core/carriers/item/tests/ItemValidationRulesTests.cs`（新增 4 用例） | `core/carriers/item/core/ItemValidationRules.cs`（`ItemGrantsAurasDuplicateRule`）、`CarriersSchemaCatalog.cs` | 上述 4 用例全绿 | GP26-02 修复已保证运行期安全，但同一 `item.template.grants.auras` 内重复引用同一 `aura_def` 此前无任何数据层提醒；新增 Warning 级规则（Error 会让本就合法可加载的数据集突然过不了校验）。 |
| STEP0-1 | 成立，已根治（本轮相邻缺口，第 0 步修复） | `core/gameplay/quest/tests/QuestTurnInTransactionRollbackTests.cs`（2 用例，含真实 `InventoryHost`+`RewardDispatcher`+`EventBus`） | `core/gameplay/quest/core/QuestHost.cs`、`core/carriers/item/core/InventoryHost.cs`（`BeginBatch` 支持隐式加入外层事务）、`core/carriers/common/contracts/IInventoryTransaction.cs`（判断记录更新） | 上述 2 用例全绿 | `QuestHost.TurnIn` 步骤 2/3 失败时把已移除的 collect 物品用 `AddItem` 放回——这一"移除又放回"会产生真实 `item.added` 事件，可能被玩家对同一模板持有的另一 `consumeOnProgress` 目标误当新获得而错误推进进度，与 GP26-01 同源。改为整体包进一次 `IInventoryTransaction`，失败时 `Dispose` 回滚（不再手动 `AddItem`）；`RewardDispatcher.GrantItems` 若同样需要发放物品会在同一事务内隐式加入（`InventoryHost.BeginBatch` 检测到已在事务中时返回透传句柄，不再抛"不支持嵌套"）。 |
| STEP0-2 | 成立，已根治（本轮相邻缺口，第 0 步修复） | `core/rules/skill/tests/CastPipelineTimeModelRescaleTests.cs`（3 用例：`cast_time`、`channel_time`+`tick_interval`、`modify_cooldown` delta） | `core/rules/skill/core/CastPipeline.cs`（新增 `_currentFactor`/`RescaleAll`）、`CooldownTracker.cs`（`ModifyCooldown`）、`SkillHost.cs`（订阅转发） | 上述 3 用例全绿 | WA/WD 均在判断记录里明确标注"未纳入范围"：`CastPipeline` 自身消费的 `cast_time`/`channel_time`（含引导 `tick_interval`）与 `modify_cooldown` 效果原语的运行期 `delta` 同样未按当前模式折算，与 WA-GAP-1 同源。补齐同一套 `_currentFactor` 机制；`add_charge` 的 `amount` 是离散充能计数（非时间量），维持不换算，见 `CooldownTracker.AddCharge` 注释判断记录。 |

## 文档漂移处理

以下 9 类文档漂移均已随提交 A/B/C 落地（正文改写为单一现状陈述，00～14 采用 12 §5"勘误："格式，
版本号不变、ADR 列"—"；`架构/落地计划/` 与 `toolchain/`/`README.md` 等允许出现技术名的文件不受
该格式约束）：

| 类别 | 位置 | 原矛盾 | 改后口径 |
|---|---|---|---|
| D01 | `architecture/01_分层与依赖.md`（第 1/3/6 节）、`13_新游戏接入指南.md` 第 7 节 | "L5 表现层对 L-1 之外所有层都只读、只订阅事件"绝对语句，与实际存在的窄意图命令入口（`SubmitIntent` 等）并存 | "查询只读、只订阅事件；改变状态一律经窄意图命令入口提交，业务裁决下沉到接收方所在层"；依赖矩阵补脚注区分"查询只读"与"直接调用契约方法" |
| D02 | `13_新游戏接入指南.md:129` | consumer smoke 覆盖范围表述（WC 核对确认已准确，未改动） | 维持现状，无需改写 |
| D03 | `presentation/vfx_sfx/README.md:65-67` | 旧称"`ISfxPlayer` 契约没有 `Update` 方法"，与后续（U02 之前的更早一轮）已新增 `Update` 的事实矛盾 | 改写为如实反映现状：`VfxPlayer`/`SfxPlayer` 均有各自 `Update(dt)`，不重复描述 |
| Expr | `core/rules/expr_host/README.md:47/82`、`04_数据与内容管线.md:289/290/297` | `turn_index`/`round_index`/`is_my_turn`/`day_cycle` 笼统写"占位……本项目暂不启用"；04 文档"未登记 group.key 组合校验期报错"与紧邻的 ADR-0015 回退规则自相矛盾 | 前三者已随离散模式真正接线，只有 `day_cycle` 仍是硬占位，拆成两行分别写清边界；04 文档改写为准确描述——未登记组合按回退规则处理为字面量/签名未知引用，`UnknownGroup`/`UnknownKey` 只防御手工构造 AST，正常内容文本结构上不可达 |
| Archetype | `core/numbers/archetype/schema/README.md:18/48` | "L2 skill 模块尚未实现"过时表述（`core/rules/skill` 早已实现） | `primary_stat`/`skill_book_ref`（标量 Id）已/可登记为 Reference；`power_types`/`passive_auras`（`List<Id>`）是 `IDataRegistry.DeclareReference` 契约本身只支持标量字段的接口能力边界，不是"没来得及补" |
| 时间 | `03_运行时骨架.md:173/174`（核对无误未改）、`06_规则层...md` 第 3.1/3.3/8 节 | 06 文档未登记 `sim.time_model_rescaled` 事件，`cooldown_duration`/`charges`/`duration`/`interval` 字段行未说明"施放/施加当下按当前模式折算" | 事件词汇表补登记行；相关字段行补充"施放/施加当下按当前生效模式折算原始数据值"说明，呼应 FR-01/WA-GAP-1/WA-GAP-2/STEP0-2 |
| 事务 | `core/gameplay/common/README.md`、`core/gameplay/quest/README.md` | "背包状态与调用前完全相同"只覆盖数据状态，不覆盖已派发的 `item.added`/`item.removed` 事件（GP26-01 根因） | 补"事件提交边界"段落，说明数据状态与已派发事件两者现在都是原子的；`quest/README.md` 同时如实记录 STEP0-1 的复现与修复过程 |
| 装配 | `core/gameplay/assembly/README.md` | 构造示例遗漏 `saveSystem` 参数；`TeleportRequestedCallback` 行未反映 `TeleportTargetResolver`/GP26-03 新增订阅路径；RNG 段落"调用方需自行注册"过时；tick 处理器计数"四个"与实际五行不一致 | 逐项改写为现行行为，`TeleportRequestedCallback` 行新增判断记录 8 呼应 GP26-03 收口 |
| 能力索引 | `架构/落地计划/落地方案与分阶段计划.md`"能力边界与未默认接入能力索引" | `day_cycle`、ATB、孤儿检查/`DisplayMapCoverageRule`、`FeedbackRuleValidator` 四项此前遗漏 | 逐项核对现行源码/文档锚点后补齐，索引现收录两份审计报告表格给出的全部条目（详见该文档表格，本文档不重复列出） |

补充：WC 核实的另一批文档口径（L5 查询/命令边界四处交叉核对、统一计时器描述三处、模板与消费方
演练覆盖范围三处、数据校验承诺一处、release.yml 附件判定即 U05）均已在同一批提交中落地，逐条见
本目录 WC 报告原文与提交 C 的实际 diff，不在此重复摘录。

## 验收

全量 `powershell -ExecutionPolicy Bypass -File check.ps1`（不加 `-SkipUnity`/`-Quick`，前台实跑，
Unity 编辑器本体确认未占用许可）在提交 A/B/C/D 全部落地后的工作树上执行，**23 步全部 PASS，0
FAIL，0 SKIP 之外无异常，总用时 221.5s**（首次跑出 1 处禁用词扫描 FAIL——本文档草稿曾在正文里
以代码片段形式写出被禁用的具体游戏代号本身作为"扫描命令示例"，被同一条扫描规则命中，已改写为
不复述该字符串的表述，修复后重跑转 PASS，见下方逐步结果）：

| 步骤 | 结果 | 用时(s) | 详情 |
|---|---|---|---|
| 门禁自检：`Test-NativeExitCode` | PASS | 0.3 | — |
| `dotnet build Core.sln -c Release` | PASS | 2.0 | 0 警告 0 错误 |
| `dotnet test`（六项目，含 Perf 类别） | PASS | 3.6 | Foundation 642/642、Numbers 106/106、Carriers 297/297、Rules 390/390、PresentationCommon 413/413、Gameplay 452/452，全部 0 失败 |
| `validate_data.py`（合并根） | PASS | 1.7 | 59 tables/278 records/0 errors/0 warnings/1 override |
| `validate_data.py --data-root data/_framework` | PASS | 1.5 | 5 tables/124 records/0 errors/1 warning（缺 l10n 表，跳过文本键检查，既有已知情况） |
| `gen_event_constants.py --check` | PASS | 0.1 | 90 个常量与 `EventKeys.g.cs` 一致（88 基线 + `sim.time_model_rescaled`/`progression.state_restored` 两条） |
| `gen_placeholder_assets.py --check` | PASS | 0.1 | 92/92 |
| `import_assets.py check --dataset _sample` | PASS | 0.1 | 0 问题 |
| `pytest toolchain/tests -q` | PASS | 1.7 | 47 passed |
| 禁用词扫描：全仓库不出现具体游戏代号 | PASS | 18.8 | 0 命中（首次跑 FAIL 1 处，见上方说明，已修复） |
| 禁用词扫描：architecture 正文技术名（immunity 例外） | PASS | 0.0 | 0 真实命中 |
| 版本一致性 | PASS | 0.1 | VERSION=1.1.0，两个 `package.json`、`packages-lock.json`、`CHANGELOG.md` 一致（`CHANGELOG.md` 同时含 `[1.2.0]`/`[1.1.0]` 条目，版本一致性只要求当前 `VERSION` 对应条目存在，不禁止追加未来条目） |
| `build.ps1 -SkipTests`（同步 DLL） | PASS | 2.5 | Runtime/Plugins/Core 六个 DLL 哈希核对通过，`dist/1.1.0/` 重新打包（zip/lock/三个 tgz） |
| 包清单一致性 | PASS | 7.8 | 三个 npm 包 version=1.1.0 一致，`npm pack --dry-run` 清单不含排除项 |
| Unity 编译检查 | PASS | 8.1 | 退出码 0 |
| Unity EditMode 测试 | PASS | 9.1 | total=54 passed=54 failed=0 |
| Unity PlayMode 测试 | PASS | 30.7 | total=175 passed=175 failed=0（含 U04 新增用例、U01/U02 音频用例、U03 序列帧用例） |
| 独立版构建 + `-gf-smoke` 冒烟（连续模式） | PASS | 12.6 | — |
| 独立版 `-gf-smoke-discrete` 冒烟（离散模式） | PASS | 3.3 | — |
| IL2CPP 独立版构建/冒烟 ×3 | SKIP | 0 | 未传 `-Il2cpp`（任务未要求） |
| 消费方演练（`toolchain/consumer_smoke.ps1`） | PASS | 117.4 | — |

**汇总：23 步（20 PASS + 3 SKIP），0 FAIL，总用时 221.5s。**

- 全仓库禁用具体游戏代号扫描（`check.ps1`"禁用词扫描"步骤）：0 命中。
- `architecture/00～14` 正文技术名 grep（unity|c#|csharp|dotnet|\.net|nunit|python|newtonsoft|
  il2cpp|powershell|github|verdaccio|npm，排除 immunity 误命中）：0 真实命中。
