# 文档代码一致性审计（2026-09-06）

> 本文件允许出现具体技术名（属于"落地计划"目录，见 CLAUDE.md 硬性规则）。本轮任务：对
> `architecture/00～14`、`README.md`、`adr/`、`选型/`、`落地计划/`、各层 `README.md`
> （`data/README.md`、`games/_template/data/README.md`、`presentation/README.md`、
> `core/*/README.md`）做全覆盖的文档代码一致性深度检查，相对
> [文档代码一致性审计_2026-09-05.md](./文档代码一致性审计_2026-09-05.md) 新增覆盖
> `00`/`02`/`05`/`12`/`13`/`14`/`README.md`/`adr/`/`选型/`，以及收边 I3、加固 J1/J2/J3
> 与本次任务开始时工作树里尚未提交的改动引入的变化。审计分四组并行执行（A：README/00/12/13/14/
> adr/选型/落地计划；B：01/02/03/10；C：04/05/07/08/data README；D：06/09/11 增量 +
> presentation 与 core 各层 README + 代码合规抽查），本文件是四组结论的合并收口，并补一轮
> 全量终扫（硬性规则扫描、变更记录规范检查、事实一致性抽检）。
>
> 口径沿用 2026-09-05 审计：**已修**＝已按
> [12_扩展与变更流程.md](../12_扩展与变更流程.md) 第 5 节做细节勘误/补充（版本号不变，变更记录加
> 一行）；**无需修**＝核对后文档与实现一致，未改动；**代码待补**＝文档描述的目标形态正确，实现
> 尚未跟上，本次不改代码，只记录；**需拍板**＝文档与实现两条处理路径（改文档 / 改代码）都成立，
> 涉及可能改变已定稿结论的判断，留待设计层裁决，本次均未改动任何文件。
>
> 本次实际写入范围（仅此三处，见任务书）：①本文件；② `architecture/04_数据与内容管线.md`
> 变更记录 2026-09-05 行末尾追加一句更正性说明（不改历史行本身，见五节文件清单）；③不改动
> 其他任何文件。`architecture/01_分层与依赖.md` 的 L5 `shell`/`ui` 两行事件订阅/契约接口名列
> 由另一会话并行处理中，本次未触碰，相关发现记入四节"需拍板清单"并注明"另一会话任务进行中"。

## 一、逐文档审计结果

### `00_架构总则.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §名词表 vs `adr/0001～0016` | 无需修 | 名词表 40+ 条与 11 条设计原则、ADR-0012～0015 新增术语（CharacterRig/IRenderer3D/ICamera/Socket/Slot/AnimSet/TimeModel/SimStep/TurnScheduler/PacingPolicy/Initiative/ActionPoints）逐一核对一致，无遗漏无冲突。 |
| §设计原则 11 条 vs `adr/0001～0016` | 无需修 | 一一对应；ADR-0016 不是"设计原则"类 ADR（属 12 §5"文档与实现不一致"处理产生的契约级 ADR），不需要在此新增第 12 条原则。 |
| §2.2 非目标 vs `ADR-0012～0016` | 无需修 | 逐条核对：仅"3D 渲染"一行因 ADR-0012（可选 model 型外形）此前已同步改写为"排除脱离约定的全三维场景/碰撞/导航"的限定表述；ADR-0013/0014/0015/0016 均未触及任何一条非目标的成立前提。 |

### `01_分层与依赖.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| "实现级共享目录"段各层组装根 | **已修** | 原文列出的 `core/foundation/assembly`、`core/numbers/assembly` 两条实际不存在（`find core -iname assembly` 只有 `core/carriers/assembly`、`core/gameplay/assembly`、`core/rules/assembly` 三个物理目录），已改写为累加式描述：`core/rules/assembly` 承担 L0～L2 装配与表结构登记（`RulesSchemaCatalog.cs:59-61` 依次调用 `RegisterL0Schemas`/`RegisterL1Schemas`/`RegisterL2Schemas`）；`core/carriers/assembly` 承担 L0～L3（`CarriersAssembly.cs:29`）；`core/gameplay/assembly` 承担 L0～L4（`GameplayAssembly.cs:53`）；`presentation/assembly` 承担 L0～L5（`PresentationAssembly.cs:123-124`）。 |
| §3 依赖矩阵下澄清 | **已补** | 补一句：矩阵约束逻辑层调用纪律而非物理程序集引用清单，`Adapter.Unity` 单一 asmdef 同时承载 L-1（`EngineAdapter/` 仅 `using Core.Foundation`）与 L5 引擎侧（`Presentation/Ui/Shell` 引用 `Carriers/Gameplay/Presentation`），构建级依赖为两层允许依赖之并集。 |
| L3 载体层模块表 | **已补** | 补 `projectile`（抛射物）一行：目录 `core/carriers/projectile`；契约 `ProjectileHost`，实现声明于规则层公共契约 `IProjectileSpawner`（依赖倒置）；无独立数据表，参数内联在技能 `projectile` 效果的 `params`；事件复用 `entity.created`/`entity.destroyed`；策略项含默认速度/射程/弧高/命中半径/命中标签/离散步等效秒。 |
| "实现级共享目录"段 | **已补** | 补 `adapters/conformance`（L-1 契约一致性场景库，46 场景，见 02 §2）。 |
| L5 `shell` 行订阅事件"不订阅 `save.completed`" | **未触碰（另一会话任务进行中）** | 09/D 组复核发现 `presentation/shell/core/ShellViewModel.cs:41-45` 实际订阅 `save.completed`/`save.loaded`（刷新存档槽摘要），与本行文字矛盾；本行由另一并行会话处理，本次按任务书要求未修改，已记入四节"需拍板清单"。 |
| L5 `ui` 行契约接口名列 | **未触碰（同上）** | `TurnStatusPanel` 例外架构（绕开 `UiPanel`/ViewModel 体系直连 `GameplayAssembly`/`UiIntents`）与 `IAudioLayerVolumeHost`/`ISkillBindingHost` 两个契约类型未列入本行，同属另一会话处理范围。 |
| 前次审计"待复核"标注（app_lifecycle/hook_registry/combat 等） | 无需清除 | `grep "待复核" architecture/01_分层与依赖.md` 零命中——该标注只存在于审计记录文件自身的表格结论列，不是写入 01 正文的标记，01 正文无需改动。 |
| L0～L4 模块表其余行（sim_loop/save_system/creature/gobj/summon/encounter 等） | 无需修（2026-09-05 已勘误的部分维持） | 复核 2026-09-05 已完成的逐行核对结论（`IPacingPolicy`、存档 meta 段措辞、`ICreatureTemplateQuery`、gobj 锁三种判定方式、`ISummonHost`、`EncounterHost`+`LevelHost` 等）仍与代码一致，本轮未发现新的分歧。 |

### `02_引擎适配层.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §1.8 `setBlocking` 语义 | **已修** | 补"整批替换而非追加，调用方自行维护完整阻挡集合"；`adapters/stub/StubNavigation2D.cs:152`、`UnityNavigation2D.cs:138` 均按整批替换实现，`UnityNavigation2DTests.cs:38` 有专门用例验证"第二次调用应整批替换、不是追加"。 |
| §1.9 `filter` 措辞 | **已修** | 改为"只承载中立的字符串标签匹配（包含/排除），标签语义由上层与登记方约定，接口本身不解释"；`core/foundation/engine_adapter/contracts/ISpatialQuery.cs:8-13` `QueryFilter` 类型注释原文即持此结论。 |
| 角度单位缺失 | **已修** | 接口清单前补一句："除 `ICamera.configure` 的 `pitchDegrees`/`yawDegrees` 外，全部角度类参数一律以弧度为单位"；`MovementTickHandler.cs:210/254/261`、`ProjectileHost.cs:121/197/245` 等 `Facing` 字段均用 `Math.Atan2`（弧度）赋值。 |
| §1.7 `getLoadProgress` 范围 | **已修** | 补"取值范围为 `[0,1]`"；`StubResourceLoader.cs:66-71`、`UnityResourceLoader.cs:247-252` 均落在该范围内。 |
| §1.6 内容根拓扑 | **已修** | 补"落地拓扑（单实例路径路由/双实例分工）由实现自选，调用方不得假设具体拓扑"；桩为单实例按前缀路由（`StubFileSystem.cs`），Unity 实现为双实例模式（`UnityFileSystem.cs`）。 |
| §2 契约一致性验证 | **已补** | 新增条款，中立表述描述"每接口至少一组与测试框架无关的场景、同一份场景源码驱动桩与真实引擎、允许因平台限制声明跳过但不得放宽断言"；`adapters/conformance/Runtime/*.cs` 共 46 个 `ConformanceScenario`（16 个文件逐一核对求和确认）。 |
| §4 迁移步骤第 3 步 | **已修** | 改为"复用已有契约一致性场景包一层薄壳并全部跑通，而非从零编写针对性测试"。 |
| ICamera 引擎实现 pitch/yaw 仅记录配置 | 无需修（实现进度，非文档矛盾） | 固定俯角正交近似，属实现选择，不构成文档与代码矛盾。 |

### `03_运行时骨架.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §3.1 步骤 1 触发点 | **已修** | "每次 `IClock.onFrame` 触发"改为"每次 `IClock.requestFixedStep` 的固定步回调触发"，并注明 `onFrame` 只供表现层插值使用；`GameFoundationBootstrap.cs:417-418` 分别持有 `RequestFixedStep`（驱动 `Gameplay.Advance`）与 `OnFrame`（供插值）两个独立句柄。 |
| §2 状态机表驱动 | **已补** | 补一段：默认转移表可由 `found.game_state` 数据表驱动/扩展，空表回退内置默认表；`AppStateMachineConfig.FromRegistry`（`core/foundation/app_lifecycle/contracts/AppStateMachineConfig.cs:179`）与对应测试、`data/_framework/found/found.game_state.json`（17 行）为证。 |
| §2 叠加规则措辞 | **已修** | 改为"框架默认已放行战斗中打开菜单叠加这一组，游戏层可扩展其余叠加关系"；`AppStateMachineConfig.cs:62,76` 默认表同时放行 `Explore→MenuOverlay` 与 `Combat→MenuOverlay`。 |
| §9 SimStep 阶段 | **已补** | 补一句：当前调度器只产出 `act` 阶段离散步，`turn_start`/`turn_end`/`round_end` 由对应事件承载；`TurnScheduler.cs:155` 唯一一处 `SimStep.Discrete(actor, StepPhase.Act)`，`Events.cs:19-21` 定义三个事件。 |
| §7 示例动作表 | **已补** | 补 `EndTurn` 一行；`data/_framework/found/found.input_action.json` 已有 `input.action.end_turn` 行。 |
| §6 加载失败分支 | **已补** | 补一句：加载失败只记诊断、请求回退 `MainMenu`，不发事件；`SceneRouter.cs:21-23,225,228` 为证。 |
| `InWorldSubState.cs` 过时注释 | 代码待补（不改文档） | 注释仍写"离散时间模型本项目暂不启用"，与实际已实现的离散调度矛盾，记入三节"代码不符设计清单"。 |
| `alpha` 插值返回值 Unity 参考实现未消费 | 无需修（实现选择） | 引擎侧改用自建 `_interpAccumulator`，只要视图插值效果等价，不要求逐字消费该返回值。 |

### `04_数据与内容管线.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §5 校验器检查项"外形映射存在" | **已修** | 补充说明该规则（`DisplayMapCoverageRule`）默认不随其余规则一并自动登记，需调用方显式声明参与覆盖检查的内容表来源，游戏接入时若要启用须自行显式登记（`PresentationSchemaCatalog.cs:106-113` 判断记录为证）。 |
| §5"循环引用检测"是否需游戏层显式登记 | **子任务证据不成立，未采纳** | 子任务原判断称相关规则不自动注册；核实 `ArchTalentTreeCycleValidationRule`（`RulesSchemaCatalog.cs:108`）、`ChainDefValidationRule`（`:146`）、`DialogContentValidationRule`（对话树成环）均经标准流水线默认自动注册，04 不改。 |
| §5"孤儿记录检测" | **已修** | 全仓库检索确认无任何实现（`grep 孤儿\|OrphanRecord\|orphan` 只命中 README 说明与测试同名变量），补"当前无对应校验规则实现"。 |
| §6.2 `time` 分组示例 | **已修** | 补 `time.sim_time`（经构造注入的仿真时钟读数）；`RulesExprSchema.cs:95` 已注册、`RulesExprHostFactory.cs:449` 已接线。 |
| §6.3 `event` 分组消歧例外 | **已修** | 补充：未登记的 `event.<key>` 不落入"未登记→id 字面量"默认路径，因 `event` 不是内容域名，仍归类为 `<reference>`（`ExprParser.cs:164-174`）。 |
| §3 schema 迁移现状 | 未改文档（现状记录） | 全部 63 张生产表 `currentSchemaVersion=1`，迁移链从未在真实表触发，属"现状"非"不一致"，只记入本文件。 |
| 变更记录 2026-09-05 行"`found.game_state`/`found.hook` 仍无实现级 schema 登记"表述已过时 | **已修（本次任务写入范围第 2 项）** | 两表已于收边 I1 完成登记并落入框架级数据目录（`RulesSchemaCatalog.cs:88-89` 注册 `FoundGameStateSchema.Table`/`FoundHookSchema.Table`，`data/_framework/found/found.game_state.json`/`found.hook.json` 均已落地）；按任务书要求，不改写历史行本身，只在该行末尾追加一句更正说明。 |

### `05_对象模型与世界.md`（此前从未审过，本轮首次逐节核对）

| 位置 | 结论 | 说明 |
|---|---|---|
| §1.2 `Unit.statBlock/powerSet/auras/threatTable` | **已修** | 标注为"引用性字段"（`Unit.cs:17-27` 类型注释明确不重复持有，由 L1/L2 各宿主按 `entityId` 索引）。 |
| §1.2 `Unit.tags`/`level` | **已修** | 补两个此前未列出的已实现字段（`Unit.cs:47-54`）。 |
| §1.2 `PlayerUnit.inventory/equipment/skillBook` | **已修** | 同理标注为引用性字段（`PlayerUnit.cs:11-19`）。 |
| §1.2 `PlayerUnit.questLog` | 记录（代码待清理，未改文档结论） | `PlayerUnit.cs:35` 仍为空 `JsonObject` 占位，quest 模块（08）落地后从未读写它；05 已加一句"留作已知缺口"。 |
| §5.3 `SpawnHost.notifyDespawn` reason 参数 | **已修** | 由枚举 `died|despawned` 改为自由文本 `String`，与 `ISpawnHost.cs:25` 实现一致。 |
| §5.3 `SpawnHost` 契约 | **已补** | 补 `update(dt)`（:34）、`triggerNever(spawnId)`（:39）、`unloadMap(mapId)`（:63）三个已实现成员。 |
| §6 离散模式行动点移动预算 | **已补** | 新增 6.3 节：`MovementTickHandler.TryConsumeMovementActionPoints`（:155）、`MovementOptions` 五个离散预算字段；`MovementState.PathIndex` 同步补入字段表。 |
| §8 WorldState 类型校验开关 | **已补** | 补一句：`WorldStateOptions.EnforceSchema`（默认 false）+ `WorldState.cs:58` 接线。 |
| §4.1 `world.map` 四字段 schema 缺口 | **已补（记录代码待补）** | `WorldMapSchema.cs:14-21` 明确 `regions`/`teleport_points`/`music_ref`/`allowed_difficulties` 仅结构声明、未接入字段校验（"暂存处，待迁移补全"），05 按现状补充说明，不改变目标形态。 |
| §1 `AreaTrigger` 是否为 `Entity` 子类 | **已拍板：改代码，已落地（2026-09-06）** | 05 继承树把 `AreaTrigger` 列为 `Entity` 子类；此前代码侧 `core/gameplay/area_trigger` 只有 `AreaTriggerDef`（纯数据）+ `AreaTriggerHost`（字典管理），无 `Entity` 子类。已按拍板补 `AreaTriggerEntity : Entity`（`Kind=EntityKinds.AreaTrigger`）、`AreaTriggerHost` 经 `IWorldSim` 创建/销毁对应实体、`EntityKindMapping.cs` 的 `"area_trigger"` 占位字符串换成正式常量，见 `core/gameplay/area_trigger/README.md` 判断记录。 |
| §3.6 碰撞层三常量 | **已拍板：补实现，已落地（2026-09-06）** | 05 §3.6 列 `terrain_block`/`unit_block`/`trigger_only` 三层；已补 `CollisionLayers` 常量类（`core/foundation/engine_adapter/contracts/CollisionLayers.cs`）、`CarriersAssembly.DefaultSpatialSyncKinds` 补 `unit_block`/`trigger_only` 标签登记、`MovementOptions.UnitBlocking`/`UnitBlockRadius` 接线 `MovementTickHandler`、四处不带 `RequiredTags` 的空间查询点补 `ExcludedTags`，详见 05 §3.6 变更记录与各模块 README 判断记录。 |

### `06_规则层_属性技能战斗AI.md`（复核收边I3/加固J 批次增量 + 事件词汇表全量 diff）

| 位置 | 结论 | 说明 |
|---|---|---|
| §4.4 `ThreatTable` 接口结构 | **已修** | 补 `getThreat`/`setThreat`/`getAll` 三个方法（`IThreatTable.cs:20,26,30` 早已实现，06 此前只列了 `addThreat`/`getTopThreat`/`clear`，遗漏了供嘲讽"强制置顶"与召唤模块"共享仇恨表"策略实际依赖的三个方法）。 |
| §8 事件词汇表 | **已修** | 补 `ai.decision_made { unitId, decisionId }` 一行（`found.event_catalog.json`/`Events.cs` 的 `AiDecisionMadeEvent` 已登记为建议行，06 此前未提及，比照 `targeting.resolved` 处理方式登记为预留位）。 |
| `AuraHost` 订阅 `entity.destroyed` 自愈 | 无需修 | `entity.destroyed` 权威登记处是 03（`WorldSim` 生命周期发出），06 定位是"统一登记规则层发出的事件"，不遗漏。 |
| 投射物 Effect 原语（`projectile`）参数 | 无需修 | `EffectDispatcher.ApplyProjectile`/`ProjectileHost.Spawn` 实际字段与 06 §3.2 概括粒度一致（同表其余行同样不逐参数穷举）。 |
| `CastFailureReason` 枚举 | 无需修 | 代码 16 项与 06§3.6 步骤表对应值齐全，5 个"补充"项与既有审计记录一致，本轮 4 次提交未新增第 6 个额外值。 |
| `event_catalog.json` 全量 diff（skill/combat/aura/stat/power/ai/unit/sim domain，约 29 行） | 24 项一致，1 项（`ai.decision_made`）判定为遗漏（已修，见上），4 项（`sim.tick_started`/`sim.tick_finished`/`unit.moved`/`unit.skill_binding_changed`）经核实权威在 03/01/10/carriers，不属 06 范畴。 | |

### `07_载体层_物品生物物件.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| 新增"抛射物"一节 | **已修（补充）** | 新增第 7 节（定位/效果参数表/契约事件生命周期/策略配置项），依据 `core/carriers/projectile/README.md` 全文核对：5 字段 `ProjectileEntity`（05 §1.4 已有）、9 个效果 `params` 字段、`IProjectileSpawner` 依赖倒置接口、无专属事件复用 `entity.created/destroyed`、挂 `TickPhase.MovementAndNavigation`、不进存档（`ProjectileHostTests.cs:317-320` 断言不实现 `IPersistable`）。 |
| "每个模块的补充信息"表 | **已修** | 新增抛射物一行。 |
| §4 召唤 `PlayerCanControl` 策略项 | **已修** | `SummonOptions.cs:51`（默认 false）经 `SummonHost.IsControllableByOwner`（`:149`）接入、并接进 `GameplayAssembly` 的 `IsPlayerActor` 判定；07 此前只字未提，已补一句并同步补充信息表。 |

### `08_玩法层_掉落任务对话关卡.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §4.3"阶段切换于回合结束时生效" | **已修** | 改写为"离散模式下遭遇的整轮求值（波次触发、阶段推进、场地规则、胜负判定）改由回合结束/轮结束事件驱动"；`EncounterTickHandler` 经 `EncounterHost` 订阅 `sim.turn_ended`/`sim.round_ended` 驱动的是整轮求值，不只是阶段切换。 |
| §4.1 `combat_mode_override`/`initiative_override.policy` 未知取值处理 | **已修** | `TimeModelSwitch.cs:204-209`：`combat_mode_override` 未知取值与缺失值走同一 `default → null`；`:220-229`：`initiative_override.policy` 未知取值抛异常；并补充"校验期不拦截"这一行为说明（`combat_mode_override` 是 `FieldKind.Enum` 内容校验期即可拦截，`initiative_override` 是 `FieldKind.Object` 不透明，内部 `policy` 完全不经内容校验）。 |
| §9 `DifficultyHost.apply` 返回类型 | **已修** | 补充显式返回类型 `Bool`（`DifficultyHost.cs:77`/`IDifficultyHost.cs:44` 均为 `bool Apply(...)`）。 |
| §5.2 `AllowMidSwitch` 与 `entryDifficultyOptions` 关系 | **已修** | 补充说明二者是两个独立机制，映射关系归游戏层接线（`DifficultyOptions.cs:30` 判断记录原句）。 |
| §7 `econ.vendor` 补货时机 | **已修** | 补一句：`on_map_enter` 由 `GameplayAssembly.EnterMap` 显式调用（非事件订阅），`timer` 补货仅在连续步推进，离散步不推进。 |

### `09_表现层.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §2 `ViewFactory.createView` | **已修** | 补 `entityRef: Id` 参数（`IViewFactory.cs:15` 实际签名多此参数，被 `ViewBinder.cs:146` 依赖）。 |
| §3.2 方向索引重映射策略 | **已修（补充）** | `RenderOptions.cs:31 DirectionIndexRemap`、`RenderConventionHost.cs:36-51 ResolveDirectionSlot` 实际消费，文档此前零提及。 |
| §3.3.1 `IAnchorQuery` 接口 | **已修** | `IAnchorQuery.cs:27-45` 被 `VfxPlayer` 的 `attach_mode:anchor` 依赖，文档未提及，已补一句。 |
| §3.3.2 `IModelHandleProvider` | **已修** | 供 `attach_mode:socket` 挂接，文档未提及，已补一句。 |
| §5.5 `IAudioLayerVolumeHost` | **已修** | 比 §5.3 `SfxPlayer.setLayerVolume/setLayerMuted` 更高一层的"设置面板"汇总契约（层清单+持久化），文档完全未提及，已补一句。 |
| §3.3.1 锚点表 `parent_layer`/`offset_by_direction` | 代码待补 | `SpriteInfo.cs:47`/`DisplaySchemas.cs:57` 的 `anchor_points` 只是扁平 `Dictionary<string,Vec2>`，无 `parent_layer`，也无按方向覆盖；镜像水平分量取反是运行期自动派生。 |
| §3.3.2 挂点表 `socket_id`/`attach_ref` | 代码待补 | `display.map.sockets` 只是裸 `Id` 列表，没有"该挂点默认挂接资源引用"这个子字段；实际挂接资源只来自 `display.equip_visual` 按物品声明。 |
| §3.4"sprite 型影子经 `IRenderer2D` 应用" | 代码待补 | `IRenderer2D.cs` 无任何影子相关方法，`ShadowSpec.cs` 转换函数只在自身单测里被调用，无生产代码消费；`blob` 型影子在 2D 下语义上等价"再画一个贴地精灵层"，理论上不需要专门方法，判定为代码尚未接线而非文档矛盾。 |
| §4.1 `CharacterRig` 职责表、程序动画原语（`move/rotate/scale/flash/trail/stagger/topple/fade`） | 代码待补 | 全仓库无此类名；`trail`/`topple`/`stagger` 零匹配；层/槽位管理部分由 `SpriteViewBase` 承担，"动画状态机驱动"/"程序动画原语"两项职责代码零实现。 |
| §4.2 `enum AnimState` | 代码待补 | 全仓库零匹配。 |
| §4.3 命中帧同步规则（`logic_driven`/`anim_keyframe_driven`） | 代码待补 | 两个字符串全仓库零匹配；`IRenderer3D.OnAnimEvent` 接口已实现但无消费方接线。 |
| §7.1 回合顺序条/行动点显示 | 代码待补 | `TurnScheduler.GetOrder()`（:438）、`TryConsumeActionPoints`（:421）均已实现，但 `presentation/`、`adapters/unity/`（Tests 除外）零调用，没有 UI 展示。 |
| 投射物视图/命中反馈链路 | 无需修 | `EntityKindMapping.cs:39-41` 走通用 sprite View 创建路径，命中特效复用既有结算事件经 `feedback_binder` 触发，未产生需要文档新增描述的专属机制。 |
| 回合 HUD——当前行动者/轮次/结束回合按钮 | 无需修 | `GameplayPanels.cs:344-449 TurnStatusPanel`；`UiIntents.EndTurn()` 转发 `TurnScheduler.EndTurn`。 |
| `TurnStatusPanel` 架构落点 | 已记录，超出 09 范围 | 自身注释说明是刻意的架构妥协；属 01 文档"ui 行"精度问题（见 01 节"另一会话任务进行中"）。 |

### `10_存档与持久化.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §8 `ReplayPlayer` | **已修** | 补 `loadDiscrete(data, worldFactory)`，区分连续（按录像逐步重算模拟）与离散（不读录像行动者/阶段记录，改由调度器重新驱动、仅在需要外部输入时取录制的意图/结束回合决策，确定性额外依赖调度逻辑与录制时一致）两条互斥路径；**未使用"纯录像回放"措辞**（该短语在 10 全文零命中，只出现在 J2 提交信息里，属命名误导，未沿用）。 |
| §6 自动存档触发点 | **已补** | 补一句：地图切换/任务完成两触发点框架不提供默认接线，由游戏层自行接。 |
| §5/§2.1 `save_version` 消歧 | **已补** | 补充信封层（顶层）`save_version` 是迁移链唯一依据，meta 段同名字段为镜像展示值（`SaveSystem.cs:252,426,596`、`SaveMeta.cs:14-17`）。 |
| §6 存档点/对话 save 是否受 `ShouldAutoSave` 门控 | **需拍板，不改文档** | `GameplayAssembly.cs:200-201` 的 `RequestAutosave` 本地函数直接调用 `saveSystem.Save(...)`，未调用 `ISaveSystem.ShouldAutoSave(AutoSaveTrigger.SavePoint)`；`GobjOptions.SaveRequester`（:572）与 `DialogHost` 的 `saveRequested` 参数均绑定同一函数。见四节需拍板清单第 1 条。 |
| §3 固定段序 vs `RegisterPersistables` | 无需修（复核） | J2 未触碰该方法，段序与既有审计一致，无新增/删除。 |

### `11_工程规范与测试.md`（另一会话已于 878e0d0 提交，本次复核确认）

| 位置 | 结论 | 说明 |
|---|---|---|
| §2 模块范式（抽查 skill/combat/projectile/encounter/vfx_sfx） | 无需修 | 除 `core/carriers/projectile` 无 `schema/` 目录外其余四样齐全，该模块 README 已自述缺项合理。 |
| §2"纯逻辑零引擎依赖" | 无需修 | 抽查零 `UnityEngine` 引用。 |
| §2.1 `toolchain/asset_import/` 目录归属 | 无需修 | `import_assets.py` 是 24 行薄入口，内部包结构属实现细节。 |
| §3 命名规范 | 无需修 | 抽样 20～30 例全部符合规则。 |
| §4 错误处理 | 无需修（1 行为原则性描述） | 数据引用拦截、表现层异常隔离两行有扎实证据；"调试断言/发布降级日志"二态区分文档本身自称"建议"性原则，不构成不一致。 |
| §5 性能约定 | 无需修（覆盖不均，记录） | "空间查询"有直接性能基线佐证；"对象池"/"事件批处理"只有功能正确性测试、无专门性能基准，不构成文档矛盾。 |
| §6 测试分层表格 | **已修（补充 4 行）** | 补"契约一致性测试套件""引擎灰盒测试""无人值守冒烟""消费方演练"四层，此前只以散落文字/独立小节存在。 |
| §8 提交门槛 | **已修（补充说明）** | 补"CI/pre-commit 均为门禁子集，完整门禁需本机全量跑"；`import_assets.py check` 未接入 check.ps1 的通用资产校验步骤仍是真实缺口（见三节代码不符设计清单）。 |

### `12_扩展与变更流程.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §1 新增内容 checklist | 无需修 | 表格用类别化中立表述，核对 `core/**/core/*ValidationRule*.cs` 与描述语义相符。 |
| §3 ADR 模板 vs 16 篇 ADR | 无需修 | 抽查 ADR-0016 逐节核对模板六部分齐全。 |

### `13_新游戏接入指南.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §4 口味配置项清单（28 行）vs `GameOptions.cs` | 无需修 | 复核：28 行数据行与 `GameOptions.cs` 注释"13 §4 第 1～28 行"连续无缺漏，编号无多余，本次审计对齐度最好的一份文档。 |
| §7 跑验收/消费方演练路径 | 无需修 | 与 `toolchain/consumer_smoke.ps1`、`games/_template/README.md` 七步立项对照表实际内容一致。 |
| §6 TMP 与占位字体拷贝步骤 | 无需修（不属本文档范围） | 属具体引擎实现级细节，已记录在 `games/_template/README.md`（允许技术名），不应写入 13。 |

### `14_资产规格书模板.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| §1.2 命名规则 vs `common.py`/`SpriteViewBase.cs` | 无需修 | 文件名拼接模板逐字一致。 |
| §2.1 方向档位命名与镜像规则 | 无需修 | 与 `directions.py` 实现完全一致；16 方向档位的扩展命名属工具自行约定的扩展，不改变已拍板字段结构。 |
| §2.1"Id 前缀"说明 | 无需修 | 2026-09-05 已有勘误记录，`common.py` 注释显式引用该结论为权威。 |
| §1.1 字段映射表、`display.map` 字段结构 | 无需修 | 抽查样例数据与 14 §1.1 映射表逐字段对应。 |
| §3/§4 纸娃娃层清单、锚点清单 | 无需修 | 与代码注释共用同一套裸层名/裸锚点名。 |

### `README.md`（文档集导读）

| 位置 | 结论 | 说明 |
|---|---|---|
| 第 4 节文件清单 | **已修** | ADR 实际已有 16 篇（0001～0016），文件清单原写"共 15 篇"、树状图未列 `adr/0016`，已补一行并把计数改为 16 篇（实测 `architecture/adr/*.md` 共 17 个文件，含 1 个 `README.md`，16 篇编号 ADR）。 |
| `adr/README.md` 索引 | 无需修 | 已登记 ADR-0016（编号/标题/日期/状态），与实际文件一致。 |
| ADR-0016 结构 | 无需修（核对） | 对照 12 §3 模板逐节核对六部分齐全。 |
| 阅读顺序、强制约束一览 | 无需修 | 与 `00_架构总则.md` 实际章节逐条核对一致。 |

### `adr/`（0001～0016）

| 位置 | 结论 | 说明 |
|---|---|---|
| `projectile` 效果原语是否需要补 ADR | **子任务原判断不成立，未采纳** | 子任务原认为"projectile 是定稿后新增的原语、缺 ADR"；主会话复核确认定稿版 06 §3.2 效果表在 2026-09-04（`55d2b21` 第 136 行）已含 `projectile`，收边 I1 只是补齐实现，不属于"定稿后新增原语"，**不需要 ADR**，本节维持既有 16 篇，未新增。 |
| 本轮 0001～0016 文件本身 | 无需修 | 未发现需要改动的既有 ADR 文件；ADR-0016 内容与既有 15 篇体例一致（见上 README 一节）。 |

### `选型/`

| 位置 | 结论 | 说明 |
|---|---|---|
| `选型/01` §5 结论"2D 寻路依赖第三方包" | **已修（勘误）** | 实际实现 `UnityNavigation2D.cs` 是自研网格 A*，代码头注释明确"不引入第三方寻路包"（Unity 内置 NavMesh 偏三维，与二维平面定位不贴合）；不改变"`INavigation2D` 契约把寻路能力隔离在适配层"这条拍板结论本身。 |
| `选型/01` §8 风险表"2D 寻路依赖第三方包"行 | **已修（勘误）** | 同上，改写为"自研网格 A* 寻路的能力/性能边界"，缓解措施补充"若不满足需要可在契约不变前提下改接第三方包"。 |
| `选型/01` §11"发布形态验证"章节 | **已由另一会话落地（878e0d0），本次复核确认** | 2026-09-05 审计标注"缺失该实测数据章节"，现已补齐：本机 Unity 6000.3.23f1 实测 Mono 构建+冒烟 14.8s/3.3s 均成功，IL2CPP 构建因本机缺少 Visual Studio C++ 工具链与 Windows SDK 而失败（`ToolchainNotFoundException`，`build_il2cpp.log` 原文记录），属真实环境限制，已如实记录而非编造数据。 |
| `选型/01` §4 打分表"导航能力"维度 | 无需修 | 评估的是各引擎通用生态事实，与最终选择自研寻路无关，不受上述勘误影响。 |
| `选型/01` 其余版本号/路径 | 无需修 | 与 `Directory.Build.props`、`manifest.json`、`ProjectVersion.txt` 逐一核对一致。 |
| `选型/02`"UI 框架与字体"行状态列 | **已修（勘误）** | "待装引擎"早已不成立（Unity 6000.3.23f1 + TextMeshPro 已就绪并大量使用），改写为"已就绪"。 |
| `选型/02` §2"本地已装/待装"清单 | **已修（勘误）** | Unity 与 .NET SDK 均已就绪，改写为"本地已装"，"待装"收窄为"抠图模型"（本次未核实是否就绪，如实标注）。 |
| `选型/02` §1 其余资产类别状态 | 无需修（未核实，非明显过时） | 依赖仓库外的本地软硬件环境，无法从仓库内文件核实。 |

### `落地计划/落地方案与分阶段计划.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| H3b/H4/H5、I1/I2/I3、J1/J2/J3 各波次 | 无需修 | 逐一核对 `git log --since=2026-09-05` 全部提交，对应小节均已登记提交 hash 与摘要。 |
| 工程收尾 K（CI/pre-commit/IL2CPP/editor/） | **已由另一会话落地（878e0d0），本次复核确认** | 新增"工程收尾 K"一节，列出 CI 工作流、提交前钩子、IL2CPP 发布形态验证、`requirements-optional.txt` 拆分、`editor/` 新目录五类改动，末节追加四次门禁实测结果与三条判断记录；全部标注不改变任何拍板结论、不涉及新增原语、无需 ADR。 |
| IL2CPP 前向引用缺口 | **已解决（878e0d0 补齐 `选型/01` §11）** | 2026-09-05 审计任务标注"待补"的实测数据章节已由该提交补齐（见上"选型"一节）。 |

### `data/README.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| "多根加载与合并规则"一节 | **已修** | 此前只字未提行级 `override`/`final` 覆盖语义，与 04 第 4 节 2026-09-06 改动（`DataRegistry.cs:490-543`）和 `games/_template/data/README.md` 自身已假定该机制存在互相脱节，已补充完整行为说明，保留原有两条规则不变。 |
| 其余判断记录（`found.game_state`/`found.hook`/`arch.power_type`） | 无需修 | 与代码现状一致（`RulesSchemaCatalog.cs:88-89` 已注册 schema）。 |
| `python toolchain/validate_data.py` 复核 | 无需修（旁证） | 重跑一次：58 表、274 行、0 错误、0 警告、1 条覆盖，与两份 README 描述一致。 |

### `presentation/README.md`

| 位置 | 结论 | 说明 |
|---|---|---|
| 标题与内容 | **已修** | 原标题误写"Presentation.Common"、内容是 `common` 子模块旧版简介，未覆盖 presentation/ 实际 9 个子模块（render/camera/common/feedback_binder/shell/ui/vfx_sfx/view_binding/assembly），已改为描述整个 L5 层并列出全部子模块。 |

### `core/*/README.md`（foundation/numbers/rules/gameplay/carriers）

| 位置 | 结论 | 说明 |
|---|---|---|
| `core/foundation`、`core/numbers`、`core/rules`、`core/gameplay` 各 README | 无需修 | 均为统一的"标题+职责一句+依赖一句"极简三行体例，未列出任何模块清单，不构成"列表内容有误"。 |
| `core/carriers/README.md` | 无需修（观察记录） | 同上体例，未提及 `projectile`/`summon` 两个新模块名，但对既有模块同样未提及，不构成"漏写这两个"的不一致；建议后续如升级为逐模块枚举时一并补齐（本次未改）。 |

## 二、硬性规则扫描结果

### 技术名扫描

对 `architecture/00～14.md` 与 `architecture/adr/*.md` 全文 grep（不区分大小写，词表：
`unity、c#、csharp、.net、dotnet、python、json、xunit、nunit、powershell、.ps1、\bgit\b、
github、visual studio、rider、textmesh、urp、il2cpp、mono、unreal、godot`）：

| 命中 | 位置 | 判定 |
|---|---|---|
| `unity`（子串） | `06_规则层_属性技能战斗AI.md:158` `immunity` | 英文单词内部子串巧合匹配，非技术名，非违规。 |
| `json`（子串） | `05_对象模型与世界.md:70` `JsonObject`（`questLog` 字段类型说明） | **真实命中，判定为轻微违规**：这是本轮（C 组）新写入的表述，`git show a9649a6:...05...` 基线该行没有此类型名；05 是技术无关的架构正文，写入具体 C# 类型名 `JsonObject` 不符合硬性规则。因不在本次任务书授权的写入范围（只授权本文件与 04 一行），本次未修改 05，已如实记录，建议下一轮任务把"`JsonObject`"改写为中立表述（如"未展开结构的通用对象占位"）。 |
| `json`（子串） | `06_规则层_属性技能战斗AI.md:18`、`06_规则层_属性技能战斗AI.md:462` 两处 `found.event_catalog.json` | **真实命中，判定为轻微违规**：D 组本轮新增的变更记录行与事件表引用了具体数据文件名（含 `.json` 扩展名）作为证据来源；对照仓库内既有惯例（其余文档引用同一张表时一律用不带扩展名的逻辑表名 `found.event_catalog`），这两处是本轮唯一带 `.json` 扩展名直接出现在 00～14 正文里的情形。同样不在本次写入范围，已记录，建议下一轮改写为不带扩展名的表名引用。 |
| 其余全部词条（`c#/csharp/.net/dotnet/python/xunit/nunit/powershell/.ps1/git/github/visual studio/rider/urp/il2cpp/mono/unreal/godot`） | — | **零命中**。 |

技术名合理出现位置（选型/落地计划目录，允许）：`architecture/选型/01_引擎与语言选型评估.md`、
`架构/选型/02_美术与工具选型.md`、`架构/落地计划/文档代码一致性审计_2026-09-05.md`、
`架构/落地计划/文档代码一致性审计_2026-09-06.md`（本文件）、`架构/落地计划/落地方案与分阶段计划.md`。

**结论**：00～14 正文与 adr/ 整体符合"技术无关"硬性规则。终扫发现本轮新写入的 3 处轻微违规（05 一处 `JsonObject` 类型名、06 两处带 `.json` 扩展名的文件名引用）以及 2 处历史遗留的源码文件扩展名泄漏（04 变更记录 2026-09-06 行引用 `DataRegistry.cs`，来自 d93ac8e；11 第 1 节目录树引用 `import_assets.py`/`validate_data.py`，来自 d4b5c5b），**均已由主会话在收尾时改为中立表述**（05 改为"未定型的结构化值"，06 改为逻辑表名 `found.event_catalog` 与"`core/rules/common/contracts` 下的对应事件类型定义"，04 改为"`core/foundation/data_registry` 注册表实现"，11 改为"资产导入工具入口 / 校验入口脚本"）。修正后对 00～14 与 adr/ 复扫技术名词表与 `.cs/.py/.ps1/.json/.asmdef/.csproj` 扩展名均为零命中。

### 游戏代号扫描

对整个仓库（排除 `.git`、`bin`、`obj`、`Library`、`dist`、`Logs`）对首款游戏代号做全仓不区分大小写检索（代号本身按规则不写入本文件）：**零命中**，
含新出现的 `editor/`、`.github/`、`.githooks/` 等未提交目录。符合"任何文件不出现具体游戏代号"规则。

## 三、代码是否符合设计

### （1）合规确认项

| 项 | 证据 |
|---|---|
| 引擎隔离（逻辑不依赖引擎对象） | `core/` 与 `presentation/` 全量 `grep UnityEngine\|UnityEditor` 零命中；`core/*.csproj` 无对 presentation/adapters 程序集的 `ProjectReference`。 |
| 依赖矩阵（单向依赖链） | `csproj`/`asmdef` 依赖链单向 Foundation→Numbers→Rules→Carriers→Gameplay→Presentation.Common；Stub/Conformance 仅依赖 Foundation；无环、无 core→adapters/presentation 生产引用；`core/gameplay/tests/EndToEnd/GameWorldFixture.cs:212-219` 注释主动写下"不应反向依赖 presentation"的判断记录。 |
| 表现层只读（P3 不写回逻辑数据） | 全仓库 `presentation/` 全部写方法调用收敛在三个"意图转发出口"：`UiIntents.cs`（18 方法转发到对应 Host）、`ShellHost.cs`、`AudioLayerVolumeHost.cs`；其余模块对 Host 的调用逐一核查全部是只读查询方法。 |
| 数据即内容 | `core/` 无对具体内容 id 的业务分支；命中均为事件 key 常量、Options 默认值、表名字符串、`WellKnownHooks`/`WellKnownPowers`（已登记 `data/README.md`）。 |
| 一个条件语言 | 无绕过 Expr 的自制条件语法；`AreaTriggerHost.ParseCondition` 只是 `ExprParser` 薄封装。 |
| 原语审批（唯一定义无影子） | `EffectKind` 19 / `AuraEffectKind` 10 / `GobjKind` 10 / `QuestObjectiveType` 8 / `DialogActionKind` 10 / `NpcFlag` 6，与 06/07/08 文档一致；`projectile` 效果原语在定稿版 06（2026-09-04）已登记，不属于需补 ADR 的"定稿后新增原语"。 |
| WorldState 替代 Phasing | 唯一存储，`IWorldFlags` 是 `WorldState` 的窄接口。 |
| 存档是唯一持久化 | `IFileSystem` 生产调用方仅 `SaveSystem`（存档+备份轮转）、`SettingsStore`（设置，独立信封，10 §7 已记录为既定设计）、`FileSystemDataSource`（只读内容）；未发现玩家进度绕过 `ISaveSystem`。 |
| 固定步长确定性（系统时间/系统随机隔离） | `DateTime.UtcNow` 仅出现在自动存档时间戳提供者（已声明不在确定性铁律范围）；`new Random` 仅在测试固定种子场景；tick 路径（`WorldSim`/`SimClockHost`/`TurnScheduler`）零命中系统时间/系统随机；`RngHost` 无系统随机绕过。 |
| core 引用 presentation 检查 | `grep -rn "presentation" core/` 命中约 14 处，逐条核实全部是注释/事件 key 字符串常量，无一处 `using`/类型引用。 |
| presentation 订阅事件 key vs `found.event_catalog.json` 登记 | 逐一核查全部订阅 key 均能找到登记行，无拼写错误或历史改名遗留。 |

### （2）代码不符设计清单

| # | 设计出处 | 代码位置 | 差异 | 判定建议 |
|---|---|---|---|---|
| 1 | 10 §6"自动存档时机是口味配置项……基础架构提供触发点机制"，隐含"是否触发要经策略判定" | `core/gameplay/assembly/GameplayAssembly.cs:200-201` `RequestAutosave`；被 `GobjOptions.SaveRequester`（存档点物件，:572）与 `DialogHost` 的 `saveRequested`（对话 save 动作）复用 | 直接 `saveSystem.Save(...)`，未调用 `ISaveSystem.ShouldAutoSave(AutoSaveTrigger.SavePoint)`；`OnSavePoint=false` 时仍无条件写盘 | **需拍板**（见四节第 1 条）：补代码门控 或 把这两处重新定性为"手动存档" |
| 2 | 03 §4.2"固定顺序……是'同种子同输入同结果'确定性要求的基础"、10 §8"保证同种子同输入序列产出同结果" | `MovementTickHandler.cs:210,254,261`、`ProjectileHost.cs:109,121,197,245`、`AreaTriggerShapeGeometry.cs:46-47,71,83`、`EncounterShapeMath.cs:48,68-69`、`Vec2.cs:26` 等 `Math.Atan2`/开方 | 同平台同二进制可复现，跨平台/CPU 浮点超越函数实现细节可能不逐位一致，文档"同种子同结果"未限定"仅同平台" | **需拍板**（见四节第 2 条）：文档明确限定同平台 或 后续改定点数/查表实现 |
| 3 | 05 §1 继承树把 `AreaTrigger` 列为 `Entity` 子类 | `core/gameplay/area_trigger`：`AreaTriggerDef`（纯数据）+ `AreaTriggerHost`（字典）；`EntityKinds.cs` 无该常量；`EntityKindMapping.cs` 留死分支 | 文档说是 Entity 子类，代码是纯数据记录 + 宿主登记，不产生 Entity 实例 | **已拍板：改代码，已落地（2026-09-06）**（见四节第 3 条）：补 `AreaTriggerEntity : Entity`，`AreaTriggerHost` 经 `IWorldSim` 创建/销毁实体，`EntityKindMapping.cs` 占位字符串换成正式常量，详见 `core/gameplay/area_trigger/README.md` → 已按拍板改代码实体化（2026-09-06） |
| 4 | 05 §3.6 列 `terrain_block`/`unit_block`/`trigger_only` 三个碰撞层 | 此前全仓检索无这三个常量；`GameOptions.UnitBlockEnabled` 未被任何空间查询代码读取 | 文档承诺三层碰撞概念，`ISpatialQuery` 实际只有通用标签过滤 | **已拍板：补实现，已落地（2026-09-06）**（见四节第 4 条）：补 `CollisionLayers` 常量类、`CarriersAssembly.DefaultSpatialSyncKinds`/`MovementOptions.UnitBlocking` 接线、四处查询点补 `ExcludedTags`，`GameOptions.UnitBlockEnabled` 已接到 `MovementOptions.UnitBlocking` |
| 5 | 05 §4.1 `world.map` 字段表 `regions`/`teleport_points`/`music_ref`/`allowed_difficulties` | `WorldMapSchema.cs:14-21` 只登记 `id`/`scene_ref`/`nav_ref`/`spawn_points` 四字段 | 四字段只有文档声明，无 schema 校验 | 代码待补（目标形态正确，未接线） |
| 6 | 08 §4.1 `initiative_override.policy` | `TimeModelSwitch.cs:220-229` 未知取值在装配/求值期才报错 | 内容提交阶段（`toolchain/validator`）不拦截非法值；姊妹字段 `combat_mode_override`（`FieldKind.Enum`）反而在内容校验期即可拦截，两者校验时机不一致 | 代码待补：给 `initiative_override.policy` 补一条 `IValidationRule` |
| 7 | 03/08 离散时间模型已定稿实现 | `core/foundation/app_lifecycle/contracts/InWorldSubState.cs:8-11`、`core/gameplay/encounter/core/EncounterDefinition.cs:34-42`、`core/gameplay/assembly/GameplayAssembly.cs:828-835` | 三处代码注释仍写"本项目离散模式未启用"，与 `EncounterHost.cs:51-58`（`CombatModeOverride`/`InitiativeOverride` 已透传消费）、`TurnScheduler`/`TimeModelSwitch` 实际接线矛盾 | 代码待补：清理过时注释（不涉及文档改动） |
| 8 | 08 落地（quest 模块已实现） | `core/carriers/unit/contracts/PlayerUnit.cs:35` `QuestLog` | quest 任务状态权威存储在 `QuestHost` 一侧，该字段从未被读写，是死代码 | 代码待补：移除或接线 |
| 9 | 04 §3 schema 迁移机制（已实现有测试覆盖） | 全部 63 张生产表 `currentSchemaVersion=1` | 迁移链从未在真实数据表上触发过一次真实迁移 | 现状记录，非代码缺陷，留待真实内容扩展时验证 |
| 10 | 09 §4.1"CharacterRig"职责表、程序动画原语 8 项（`move/rotate/scale/flash/trail/stagger/topple/fade`） | 全仓库无 `CharacterRig` 类名；`trail`/`topple`/`stagger` 零匹配；`AnimState`（§4.2）零匹配；命中帧同步策略（§4.3）两字符串零匹配 | 09 目标形态描述完整，代码大片未实现（角色骨架/程序动画/命中帧同步） | 代码待补，规模较大，建议单独立项 |
| 11 | 09 §3.4"sprite 型影子经 `IRenderer2D` 应用" | `IRenderer2D.cs` 无影子方法；`ShadowSpec.cs` 转换函数仅单测调用，生产代码零消费 | 代码尚未把 `ShadowSpec` 接到任何实际绘制调用；判定为代码待补而非文档矛盾（`blob` 型影子在 2D 下语义上不需要专门方法） | 代码待补 |
| 12 | 09 §7.1 回合顺序条/行动点显示 | `TurnScheduler.GetOrder()`/`TryConsumeActionPoints` 均已实现 | `presentation/`、`adapters/unity/` 零调用，没有 UI 展示 | 代码待补 |
| 13 | 11 §8 提交门槛"新增资产经导入工具校验" | check.ps1 步骤 6 只测占位资产生成器自身一致性（`gen_placeholder_assets.py --check`） | 无通用的 `toolchain/import_assets.py check`（`assets/<dataset>/` 与 `data/<dataset>/` 交叉校验）步骤接入门禁 | 代码/门禁待补：本框架仓库自身无真实游戏资产，可能是暂未接入的原因 |
| 14 | 01 文档 L5 `shell` 行"不订阅 `save.completed`" | `presentation/shell/core/ShellViewModel.cs:41-45` | 实际订阅 `save.completed`/`save.loaded`（刷新存档槽摘要），与 01 文档字面矛盾；01 文档"契约接口名"列也未列 `ShellViewModel` | **另一会话任务进行中**，本次未改 01，待其落地后复核 |

补充（次要，未纳入以上 14 条编号，供参考）：09 §3.3.1 锚点表缺 `parent_layer`/`offset_by_direction`
子字段、§3.3.2 挂点表缺"默认 `attach_ref`"子数据——均为"文档拆得更细、schema 揉成一个 Object/Array
字段兜底"的常规粒度差，已在 09 表格中如实记录，不单独计入代码待补统计。

## 四、需拍板清单

1. **存档点物件/对话 save 动作是否应受 `AutoSaveTrigger.SavePoint` 门控约束**（10 §6）——文档说
   "触发点是否生效应经策略判定"，实现是"存档点/对话 save 无条件写盘"。不能当勘误处理的原因：
   补代码门控会改变现有行为（关闭 `OnSavePoint` 后这两处将不再自动存档），属功能变化而非文字
   订正。两条路径：①补 `if (saveSystem.ShouldAutoSave(...)) RequestAutosave(...)` 门控调用；
   ②把"存档点物件/对话 save"改文档重新定性为不受该开关约束的"手动存档"。
2. **确定性承诺的平台范围口径**（03 §4.2、10 §8）——文档说"同种子同输入同结果"，实现里模拟路径
   存在未定点化的浮点三角/开方运算，跨平台/CPU 不保证逐位一致。不能当勘误处理的原因：这会改变
   拍板决策 3（固定步长模拟）的措辞范围。两条路径：①文档明确限定"仅同一构建目标平台"；②后续
   把这些运算改为定点数/查表实现以获得跨平台位级一致。
3. **`AreaTrigger` 是否应作为 `Entity` 子类**（05 §1）——**已拍板：改代码，已落地（2026-09-06）**。
   文档继承树列为 Entity 子类，此前实现是纯数据记录 + Host 字典管理，无 Entity 子类、无 `Kind`
   常量；已按拍板②补代码：`core/gameplay/area_trigger` 新增 `AreaTriggerEntity : Entity`
   （`Kind = EntityKinds.AreaTrigger`），`AreaTriggerHost` 经 `IWorldSim.AllocateEntityId`/
   `AddEntity`/`MarkForDestruction` 创建/销毁对应实体，`EntityKindMapping.cs` 占位字符串换成正式
   常量，判断记录详见 `core/gameplay/area_trigger/README.md`。
4. **碰撞层三常量是否降级为"标签约定示例"**（05 §3.6）——**已拍板：补实现，已落地（2026-09-06）**。
   文档列 `terrain_block`/`unit_block`/`trigger_only` 三个固定层，此前实现只有通用标签过滤，
   `GameOptions.UnitBlockEnabled` 未接线；已按路径②落地：新增 `CollisionLayers` 常量类
   （`core/foundation/engine_adapter/contracts/CollisionLayers.cs`）；`CarriersAssembly.
   DefaultSpatialSyncKinds` 补 `creature`/`player` 的 `unit_block` 标签、新增 `area_trigger` 条目
   打 `trigger_only` 标签（半径经 `GameplayAssembly` 用 `AreaTriggerEntity.BoundingRadius` 精确
   计算）；`MovementOptions` 新增 `UnitBlocking`/`UnitBlockRadius`，`MovementTickHandler` 据此在
   位移前查询阻挡；`core/rules/targeting/core/BuiltinTargetStrategies.cs`/`core/rules/ai/core/
   AiHost.cs`/`core/rules/expr_host/RulesExprHostFactory.cs`/`core/gameplay/assembly/
   TimeModelSwitch.cs` 四处不带 `RequiredTags` 的查询点补 `ExcludedTags`；`GameOptions.
   UnitBlockEnabled` 接线 `MovementOptions.UnitBlocking`（`BuildMovementOptions()`）。详见 05 §3.6
   变更记录（2026-09-06 补充）与 `core/carriers/assembly`/`core/carriers/unit`/`core/gameplay/
   area_trigger` 三个模块 README 判断记录。
5. **01 文档"shell 行：不订阅 `save.completed`"与 `ShellViewModel` 实际订阅矛盾**（01，另一会话
   任务进行中）——`ShellViewModel.cs:41-45` 确实订阅该事件，01 文档字面相反。本次未改 01，待另一
   会话完成其正在进行的修订后复核；若其修订未覆盖这一点，需后续任务补上。
6. **01 文档"ui 行"未列出 `TurnStatusPanel` 例外架构与 `IAudioLayerVolumeHost`/`ISkillBindingHost`
   两个契约类型**（01，同上，附属于第 5 条，同一会话处理范围）——`TurnStatusPanel` 绕开
   `UiPanel`/ViewModel 体系直连 `GameplayAssembly`/`UiIntents`（代码自身注释承认是刻意妥协），01
   "契约接口名"列只列了 `UiFrameworkHost`（实现级为 `UiDataSource`/`UiIntents`/`UiPanelRegistry`+
   十个 ViewModel），未提及这两个契约类型与该例外面板。

## 五、统计

- **勘误条数**（本轮 2026-09-06 changelog 中"勘误："前缀的行）：10 行（`01`、`02`、`03`、`04`
  第一行、`05` 第一行、`06`、`08`、`09`、`10`、`README.md`，`11` 的一行已由 878e0d0 提交）。
- **补充/新增条数**（"补充："/"已补"前缀，含 05 第二行、07 一行、11 一行）：约 10 行。
- **修订条数**（改变已定稿结论、版本号 +1）：1 行（`04` 数据合并覆盖规则 override/final，
  2026-09-05 已完成，本轮只是复核确认）。
- **变更记录行数合计**（00～14 正文，2026-09-06 日期行，含已提交进 878e0d0 的 `11` 两行）：17 行。
- **代码待补项数**：14 项（见三节(2)编号 1～14，其中第 1、2、3、4、14 项同时也是需拍板/待复核
  项，其余 9 项为单纯代码待补）+ 2 项次要补充（09 锚点/挂点子字段缺口，未单独编号）。
- **需拍板项数**：6 项（见四节）。
- **合规确认项数**：11 项（见三节(1)）。
- **本轮被修改文件清单**（相对路径，含已被 878e0d0 收进提交的 3 份）：
  - `architecture/00_架构总则.md`（无改动，仅复核）
  - `architecture/01_分层与依赖.md`
  - `architecture/02_引擎适配层.md`
  - `architecture/03_运行时骨架.md`
  - `architecture/04_数据与内容管线.md`（含本次任务写入的第 2 项追加说明）
  - `architecture/05_对象模型与世界.md`
  - `architecture/06_规则层_属性技能战斗AI.md`
  - `architecture/07_载体层_物品生物物件.md`
  - `architecture/08_玩法层_掉落任务对话关卡.md`
  - `architecture/09_表现层.md`
  - `architecture/10_存档与持久化.md`
  - `architecture/11_工程规范与测试.md`（已提交进 `878e0d0`）
  - `architecture/README.md`
  - `architecture/选型/01_引擎与语言选型评估.md`（已提交进 `878e0d0`）
  - `architecture/选型/02_美术与工具选型.md`
  - `architecture/落地计划/落地方案与分阶段计划.md`（已提交进 `878e0d0`）
  - `architecture/落地计划/文档代码一致性审计_2026-09-06.md`（本文件，新建）
  - `data/README.md`
  - `presentation/README.md`

## 六、已知局限与后续

- **另一会话 01 任务进行中**：`architecture/01_分层与依赖.md` 的 L5 `shell`/`ui` 两行（事件订阅列、
  契约接口名列）由另一并行会话处理，本次全程未触碰；四节第 5、6 条"需拍板"实为该会话待解决的
  已知发现（`ShellViewModel` 订阅 `save.completed` 与文档字面矛盾、`TurnStatusPanel`/
  `IAudioLayerVolumeHost`/`ISkillBindingHost` 未列入），其落地后需要一次复核确认是否已覆盖。
- **硬性规则终扫发现的 3 处本轮带入 + 2 处历史遗留技术名/扩展名泄漏已在收尾时由主会话修正**（见二节技术名扫描结论），复扫为零命中。
- **本地 main 与 PR #1 分支存在两版不同的 CI 修复**（`68b7a9e` vs `ae3f667`/`9ce4f1a`）：本次审计
  未核实两版差异细节（不在任务书授权范围），待合并 PR 时需要人工取舍，不应假设某一版已经过时。
- **`editor/` 目录**：新产品线（内容编辑器）的文档桩，实现未开始，与 CLAUDE.md"本目录是技术无关
  框架基础设施专属项目"的定位存在张力，已在落地计划"工程收尾 K"节如实登记为工作树未提交内容，
  本次未做定性判断，留待用户拍板该产品线是否属于本框架仓库范围。
- **用户尚未拍板的 6 项需拍板清单**（见四节）：在用户裁决前，不得据此扩展任何游戏层实现或文档
  结论；本文件对每项均已给出两条互斥处理路径，供设计层选择。
- **04 §3 schema 迁移链现状**：机制已实现且有测试覆盖，但全部 63 张生产表从未真实触发过一次
  迁移，属于"尚未被验证过"而非"实现有缺陷"，建议留待未来真实游戏内容做一次 schema 版本升级时
  作为该机制的首次真实验证。
