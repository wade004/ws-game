# ADR-0017 模型型外形默认路线补齐与命中帧同步

状态：已拍板 2026-09-08

## 背景

[09_表现层.md](../09_表现层.md) 第 3.3.2、4 节与 [02_引擎适配层.md](../02_引擎适配层.md) 第 1.12 节已经把 `model` 型外形的契约（`IRenderer3D`、`ICharacterRig`、槽位/挂点、命中帧同步策略）定义完整，但落地程度与 `sprite` 型外形不对称，具体表现为：

1. **`ICharacterRig` 的 `model` 型实现只是占位**：`ModelCharacterRig` 此前对层管理、锚点/挂点查询三个方法一律抛 `NotSupportedException`，只有"与外形类型无关"的状态记账/程序动画序列推进两项职责可用；同一逻辑单位选用 `model` 型外形后，除了播放骨骼动画剪辑之外几乎没有其它可用能力，与 `SpriteCharacterRig` 已有的完整层/锚点/程序动画落地不对称。
2. **`IResourceLoader` 的资源种类枚举没有模型类别**：`ResourceKind` 枚举（见 [ADR-0016](0016-引擎适配层契约阶段4联调补齐.md) 决策 5）覆盖图片、音频、字体、数据表、场景、导航网格、特效七类，唯独没有 `display.map`（`kind: model`）的 `model_ref` 与 `display.equip_visual` 的 `model_ref` 所指向的模型/骨骼动画资产类别，模型资源的首次加载责任无法用统一枚举表达。
3. **命中帧从关键帧数据到实际反馈之间没有接通**：`display.anim_set.clips[*].events`（04 第 7.1.1 节）已经登记了 `{name, time_pct}` 关键帧事件，`sprite` 型的 `FrameAnimClip.Keyframes`/`SpriteCharacterRig.HitFrameReached` 也已落地，但 `HitFrameReached` 只存在于 `SpriteCharacterRig` 一侧（未提升进 `ICharacterRig` 契约），`FeedbackBinder` 没有任何订阅方消费它——`RenderOptions.HitFrameSync = AnimKeyframeDriven` 这一口味配置项因此没有可观察的实际效果，无论选它还是 `LogicDriven` 表现层的播放时机都完全一样。
4. **武器风格没有"实体当前用的是哪一套"这一段查询**：`IWeaponStyleResolver`（09 第 4.4 节）已知 `weaponStyleRef` 之后能查到覆盖表，但"给定一个实体，它现在算哪一个 `weaponStyleRef`"这一前置查询没有任何契约或实现——装备变化事件（`item.equipped`/`item.unequipped`）已存在，但没有消费方把它与 `weaponStyleRef` 关联起来。
5. **`AnimStateMachine.StateChanged` 不携带触发技能 id**：09 第 4.4 节 `cast_anim_override` 要求按技能 id 覆盖施法动作剪辑，但状态机的切换事件只有 `(entityId, from, to)` 三元组，消费方（"持有 `WeaponStyleResolver` 的组装层代码"）拿不到是哪个技能触发了这次 `cast` 切换，无法实现按技能覆盖。

以上五项均是"契约已经说清楚该有什么，实现没跟上"的情形，其中第 2 项（资源种类枚举）与第 5 项（状态机载荷）涉及已定稿的契约签名变更，按 [12_扩展与变更流程.md](../12_扩展与变更流程.md) 第 5 节需要 ADR 说明变更理由；其余三项虽然是新增实现/新增字段而非改写既有签名，但彼此关联、共同构成"model 型外形默认路线"与"命中帧同步"这两条完整链路，一并在本 ADR 决策，便于对照。

## 决策

1. **`ResourceKind` 新增 `Model`**：加载责任沿用 [ADR-0016](0016-引擎适配层契约阶段4联调补齐.md) 决策 6——谁首次引用一个模型资源 id（`display.map.model_ref`、`display.equip_visual.model_ref`）谁负责调用 `loadAsync`，`IRenderer3D` 只消费已加载完成的资源，未加载时使用实现内建占位并记诊断，不隐式触发加载、不抛异常。
2. **model 型外形的默认路线是框架职责**：表现层（`presentation/render`）提供真实的 `ModelCharacterRig`（不再是占位实现），归并 09 第 4.1 节 CharacterRig 四项职责的 `model` 型落地；引擎适配层选用 `model` 型外形时必须提供 `IRenderer3D` 的真实实现（真正的三维渲染与骨骼动画管线），只有游戏全程只使用 `sprite` 型外形时才允许声明降级、不提供 `IRenderer3D` 真实实现——这一条把 09 第 3 节验收标准 5 此前"model 型是否提供真实三维渲染管线是游戏层可选投入，不是本架构验收范围"的表述收紧为"选了 model 型就必须有真实实现，可选的只是'要不要选 model 型'这个上一层决策"。
3. **动画事件到命中帧统一为 `ICharacterRig.HitFrameReached`**：`sprite` 型经序列帧剪辑关键帧（`FrameAnimClip.HitFrameMarker`）触发，`model` 型经 `IRenderer3D.onAnimEvent` 命中固定事件 id（`ModelCharacterRig.HitFrameEventId`，取值 `anim_event.hit_frame`）触发；`HitFrameReached` 从 `SpriteCharacterRig` 的专属成员提升进 `ICharacterRig` 契约本身，两种外形类型的消费方（如反馈绑定）不必关心具体是哪种外形。
4. **命中帧同步（`HitFrameSync = AnimKeyframeDriven` 时生效）**：反馈规则新增字段 `sync: "hit_frame"`（`feedback.binding.sync`，默认对 `combat.damage_dealt` 类命中反馈开启，其余事件默认不开启），标记为命中同步的规则，其全部动作延迟到攻击方 `ICharacterRig` 的命中帧事件到达后才一并播放；带超时兜底，默认 0.5 秒，超时即照常播放并记一条诊断。命中帧同步只影响反馈动作的呈现时机，伤害/状态结算仍然只由规则层按固定步长结算一次，不受呈现时机延迟影响（09 第 4.3 节"逻辑判定结果与时机永远由规则层决定"的延续）。
5. **武器风格来源新增"实体 → 武器风格引用"查询**：新增表现层契约 `IWeaponStyleSource.GetWeaponStyleRef(entityId): Optional<Id>`，默认实现按装备变化事件（`item.equipped`/`item.unequipped`）失效缓存，取该实体主手武器的物品模板 id，经既有的 `display.map.weapon_style_ref` 关联链路（09 第 4.4 节"关联方式"——物品模板对应的 `display.map` 行 `logical_id` 就是该模板 id 本身）解析出 `weaponStyleRef`；不新增任何数据表字段——`display.map.weapon_style_ref` 已经足以承载这条关联，"实体现在用哪个模板"是本决策新增的唯一缺口。
6. **`AnimStateMachine` 状态切换事件携带触发技能 id**：新增 `StateChangedWithSkill: (entityId, from, to, triggerSkillId: Optional<Id>)` 事件，与既有 `StateChanged: (entityId, from, to)` 同一次切换背靠背触发；`triggerSkillId` 只在由 `skill.cast_start` 驱动的切换（进入 `attack`/`cast`）时非空。既有 `StateChanged` 事件签名不变，保证既有订阅方不必修改代码即可继续编译通过。

## 后果

### 正面

- 选用 `model` 型外形的游戏在表现层不再遇到"层/挂点管理直接抛异常"的硬性阻断，装备外观（槽位换装/挂点挂接）、程序动画八原语、命中帧同步在引擎无关层面已经是完整可用的实现，接入具体引擎适配层（下一阶段）只需要补齐 `IRenderer3D` 真实实现本身，不需要再补一层表现层胶水代码。
- `RenderOptions.HitFrameSync = AnimKeyframeDriven` 第一次有可观察的实际效果——命中反馈动作确实会等到动画剪辑播到命中帧才播放，不再是"设了也白设"的口味配置项。
- 武器风格查询链路补齐"实体 → weaponStyleRef"这一段后，`cast_anim_override`/`impact_vfx_override` 才具备被真正接线消费的完整前提条件。
- `ResourceKind.Model` 落地后，模型资源的首次加载责任与其它六类资源遵循同一套契约，不需要引擎适配层为模型资源另开一条特殊加载通路。

### 负面

- `ICharacterRig` 契约新增 `HitFrameReached` 事件成员、`ModelCharacterRig` 构造函数签名发生实质变化（不再是无参占位构造），已经存在的（未来的）具体引擎适配层实现若直接实现了 `ICharacterRig` 或构造过旧签名的 `ModelCharacterRig`，需要跟着调整；本次落地范围内尚无任何引擎适配层代码依赖这两处（占位实现从未被实际构造使用过），无迁移成本，但记录在案供后续接入方注意。
- `AnimStateMachine` 新增一个额外事件（而非直接扩展 `StateChanged` 签名）是"新增重载"的折中，长期看两个事件并存增加了状态机对外暴露的接口面；一旦确认所有消费方都已经迁移到新事件，可以考虑在未来的破坏性版本里合并为一个事件并删除旧事件（不属于本 ADR 决策范围）。
- "选了 model 型就必须提供真实 `IRenderer3D` 实现"收紧了游戏层的选择空间——此前允许"选 model 型但暂不接三维渲染管线"的过渡态在决策 2 之后不再是合规状态，需要相应更新验收标准的措辞（见文档同步清单）。

## 备选方案与为什么不选

1. **保持 `ModelCharacterRig` 占位、把真实实现完全留给具体游戏各自实现**：好处是本轮改动量最小。否决理由——09 第 4.1 节职责表已经把"层/槽位管理、锚点/挂点查询"列为 CharacterRig（框架职责）而非游戏层职责，`sprite` 型这两项职责已由框架给出完整实现，`model` 型继续占位会让两种外形类型在"基础架构提供"这一维度上不对称，且每款选用 `model` 型的游戏都要重新实现一遍同样的槽位/挂点/程序动画映射逻辑，与"基础架构提供 CharacterRig 与程序动画原语的行为定义"这一条产品定位矛盾。
2. **命中帧统一事件不提升进 `ICharacterRig`，只在 `SpriteCharacterRig`/`ModelCharacterRig` 各自暴露、由消费方按外形类型分别处理**：好处是不改动既有接口。否决理由——`FeedbackBinder`/`IHitFrameSource` 一类消费方按实体 id 订阅命中帧，不应该也不需要知道该实体是哪种外形，分别处理会让每一处消费代码都重复一遍"先判断外形类型再取事件"的样板代码，与两种外形类型对上层"看起来是同一个 rig"的既有设计目标（`ICharacterRig` 本就是为了统一两种外形类型的操作面）相悖。
3. **武器风格来源直接在 `item.template` 新增 `weapon_style_ref` 字段，绕开 `display.map`**：好处是查询路径少一跳（不必先查 `display.map`）。否决理由——09 第 4.4 节已经明确拍板"武器表现档案是 DisplayInfo 体系的一部分"，关联方式是 `item.template.display_ref → display.map.weapon_style_ref`，`item.template` 本身属于载体层（数值/规则），在其中新增一个纯表现属性字段会打破"数值侧与表现侧互不干扰"这条既有分工（09 第 4.4 节"武器的数值侧与表现侧互不干扰"原文），且现有 `display.map.logical_id` 已经可以直接承担"物品模板 id → 该模板的 DisplayInfo"这条查询，不存在必须新增字段才能打通的真实缺口。
4. **命中帧同步不做超时兜底，命中帧事件永远不来就永远不播放**：好处是实现更简单。否决理由——命中帧事件依赖具体引擎适配层的动画事件回调是否可靠触发（如剪辑被中途打断、资源加载失败导致剪辑根本没有播放过），若无兜底，一次异常会导致对应的命中反馈永久丢失且没有任何诊断信息，与 09 第 1 节表现层一贯"缺表现资源不阻断游戏、只降级"的宽容策略相悖。

## 魔兽世界的对应做法

魔兽世界客户端的角色模型（骨骼动画、装备槽位网格替换、武器挂点）与序列帧类精灵是完全不同的两套底层资产管线，但对上层战斗系统而言二者都统一表现为"这个单位现在应该播放哪个动作、挂了什么装备"，客户端不需要按资产类型分别写战斗表现逻辑；武器的攻击特效、命中特效随装备变化实时切换，命中判定的逻辑时机与动作剪辑的视觉命中帧由客户端独立的时间轴对齐系统协调，而不是让战斗结算直接依赖动画播放进度。本 ADR 的 `ICharacterRig` 统一命中帧事件、"实体 → 武器风格引用"查询、命中帧同步的超时兜底三项决策，分别对应这三点在单机 2.5D 场景下的最小实现。

## 修订记录

- **2026-09-08（PJ130-04 根治）：决策 3 的落地方式收窄——`HitFrameReached` 改回可选接口 `IHitFrameEmitter`，不再是 `ICharacterRig` 的强制成员。**
  - **问题**：决策 3 原文"`HitFrameReached` 从 `SpriteCharacterRig` 的专属成员提升进 `ICharacterRig` 契约本身"直接给 `ICharacterRig` 新增了一个强制事件成员；[11_工程规范与测试.md](../11_工程规范与测试.md) 第 156 行把"契约签名变化"定义为 MAJOR 级变更判据，而这条修改随 1.3.0（次版本号）发布，构成编译级破坏性变更——任何继续实现 1.2.0 及更早版本 `ICharacterRig` 形状的外部代码（游戏层自定义 CharacterRig 实现，若存在）升级到 1.3.0 会直接编译失败，与"次版本号发布不应破坏既有编译"的既定语义矛盾。第六轮文档—代码深度审计（`architecture/落地计划/audit-5c444f1-20260908/`）PJ130-04 条目记录了这一发现。
  - **决定**：命中帧到达改由新增的可选接口 `Presentation.Render.IHitFrameEmitter`（`event Action<Id>? HitFrameReached`）承载；`ICharacterRig` 本身不再声明该成员。`SpriteCharacterRig`/`ModelCharacterRig` 两个框架自带实现同时实现 `ICharacterRig` 与 `IHitFrameEmitter`，事件触发的时机、次数、参数语义与决策 3/4 原文完全不变，`sprite`/`model` 两种外形类型的表现能力不受影响。消费方（`Presentation.FeedbackBinder.Core.CharacterRigHitFrameSource`）按 `rig is IHitFrameEmitter emitter` 探测：探测到则订阅，探测不到则视为"该 rig 不参与命中帧同步"（登记仍然成功，`HasRig` 仍返回 true），不抛异常——与 `RenderOptions.HitFrameSync` 未设为 `AnimKeyframeDriven` 时 rig 从不触发是同一种可观察结果，不引入新的失败模式。
  - **不改变的部分**：决策 1、2、4、5、6 与本决策 3 的其余内容（命中帧统一事件语义、命中帧同步超时兜底、武器风格来源、`StateChangedWithSkill`）均不受影响，`sprite`/`model` 两种外形类型均已实际接通命中帧同步，行为与本 ADR 原文一致。
  - **迁移说明**（供 CHANGELOG 使用）：1.3.0 起若通过 `rig.HitFrameReached` 直接访问（`ICharacterRig` 静态类型持有的变量）编译失败，改为 `(rig as IHitFrameEmitter)?.HitFrameReached`，或改用 `SpriteCharacterRig`/`ModelCharacterRig` 具体类型持有的变量（两者仍直接声明该事件）。自行实现 `ICharacterRig` 且不需要命中帧同步的外部代码不需要任何改动。
  - **文档同步**：[09_表现层.md](../09_表现层.md) 第 4.1/4.3 节的 `ICharacterRig.HitFrameReached` 表述已按本条勘误改为可选接口 `IHitFrameEmitter`（版本号不变，见该文档变更记录）。

- **2026-09-08（第七方审核 PJ140-01/PR140-02 跟进）：决策 1"renderer 只消费已加载资源、不隐式加载"改为按本 ADR 原文改代码；决策 3 的兼容边界进一步收紧到"源码兼容"。**
  - **问题一（决策 1 落地不彻底）**：`audit-c86bfa9-20260908/` 第七方审核发现引擎适配层的 `IRenderer3D` 实现里，`CreateModelInstance` 缓存未命中时自己直接调用引擎资源系统的同步读取接口，绕开了本决策"renderer 只消费已加载/占位资源"的字面要求（只是恰好读到的资源与异步路径最终会加载到的是同一份，结果上不产生可观察差异，但"谁来碰引擎资源系统"这件事没有像决策原意那样收口在加载器一处）。
  - **决定一**：不改本决策文字（原文本就写得是"renderer 只消费已加载/占位资源、不隐式加载"，是实现没跟上，不是决策本身需要调整）——引擎适配层改为把"按 id 同步解析一次资源"这件事从 renderer 挪进资源加载器自身的一个同步方法，renderer 不再直接调用引擎资源系统的任何读取接口，两条路径（同步解析、`loadAsync` 异步排队）共用同一份缓存与解析逻辑，不重复实现。可观察行为不变（仍然优先命中缓存，未命中时同步解析一次，两条路径都找不到时占位 + 异步重试），只是"谁负责碰引擎资源系统"收口到唯一一处，符合决策 1 的字面要求。
  - **问题二（PJ130-04 的兼容边界不够，第七方审核 PJ140-01）**：PJ130-04 把 `HitFrameReached` 从 `ICharacterRig` 拆到可选接口 `IHitFrameEmitter` 后，仍然不足以保住全部 1.3 源码兼容——继续实现旧版 `ICharacterRig` 形状、把变量声明为 `ICharacterRig`（不是 `IHitFrameEmitter`）再直接 `rig.HitFrameReached += ...` 的 1.3 消费方代码，升级到 1.4 后仍然编译失败（`ICharacterRig` 本身确实不再声明该成员）；同一轮审计还确认 `ViewKind` 那一次"与具体引擎核心类型同名的旧成员名改名为 `Gobj`"（技术名去模糊，方向本身正确，见上文"背景"第 1 项相邻的第八方审核结论）同样破坏了引用旧成员名的 1.3 消费方源码。CHANGELOG 迁移说明与"不破坏既有编译"的宣称因此不一致。
  - **决定二**：不撤销 PJ130-04 的接口拆分与 `Gobj` 改名（两者的方向判断本身仍然成立），改为两处各加一层"源码兼容层"：`ICharacterRig` 用默认接口实现原样加回 `HitFrameReached` 事件成员（add/remove 转发到 `this as IHitFrameEmitter`，探测不到时静默 no-op，不持有独立状态），标记为已过时、引导改用 `IHitFrameEmitter`；`ViewKind` 加回与 `Gobj` 数值相同的旧成员名作为别名（具体标识符不在架构正文出现，落地见下方"文档同步"给出的代码位置），同样标记为已过时。两处兼容层都只影响"继续按旧写法引用旧成员名"的消费方源码能否编译，不改变 `SpriteCharacterRig`/`ModelCharacterRig`/`Gobj` 本身的运行期行为，也不要求 `ICharacterRig` 的其余实现新增任何成员。
  - **不改变的部分**：本 ADR 其余决策（2、4、5、6）与本条决定一、二涉及的两条决策（1、3）除上述收窄外的部分均不受影响。
  - **文档同步**：本条只更新 ADR 修订记录，[09_表现层.md](../09_表现层.md)/[02_引擎适配层.md](../02_引擎适配层.md) 正文对决策 1/3 的表述本身与本 ADR 原文一致，不需要改写；两处兼容层的具体落地位置与判断记录见 `presentation/render/contracts/ICharacterRig.cs`、`presentation/common/contracts/ViewKind.cs`（后者的旧成员名标识符本身只出现在代码里，见该文件类型判断记录）。

- **2026-09-08（第八轮文档—代码深度审计 `architecture/落地计划/audit-3224ca1-20260908/` 跟进）：决策 1"renderer 不得隐式加载"的措辞歧义澄清，不改决策本身。**
  - **问题**：决策 1 原文"renderer 只消费已加载/占位资源、不隐式加载"字面上容易被读成"任何首次引用触发的加载都禁止"，与某具体引擎适配层实现里"资源加载器在缓存未命中时同步完成一次加载"（第七方审核 PJ140-01 跟进已把"谁碰引擎资源系统"收口到加载器一处，见上一条修订记录）表面冲突。
  - **决定**：明确决策 1 实际禁止的是**渲染器等消费方接口实现绕过 `IResourceLoader` 自行读取资源**（例如直接调用引擎原生的资源读取 API）；首次引用某资源 id 时，由 `IResourceLoader` 的实现同步或异步完成该次加载（例如缓存未命中时同步解析一次）均属允许，只要消费方实现本身只经 `IResourceLoader` 取用资源、不自行调用引擎原生读取 API，即视为遵守本决策。当前具体引擎适配层的资源加载器在渲染器触发缓存未命中时同步补一次解析，正是这一允许范围内的实现，不构成对决策 1 的违反。
  - **不改变的部分**：决策 1 的加载责任归属（谁首次引用谁负责促成加载）与本 ADR 其余决策均不受影响，本条只是澄清"不得隐式加载"这一措辞的适用边界。
  - **文档同步**：与 [02_引擎适配层.md](../02_引擎适配层.md) 第 1.7 节、变更记录表 2026-09-08 勘误口径，以及具体引擎适配层包说明文档里 `IRenderer3D` 对应实现行的同批修订保持一致（落地位置见 `architecture/落地计划/选型/` 与具体引擎适配层包目录，正文不点名）。

- **2026-09-08（第九方审核 `architecture/落地计划/audit-85f1f4f-20260908/` 根治：AUD-05"样例 model 槽位换装把模型引用按独立网格读取，槽位网格被清空"+ 动画剪辑事件登记契约差异，两条，均不改决策本身，只补充资源合同细节）。**
  - **问题一（AUD-05，P2，定向探针复现）**：`display.equip_visual.mesh_ref` 此前没有明确的资源合同——某具体引擎适配层实现把它当独立网格资产直接读取，而样例数据 `mesh_ref: model.placeholder_biped` 实际是一个模型型 `model_ref` 引用（资源加载器缓存的是完整模型实例数据，不是独立网格），读取结果为空并把原本非空的槽位网格清空成不可见——装备意图被静默变成"看不见"，且该渲染器实现不是从注入的 `IResourceLoader` 已加载资源解析，而是直接调用引擎原生资源读取 API，构成对决策 1"消费方只经 `IResourceLoader` 取资源"的字面违反。
  - **决定一**：`mesh_ref` 的资源合同明确为"引用与 `model_ref` 同一条 `model` 种类资源，不单独新增资源种类"——具体渲染器实现解析 `mesh_ref` 时：若该资源解析为模型预制体，须从中提取网格（优先取与槽位 id 同名的子对象，找不到时退回预制体上首个挂网格渲染组件的子对象）；若该资源本身是独立网格资产，直接使用。渲染器只从注入的 `IResourceLoader` 已加载/可提取的资源解析，不得绕过该接口直接读取引擎原生资源系统；`mesh_ref` 缺失、尚未加载完成或无法提取出网格时，须保留该槽位当前网格（不清空为不可见）并记一条诊断，加载/提取成功后原地替换到同一存活实例——与决策 1"资源缺失用内建占位、不隐式加载、不抛异常"同一套宽容立场的延伸，不是新决策。
  - **问题二（静态契约差异，同一份审核报告一并记录，未单独计入确认缺陷）**：model 动画关键帧事件登记（把 `display.anim_set.clips[*].events` 数据驱动写回剪辑资产自身携带的关键帧事件属性）此前直接调用引擎原生资源读取 API 取剪辑，同样违反决策 1；且登记时整体覆盖该属性，会丢弃该剪辑资产美术自带的事件，也会让"第一个触碰该 `resource_ref` 的 `display.anim_set`"的事件配置决定其它共享同一份剪辑资产、但声明了不同事件配置的 anim_set 实际看到的事件（先到先得，覆盖式写入的必然后果，不是刻意设计）。
  - **决定二**：新增 `IResourceLoader` 的 `animation_clip` 资源种类（见 02 第 1.7 节勘误），剪辑资产本身与其内建关键帧事件均须经该种类取用；具体消费方（引擎适配层的装配代码）登记数据驱动关键帧事件时改为"合并"：以该剪辑资产在本次登记之前的原始（美术自带）事件为基线，数据驱动定义中出现的事件名替换掉基线里的同名旧定义，其余美术自带事件原样保留。按 anim_set 隔离生效范围：同一 `resource_ref` 第一次被登记事件时直接合并写在共享资产上；后续遇到同一 `resource_ref` 但事件配置不同的另一个 `display.anim_set` 时，不得再次覆盖共享资产，改为运行期克隆一份私有副本承载这套不同的事件配置，只影响引用该 anim_set 的具体实例（游戏内容层不需要为规避互相覆盖而给每个 anim_set 各自复制一份剪辑文件）。
  - **不改变的部分**：本 ADR 全部既有决策（1～6）的结论本身不变——决定一、二都是"决策 1（消费方只经 `IResourceLoader` 取资源）+ 决策 1 的宽容降级立场"在两个具体资源合同上的延伸细化，不是新增决策，也不改变 `mesh_ref`/`resource_ref` 字段本身的数据形状（04 第 7.1.1、7.1.2 节字段表不变）。
  - **文档同步**：[02_引擎适配层.md](../02_引擎适配层.md) 第 1.7 节、[09_表现层.md](../09_表现层.md) 第 4.1/4.3 节、[14_资产规格书模板.md](../14_资产规格书模板.md) 第 1.2/4.3.1 节均已按本条追加勘误（版本号不变，见各文档变更记录表本行 ADR 列"—"——本条只追加 ADR 修订记录，未新建 ADR 编号）；落地位置见具体引擎适配层包的渲染器/资源加载器/View 工厂实现与对应测试（正文不点名）。

- **2026-09-08（第十轮审核 `architecture/落地计划/audit-8160178-20260908/`「PRES-17-01」根治：上一条"决定二"里"同一 `resource_ref` 第一次被登记事件时直接合并写在共享资产上；后续遇到……不同的另一个 `display.anim_set` 时……改为运行期克隆一份私有副本"这一具体算法本身是新的污染源，收窄为"任何 anim_set（含空事件配置）都不得写共享剪辑资产"，不改变本 ADR 决策 3 的语义本身）。**
  - **问题**：上一条"决定二"字面上给"第一个触碰某 `resource_ref` 的 `display.anim_set`"开了一个直接写共享资产的特例（"后续"才需要克隆隔离），且没有提到空事件配置（`clips[*].events` 为空数组）与"登记状态本身的存活范围"两点。第十轮审核发现具体引擎适配层实现严格照这条特例落地后，产生两类真实可复现的污染：一，空事件配置的 anim_set 被直接跳过登记，实际播放时看到的是共享资产当时的任意状态（可能已经被"第一个"非空配置写脏）；二，"登记过哪个 `resource_ref`""捕获的美术自带基线快照""已经克隆好的隔离副本"这三项状态被实现成消费方内部某个装配周期新建对象的实例状态，导致该对象重建（典型触发：场景重进）后，新对象把上一个对象已经写入共享资产的数据事件当成"美术自带基线"一并保留——是"决定二"字面允许的直接写共享资产这一特例，与"消费方内部对象可以重建"这一无关但同样合理的既有假设叠加后才暴露的组合缺口，不是某一处单独看就错的实现。
  - **决定**：收窄"决定二"为"任何 anim_set 都不得把合并结果写回共享剪辑资产本身，不区分是不是第一个触碰该 `resource_ref` 的 anim_set"；空事件配置的 anim_set 视为"当前生效范围就是这份资产的美术自带基线"，同样纳入本条隔离要求，不是可以跳过登记的特例。承载"美术自带基线快照""已经克隆好的隔离副本"这两项状态的存储必须以资源本身的引用（`resource_ref`，及区分不同事件配置的签名）为键，能够跨越消费方内部任何对象的重建（场景重进、实体销毁重建等）继续生效，不得绑定在某个随装配周期新建、旧对象销毁后状态即丢失的对象实例上。可观察行为不变的部分：合并（不整体覆盖美术自带事件）、按 anim_set 隔离生效范围（互不覆盖）两条既有结论完全不变，本条只收紧"写共享资产的特例"与"状态存活范围"这两处此前留有歧义、被证实会被踩中的空子。
  - **不改变的部分**：本 ADR 决策 1～6 的结论本身、上一条"决定一"（`mesh_ref` 资源合同）均不受影响；决策 3"命中帧事件语义"本身也不变，本条只是决策 3 落地方式的进一步收紧，同上三条已有修订记录同一惯例。
  - **文档同步**：[09_表现层.md](../09_表现层.md) 第 4.3 节、[14_资产规格书模板.md](../14_资产规格书模板.md) 第 4.3.1 节原有的"合并 + 按 anim_set 隔离"表述本身是资源合同层面的通用结论，未描述"第一个可以直接写共享资产"这一具体算法细节，本条收窄不改变其结论，不需要改写（不追加勘误行）。[02_引擎适配层.md](../02_引擎适配层.md) 第 1.7 节现有表述"运行期克隆一份私有副本承载『非首个』anim_set 的事件配置"逐字复述了本条收窄前的旧算法，与本条修订后的结论不一致，需要在下一次触达 02 正文的变更里一并改写为"任何 anim_set 均运行期克隆隔离，不写回共享资产"（本次修订的写入范围不含 02 正文，此处如实记录待办，不代为改写）；落地位置见具体引擎适配层包的 View 工厂实现（`s_authoredClipEvents`/`s_modelClipOverrides` 判断记录）与对应测试。
