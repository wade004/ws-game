# 资产规格书模板 v1

> **目的**：给出框架级的资产规格书模板——覆盖精灵与方向、纸娃娃、锚点、影子、特效、图标与界面元素、音效与音乐、场景与地图、交付目录与命名、导入工具契约，使任何新游戏在其上填具体值即可落地为该游戏的资产接入记录，不需要每款游戏各自重新设计一遍规格结构（决策依据见 [ADR-0014](adr/0014-资产契约导入工具与UI套件是框架交付物.md)）。
> **日期**：2026-09-05
> **状态**：已定稿（设计层，待用户确认）
> **上游文档**：[09_表现层.md](09_表现层.md)、[04_数据与内容管线.md](04_数据与内容管线.md)、[ADR-0014](adr/0014-资产契约导入工具与UI套件是框架交付物.md)

## 变更记录

| 日期 | 摘要 | 关联 ADR |
|---|---|---|
| 2026-09-05 | 新增本文档：落地计划阶段 5"撰写资产规格书 v1"的框架级模板产出物，归属 `architecture/` 文档集本身，随文档版本规则版本化；每款游戏在其上填具体值落到该游戏的接入记录。 | [ADR-0014](adr/0014-资产契约导入工具与UI套件是框架交付物.md) |

本文档每节先给"框架固定项"（结构、字段位、机制，全部游戏通用，不得自行增删），再给"游戏填写项"表格（具体数值，占位为 `<待填>`，由每款游戏在自己的接入记录中落实，参见 [13_新游戏接入指南.md](13_新游戏接入指南.md) 第 6 节）。

## 1. 总则

### 1.1 规格书与 `display.map` 的关系

本规格书每一节规格最终都要落到 [04_数据与内容管线.md](04_数据与内容管线.md) 第 7.1 节 `display.map`（或其展开表）的具体字段上；资产导入工具（见第 11 节）读取按本规格书产出的资源与标注，自动生成下表右列字段的数据行：

| 规格类别（本文档章节） | 对应字段 / 表 |
|---|---|
| 精灵集与方向（第 2 节） | `display.map`（`kind: sprite`）的 `sprite_set_id`、`direction_count`、`mirror_pairs` |
| 纸娃娃层（第 3 节） | `display.map`（`kind: sprite`）的 `paperdoll_layers` |
| 锚点（第 4 节） | `display.map`（`kind: sprite`）的 `anchor_points` |
| 影子（第 5 节） | `display.map`（公共字段）的 `shadow` |
| 场景排序偏移 | `display.map`（公共字段）的 `sort_offset` |
| 图标（第 7 节） | `display.map`（公共字段）的 `icon_id` |
| 特效（第 6 节） | `display.map` 的 `vfx_id`，展开定义见 `vfx.def`（09 第 5.1 节） |
| 音效与音乐（第 8 节） | `display.map` 的 `sfx_id`，展开定义见 `sfx.def`（09 第 5.2 节） |
| 武器表现（挥舞/命中特效等） | `display.map` 的 `weapon_style_ref`，展开定义见 `display.weapon_style`（09 第 4.4 节） |
| model 型槽位/挂点/换装（若该游戏选用 `model` 型外形，见 13 口味配置清单） | `display.map`（`kind: model`）的 `sockets`、`slots`、`default_slot_meshes`；`display.anim_set`（04 第 7.1.1 节）；`display.equip_visual`（04 第 7.1.2 节） |

本框架默认表现路线是 `sprite` 型外形（见 13 口味配置清单"外形类型"一项），本规格书第 2～4 节按此默认路线展开；游戏若选用 `model` 型外形，槽位/挂点/换装的结构规则已在 09 第 3.3.2 节与 04 第 7.1.1、7.1.2 节定义，本规格书不重复展开，仅在上表登记对应关系。

### 1.2 命名规则

**资源引用 id**（`sprite_set_id`、`icon_id`、`model_ref`、`vfx.def`/`sfx.def` 的 `resource_ref` 等，均为"由引擎适配层解析的不透明字符串"，见 04 第 7.1 节字段说明）不属于 04 第 2.2 节的固定内容域名清单——那份清单约束的是技能/物品/生物等逻辑记录的 id 归属域，走新增域名审批；资源引用 id 是另一套命名空间。本规格书建议其书写沿用 04 第 2.1 节的点分格式，并使用资源类别前缀保持可读性（例如 `sprite.`、`icon.`、`model.`、`anim.`、`mesh.` 等，`vfx.`/`sfx.` 本身已是 04 登记的内容域，二者语义统一），具体前缀集合与命名细节由每款游戏在其接入记录中定义，不需要走 04 第 2.2 节的域名审批流程。

**文件名**在资源引用 id 的基础上，按"去掉类别前缀的剩余部分（点号换下划线） + 方向档位（若适用） + 层名（若适用） + 帧序号（若适用）"的顺序拼接，模板：

```
<资源引用id去掉类别前缀，点号换下划线>[__<方向档位id>][__<层id>][__<三位帧序号>].<扩展名，具体格式见选型 02>
```

示例（沿用 09 第 7.1 节 `display.map` 示例记录 `sprite_set_id: sprite.creature.wolf_grey`）：该精灵集在方向档位 `front` 下、纸娃娃层 `body` 的文件名为 `creature_wolf_grey__front__body.<位图扩展名>`；若该层还是一段序列帧动画的第 3 帧，文件名为 `creature_wolf_grey__front__body__002.<位图扩展名>`（帧序号从 0 起）。

## 2. 精灵与方向

### 2.1 框架固定项

- 方向档位数量默认 **8**，允许 4 或 16（对应 04 `display.map.direction_count` 枚举 `4|8|16`，见 09 第 3.2 节、13 口味配置清单）。
- 方向档位命名沿用 09 第 3.2 节镜像规则表的档位族：`front`、`front_side`、`side`、`back_side`、`back`。8 方向默认下，`front`、`back` 各为独立档位（左右对称，无需镜像），其余三个档位族各拆出 `_r`（原创绘制）与 `_l`（镜像得到）两个具体档位，共 8 个：

  | 档位族 | 具体档位 id | 是否原创绘制 | `mirror_of` |
  |---|---|---|---|
  | front | `front` | 是 | — |
  | front_side | `front_side_r` | 是 | — |
  | front_side | `front_side_l` | 否（镜像） | `front_side_r` |
  | side | `side_r` | 是 | — |
  | side | `side_l` | 否（镜像） | `side_r` |
  | back_side | `back_side_r` | 是 | — |
  | back_side | `back_side_l` | 否（镜像） | `back_side_r` |
  | back | `back` | 是 | — |

  4 方向下只保留 `front`、`side_r`、`side_l`（镜像 `side_r`）、`back`；16 方向在每两个相邻档位族之间插入过渡档位，命名与镜像方式按同一模式延伸，具体新增档位 id 由游戏侧登记。
- 镜像规则字段结构与 09 第 3.2 节镜像规则表一致（`direction_slot`/`mirror_of`/`flip_x`），对应 `display.map.mirror_pairs`。
- 序列帧的帧序号从 0 起按播放顺序编号，写入文件名帧序号段（见第 1.2 节命名模板）；`clipId` 与图集切图的对应关系见 09 第 4.5 节 `FrameAnimPlayer`。

### 2.2 游戏填写项

| 字段 | 说明 | 值 |
|---|---|---|
| `pixels_per_unit` | 世界单位与像素的换算比 | `<待填>` |
| 角色基准尺寸 | 标准体型角色的像素高宽（用于影子、锚点等按比例的规格换算基准） | `<待填>` |
| 每行帧数 | 序列帧图集内每行排布的帧数 | `<待填>` |
| 默认帧率 / 单帧时长 | 传给 `FrameAnimPlayer.play` 的 `speed` 换算依据 | `<待填>` |
| 帧时长登记位置 | 由资源本身内嵌，还是该游戏接入记录里的独立表登记（架构本身不规定，见 09 第 4.5 节） | `<待填>` |

## 3. 纸娃娃

### 3.1 框架固定项

- **层清单**：`body`、`head`、`hair`、`chest`、`legs`、`hand_main`、`hand_off`、`back`、`effect_overlay` 是框架给出的**建议默认层集合**，对应 `display.map.paperdoll_layers`；由游戏按实际换装深度裁剪增减，不强制全部启用。
- **叠放顺序的互换规则**：`hand_main`、`hand_off`、`back` 三层的层内 z 序在"面向镜头"方向族（`front`/`front_side`）与"背对镜头"方向族（`back`/`back_side`）之间必须互换——武器/背部物件在角色面向镜头时应画在身体前方，在角色背对镜头时应画在身体后方；本规格书固定这条互换规则本身，具体每层数值由下表填写。
- **画布对齐要求**：同一逻辑对象的全部纸娃娃层必须使用相同画布尺寸、相同原点，任意层组合叠放时无需额外位移即可对齐；这是 09 第 3.3.1 节"每层声明一个锚点、跨层复用同一套锚点偏移"的前提。
- **透明与描边一致性**：各层边缘的透明通道处理方式与描边风格必须跨层统一，避免组合后出现视觉接缝。

### 3.2 游戏填写项

层内 z 序表模板（从底到顶，数字越大越靠前；`side` 系方向族按该游戏的实际视觉需要归入 front 系或 back 系，或单独一列）：

| 层 | front 系方向族 z 序 | back 系方向族 z 序 |
|---|---|---|
| `body` | `<待填>` | `<待填>` |
| `head` | `<待填>` | `<待填>` |
| `hair` | `<待填>` | `<待填>` |
| `chest` | `<待填>` | `<待填>` |
| `legs` | `<待填>` | `<待填>` |
| `hand_off` | `<待填>` | `<待填>`（须与 `hand_main` 相对位置互换，见 3.1） |
| `hand_main` | `<待填>` | `<待填>`（须与 `hand_off` 相对位置互换，见 3.1） |
| `back` | `<待填>` | `<待填>` |
| `effect_overlay` | `<待填>` | `<待填>` |

| 字段 | 说明 | 值 |
|---|---|---|
| 描边宽度 | 跨全部纸娃娃层统一的描边像素宽度（可与第 7 节图标描边共用一套规则） | `<待填>` |
| 透明通道处理方式 | 例如边缘羽化程度、是否允许半透明像素 | `<待填>` |

## 4. 锚点

### 4.1 框架固定项

- **锚点清单**：`root`、`hand_main`、`hand_off`、`head`、`chest`、`feet`、`overhead` 是框架建议的默认集合，对应 `display.map.anchor_points` 的 key 集合，供特效挂点、头顶信息（伤害飘字/姓名牌）、装备挂接使用；游戏按需裁剪增减。
- **标注方式（机制）**：锚点坐标由资产导入工具从原始出图产物的标注图层或独立标注文件中读取，自动写入 `anchor_points`/`offset_by_direction` 字段，不由内容作者手填坐标数值（呼应 11 第 2.1 节导入工具职责与 [ADR-0014](adr/0014-资产契约导入工具与UI套件是框架交付物.md)"外形表行由工具生成而非手填"）；标注图层/标注文件的具体格式由资产导入工具实现决定，本规格书只约束"标注驱动、非手填"这条机制，不约束具体标注载体格式。

### 4.2 游戏填写项

偏移表模板（对应 `anchor_points`/`offset_by_direction` 字段结构，见 09 第 3.3.1 节锚点表；`offset_by_direction` 缺省时用 `offset`，每个已启用的方向档位各占一列）：

| `anchor_id` | `parent_layer` | `offset`（默认） | `offset_by_direction.front` | `offset_by_direction.<其余已启用方向档位>` |
|---|---|---|---|---|
| `root` | `<待填>` | `<待填>` | `<待填>` | `<待填>` |
| `hand_main` | `<待填>` | `<待填>` | `<待填>` | `<待填>` |
| `hand_off` | `<待填>` | `<待填>` | `<待填>` | `<待填>` |
| `head` | `<待填>` | `<待填>` | `<待填>` | `<待填>` |
| `chest` | `<待填>` | `<待填>` | `<待填>` | `<待填>` |
| `feet` | `<待填>` | `<待填>` | `<待填>` | `<待填>` |
| `overhead` | `<待填>` | `<待填>` | `<待填>` | `<待填>` |

## 5. 影子与高度

### 5.1 框架固定项

`shadow` 字段三种模式（`display.map.shadow`，见 04 第 7.1 节、09 第 3.4 节）：`none`（不渲染）、`blob`（贴地简化影子）、`projected`（投影阴影）。影子锚定逻辑平面坐标，不随高度位移；高度偏移（跳跃、击飞等）只平移精灵纵向绘制位置，不平移影子（09 第 3.4 节）。

### 5.2 游戏填写项

`blob` 模式的通用影子贴图规格：

| 字段 | 说明 | 值 |
|---|---|---|
| 贴图复用策略 | 是否全部角色共用一张通用椭圆影子贴图，还是按体型分档各一张 | `<待填>` |
| 影子尺寸比例 | 相对第 2 节"角色基准尺寸"的宽高比例 | `<待填>` |
| 基础透明度 | `<待填>` |
| 高度联动规则 | 高度偏移增大时影子是否同步缩小/降低透明度以增强离地感，及具体曲线 | `<待填>`（框架建议：随高度增大适度缩小并降低透明度，具体数值由游戏定） |

`projected` 模式通常由 `IRenderer3D.setShadow`（09 第 3.4 节）驱动，一般不需要专属贴图资产，具体渲染参数不属于本规格书覆盖范围。

## 6. 特效

### 6.1 框架固定项

`attach_mode` 四种类型（`vfx.def.attach_mode`，见 09 第 5.1 节）对资产形态的不同要求：

| `attach_mode` | 资产要求 |
|---|---|
| `world` | 素材以世界单位（第 2 节 `pixels_per_unit`）为参考尺度制作，不需要匹配任何锚点/挂点 |
| `anchor` | 素材需要与 `sprite` 型外形的锚点体系（第 4 节）对齐，挂接基准点匹配目标锚点的偏移语义，跟随锚点位移旋转 |
| `socket` | 素材（或独立子模型）需要与 `model` 型外形的挂点体系（09 第 3.3.2 节）对齐；通常表现为独立可挂接资源而非贴图序列，具体格式不在本文档 `sprite` 路线展开 |
| `screen` | 素材以下方"屏幕空间参考画布"为尺度制作，不随世界坐标缩放 |

### 6.2 游戏填写项

贴图序列规格：

| 字段 | 说明 | 值 |
|---|---|---|
| 单帧尺寸 | | `<待填>` |
| 帧数 | | `<待填>` |
| 循环点 | 若特效循环播放，标注从第几帧起循环回放（对应 `vfx.def.lifetime` 与是否循环，见 09 第 5.1 节） | `<待填>` |

屏幕空间特效的参考画布：`<待填，与该游戏在 09 第 3.6 节、13 口味配置清单最终选定的参考分辨率一致>`。

## 7. 图标与界面元素

### 7.1 框架固定项

- 配色层级机制：图标配色按 `item.quality_definition`（04 第 1.1 节，品质分档定义表，含 id、显示名文本键、排序权重、预算倍率）的分档 id 映射到色相定位；颜色具体色值不写死在逻辑表（呼应 09 第 6.2 节飘字样式表"颜色只经色值引用间接指定"的同一原则），本规格书只登记"分档 id → 建议色相"的映射结构。
- UI 九宫格切片机制：可拉伸的 UI 边框类素材统一采用九宫格切片——四角固定尺寸不拉伸、四边中段单方向拉伸、中心双向拉伸；具体边距像素值为游戏填写项。

### 7.2 游戏填写项

图标尺寸网格（建议值）：

| 用途 | 建议尺寸 | 该游戏取值 |
|---|---|---|
| 技能/光环图标 | 64×64 或 128×128（建议取 2 的幂次，便于图集打包） | `<待填>` |
| 物品图标 | 同上 | `<待填>` |
| 成就/状态小图标 | 建议为主图标尺寸的一半 | `<待填>` |

构图规则：

| 字段 | 说明 | 值 |
|---|---|---|
| 主体占比 | 图标画布内主体轮廓占比，建议 70%~85%，四周留边避免贴边裁切 | `<待填>` |
| 描边宽度 | 按图标尺寸的固定比例给出统一描边宽度，跨全部图标一致，可与第 3 节纸娃娃层描边共用同一套规则 | `<待填>` |

配色层级映射表（`quality_definition id` 以该游戏 `item.quality_definition` 数据为准，此处仅给结构与建议色相定位）：

| `quality_definition` 分档（示例占位） | 建议色相定位 | 该游戏取值 |
|---|---|---|
| `<待填，如"普通"档 id>` | 中性色（灰/白） | `<待填>` |
| `<待填，如"优秀"档 id>` | 冷色（绿/蓝） | `<待填>` |
| `<待填，如"稀有"档 id>` | 中高饱和冷色（蓝/紫） | `<待填>` |
| `<待填，如"史诗"档 id>` | 高饱和暖色（紫/橙） | `<待填>` |
| `<待填，如"传说"档 id>` | 最高辨识度专属色 | `<待填>` |

UI 九宫格边距（像素）：`<待填>`。

字体要求：开源可商用授权；字符集覆盖中文常用字集（建议至少覆盖国标一级字集范围）与数字、常用标点、英文字母；具体字体名称登记在 [选型/02_美术与工具选型.md](选型/02_美术与工具选型.md)，本文档不写死字体名。

## 8. 音效与音乐

### 8.1 框架固定项

- 格式：位图/音频需统一格式，不逐个资源自定义扩展名；具体格式名登记在 [选型/02_美术与工具选型.md](选型/02_美术与工具选型.md)。
- 命名与分层：文件命名需体现所属分层（对应 09 第 5.2 节 `sfx.def.layer` 字段，如战斗/环境/UI/语音），采用第 1.2 节命名模板的类别前缀段承载分层信息。
- `variants` 变体命名规则（对应 `sfx.def.variants`，09 第 5.2 节）：同一逻辑音效的多个随机变体文件在基础名后追加两位数字后缀，如 `<基础名>_01`、`<基础名>_02`……，数量与顺序不设强制上限，由该游戏音效制作量决定。
- 音乐循环点标注方式（机制）：与第 4 节锚点标注方式同一原则——由约定的标注载体（可选音频文件内嵌标记，或旁路登记表）传给播放调用方，具体标注载体格式为游戏填写项，本规格书不规定统一格式。

### 8.2 游戏填写项

| 字段 | 说明 | 值 |
|---|---|---|
| 采样率 | | `<待填>` |
| 声道数 | 音效/音乐是否统一单声道或立体声 | `<待填>` |
| 响度基准值 | 全部音效/音乐统一到的响度基准，避免同批素材忽大忽小，混音期按该基准归一化 | `<待填>` |
| 音乐循环点标注载体 | 内嵌标记 / 旁路登记表二选一 | `<待填>` |

## 9. 场景与地图

### 9.1 框架固定项

- 分层导出（对应 09 第 3.1 节层结构）：地面图（地面层）、装饰层（不遮挡单位的贴花/痕迹）、前景遮挡层（遮挡单位的近景建筑/树冠）三者作为独立文件分层导出，不合并成单张整图，以便各自独立参与 09 第 3.1 节的固定排序层级（地面层最底、装饰层其上单位层之下、前景遮挡层单位层之上）。
- 导航与碰撞不属于本规格书出图范畴：导航数据与碰撞区域由引擎适配层侧在场景中手工绘制（呼应 09 第 3.1 节六层结构中"地面层/装饰层/单位层/前景遮挡层/特效层/UI 层"均为可视呈现层，导航碰撞不是其中的可视资产层），资产规格书只覆盖可视资产。

### 9.2 游戏填写项

| 字段 | 说明 | 值 |
|---|---|---|
| 单张地面图尺寸上限 | | `<待填>` |
| 分块建议 | 超过尺寸上限时是否切分为多张拼接，及拼接边界处理（防漏光缝隙等） | `<待填>` |

## 10. 交付目录与命名

### 10.1 框架固定项

`assets/<game>/` 目录模板（对应 [11_工程规范与测试.md](11_工程规范与测试.md) 第 1 节）：

```
assets/<game>/
  sprites/      单位与物件的方向精灵集（第 2 节）
  paperdoll/    纸娃娃分层原始件（第 3 节，若与 sprites/ 分开管理）
  vfx/          特效贴图序列（第 6 节）
  sfx/          音效（第 8 节）
  music/        音乐（第 8 节）
  icons/        图标（第 7 节）
  ui/           UI 素材（九宫格边框、主题贴图等，第 7 节）
  maps/         地面图/装饰层/前景遮挡层（第 9 节）
  fonts/        字体文件（第 7 节）
```

`assets/_placeholder/` 同结构，存放框架级占位资产包（见 11 第 2.1 节），供新游戏立项第一天即可跑通框架级验收。

文件命名沿用第 1.2 节命名模板；每个子目录内文件名前缀建议与该目录承载的资源引用类别前缀一致，便于人工核对文件与引用 id 的对应关系。

### 10.2 游戏填写项

无（本节结构固定，具体文件内容随第 2～9 节各类资产的实际产出物填入对应目录）。

## 11. 导入工具契约

对应 11 第 2.1 节、[ADR-0014](adr/0014-资产契约导入工具与UI套件是框架交付物.md)。

- **输入**：原始出图产物（各类资产的位图/音频原始素材）+ 标注（第 4 节锚点标注图层或标注文件、方向档位与层归属信息）。
- **输出**：
  - 切片/图集文件、锚点数据文件，落入第 10 节目录模板对应位置；
  - 自动生成的 `display.map`（含 `sprite`/`model` 型对应字段）、`display.anim_set`（`model` 型，见 04 第 7.1.1 节）数据行，直接写入数据表，不需要人工手填。
- **校验项**（对应 11 第 2.1 节 + 09 第 3.2 节镜像回退规则）：
  - 缺锚点：声明需要的锚点在标注中缺失；
  - 尺寸不符：产出尺寸与本规格书登记的规格（第 2、6、7、9 节各表）不一致；
  - 引用不存在：生成的资源引用 id 在对应数据表中找不到落点，或反向指向的逻辑 id 不存在；
  - 方向档位缺失且无镜像回退：某方向档位既未提供原创美术，也未在 `mirror_pairs` 中声明镜像来源，导致该方向无法渲染（对应 09 第 3.2 节镜像规则表）。

## 基础架构提供 / 游戏层提供

| 能力 | 基础架构提供 | 游戏层提供 |
|---|---|---|
| 本规格书的章节结构与字段清单 | 是 | 否，不得自行增删架构级规格类别 |
| 命名规则与目录模板的机制 | 是 | 具体前缀集合、子目录内文件组织细节 |
| 各类资产"游戏填写项"的字段位 | 是（字段位固定） | 具体数值（尺寸、像素比、响度基准、配色映射等） |
| 与 `display.map`/`display.anim_set`/`vfx.def`/`sfx.def` 等表字段的对应关系 | 是 | 具体数据行 |
| 导入工具的输入输出契约与校验项定义 | 是 | 导入工具的具体实现、具体校验阈值 |
| 具体美术/音频资源本身 | 否 | 是 |

## 验收标准

1. 任取本规格书第 2～9 节中的一条"游戏填写项"，能在该游戏的接入记录中找到对应的具体取值，不存在"规格书要求填但实际未填"的空缺。
2. 给定一份符合本规格书目录模板（第 10 节）与命名规则（第 1.2 节）的原始出图产物，资产导入工具能够无需人工改名/挪位置直接读取并生成对应的 `display.map` 数据行。
3. 任取一个启用了方向精灵集的逻辑对象，其 `direction_count` 对应的全部方向档位（含镜像）在渲染时都有画面，不存在"某方向空白"的情况（对应第 11 节镜像回退校验项）。
4. 任取一个纸娃娃层集合的换装组合，各层在同一画布同一原点对齐，肉眼核查无接缝错位（对应第 3 节画布对齐要求）。
5. 任取一条 `vfx` 记录，其 `attach_mode` 与实际交付的特效素材形态（贴图序列/独立模型/屏幕空间贴图）一致，不存在世界坐标特效却按屏幕空间尺度制作一类的错配（对应第 6 节）。
6. 全部音效/音乐素材经响度检测落在本规格书登记的响度基准容差范围内（对应第 8 节）。
7. 用规格书流程从出图到资产导入工具生成外形表数据行全程可计时，且不需要任何人工手填 `display.map` 字段（对应第 11 节输出定义）。
