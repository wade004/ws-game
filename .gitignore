# 构建产物
build/
bin/
obj/
*.user
*.suo
.vs/
.idea/
# 从 Core.sln 复制进 Unity 工程的 DLL，属于构建产物
**/Assets/Plugins/Core/
# Unity 工程的本地缓存目录
**/[Ll]ibrary/
**/[Tt]emp/
# Logs 只限定在 adapters/unity/ 下（Unity 工程自身生成的日志缓存），不能用不加前缀的
# **/[Ll]ogs/ —— 那会连带忽略仓库其它位置里任何literal 叫 "logs" 的目录，包括
# architecture/落地计划/audit-*/**/logs/ 这类审计归档要长期留存的证据目录，导致归档时看似
# "文件缺失"（2026-09-09 codex 第十二轮 P3 归档项复核发现，见该轮 WC.md）。
adapters/unity/**/[Ll]ogs/
**/[Uu]serSettings/
**/[Oo]bj/
**/*.csproj.user
**/*.sln.DotSettings.user
# Unity 工程自动生成的项目文件（仅 adapters/unity/ 前缀下，避免误伤根目录 Core.sln 与 core/**/*.csproj）
adapters/unity/*.csproj
adapters/unity/*.sln
# Python
toolchain/.venv/
.venv/
__pycache__/
*.pyc
# 本机文件
Thumbs.db
.DS_Store
dist/
adapters/unity/Packages/com.gamefoundation.adapter.unity/Runtime/Plugins/Core/
# U2-1：build.ps1 -SyncContent 生成的内容同步产物（data/_sample、assets/_placeholder 的镜像拷贝），
# 源头在仓库 data/、assets/ 下，本目录纯粹是构建期拷贝，不提交（含 Unity 为该目录及其父目录自动
# 生成的 .meta，父目录 Assets/StreamingAssets/ 本身除了这个子目录不再有任何其它内容）。
adapters/unity/Assets/StreamingAssets/
adapters/unity/Assets/StreamingAssets.meta
# Unity 测试框架（PlayMode 性能测试）每次跑测试在 Assets/Resources/ 下自动生成的运行时文件，不提交；
# Assets/Resources/ 目录此前不存在于仓库中，此次由测试框架自动创建，其 .meta 一并忽略。
adapters/unity/Assets/Resources/PerformanceTestRun*
adapters/unity/Assets/Resources.meta
# 私服交付通道（toolchain/registry/，见该目录 README.md）：node_modules 是 npm ci 安装产物，
# storage/ 是 Verdaccio 本地包存储（本机运行状态），htpasswd/.npmrc 含账号/令牌凭据，
# verdaccio.pid/*.log 是 -Detach 后台运行状态，均不提交。
toolchain/registry/node_modules/
toolchain/registry/storage/
toolchain/registry/htpasswd
toolchain/registry/.npmrc
toolchain/registry/verdaccio.pid
toolchain/registry/verdaccio.pid.meta.json
toolchain/registry/*.log
# 审计归档目录里由 dotnet build 产生的 obj-*/ 中间目录（如 audit-8160178 的 api-compat 复现工程曾误提交
# obj-current17zip/ 等），编译器自动生成的 AssemblyInfo.cs / AssemblyAttributes.cs 不是证据，不提交。
architecture/落地计划/audit-*/**/obj-*/
